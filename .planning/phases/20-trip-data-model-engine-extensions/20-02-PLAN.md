---
phase: 20-trip-data-model-engine-extensions
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/services/tripService.ts
  - src/services/tripService.test.ts
autonomous: true
requirements: [ENG-04]

must_haves:
  truths:
    - "createAndActivateTrip creates a type='trip' entry and activates the mode with that entry's UUID"
    - "listTrips returns all type='trip' entries newest-first"
    - "listTripEntries returns entries matching metadata.tripId in a single scan (no N+1)"
    - "Pure stat helpers compute correct values from in-memory arrays with zero Dexie calls"
  artifacts:
    - path: "src/services/tripService.ts"
      provides: "create-and-activate, list helpers, reactive hooks, and pure stat helpers"
      exports: ["createAndActivateTrip", "listTrips", "listTripEntries", "useTrips", "useTripEntries", "tripExpenseTotal", "tripExpensesByCategory", "tripDateRange", "tripActivityCount"]
      min_lines: 60
    - path: "src/services/tripService.test.ts"
      provides: "pure-helper unit tests + Dexie integration + reactive hook tests"
      contains: "createAndActivateTrip"
  key_links:
    - from: "src/services/tripService.ts"
      to: "entriesRepository.create + activateMode"
      via: "createAndActivateTrip two atomic writes"
      pattern: "activateMode\\('trip'"
    - from: "src/services/tripService.ts"
      to: "listTripEntries"
      via: "useLiveQuery with [tripId] dependency"
      pattern: "\\[tripId\\]"
---

<objective>
Create the new `src/services/tripService.ts` — the trip-specific business layer that delegates all
persistence to the existing `entriesRepository` and `activeMode` services. It provides
create-and-activate, list helpers, reactive `useLiveQuery` hooks, and four PURE stat helpers that
operate on in-memory `LifeLogEntry[]` with zero Dexie calls (so they unit-test without IndexedDB).

Purpose: Phases 22–24 build the trip Home, expense capture, Previous Trips, and per-trip expense
report directly on top of these functions. The pure helpers can be tested immediately.
Output: `src/services/tripService.ts` + `src/services/tripService.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-trip-data-model-engine-extensions/20-CONTEXT.md
@.planning/phases/20-trip-data-model-engine-extensions/20-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts the executor needs. Plan 20-01 (wave 1) must be complete first. -->

From src/services/db.ts (after 20-01):
  type EntryType = ... | 'trip' | 'activity'
  interface LifeLogEntry { id; domain; type; title; description?; occurredAt?; recordedAt;
                           sourceUrl?; amount?; location?; tags: string[];
                           metadata: Record<string, unknown>; syncedAt: number | null }
  Dexie indexes: entries '&id, recordedAt, domain'  (metadata + type are NOT indexed)

From src/services/entriesRepository.ts:
  entriesRepository.create(entry: Omit<LifeLogEntry,'id'>): Promise<LifeLogEntry>  // generates UUID
  (db.entries.where('domain').equals('trips') uses the domain index;
   db.entries.filter(...) is a correct full-scan for unindexed metadata at personal-app scale)

From src/services/activeMode.ts (after 20-01):
  activateMode(mode: string, label?: string, tripId?: string): Promise<void>
  activeModeRepository.get(): Promise<ActiveMode | undefined>

Reactive-hook convention (mirror entriesRepository.useEntries / activeMode.useActiveMode):
  useLiveQuery(fn, deps) with NO default value — undefined = Dexie loading; callers handle it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure stat helpers + unit tests (ENG-04 part 1)</name>
  <files>src/services/tripService.ts, src/services/tripService.test.ts</files>
  <read_first>
    - src/services/entriesRepository.ts (listDistinctValues string-guard `bump()` pattern; reactive
      hook shape)
    - src/services/db.ts (LifeLogEntry shape — amount?, occurredAt?, metadata, type)
    - 20-PATTERNS.md sections "tripService.ts — Pure stat helpers", "Pure helper: metadata string-guard",
      and the captureService.test pure-helper factory pattern
    - .planning/research/PITFALLS.md Pitfall 4 (floating-point money) and Pitfall 6 (single-pass / no N+1)
  </read_first>
  <behavior>
    - tripExpenseTotal: sums `amount` (?? 0) over entries with `type === 'expense'` only; ignores
      activity/trip entries. For amounts [10.10, 5.20], `Math.round(total * 100) / 100 === 15.30`.
    - tripExpensesByCategory: returns a `Map<string, number>` keyed by `metadata.category`; non-string
      or missing category falls under `'Uncategorized'`; only `type === 'expense'` entries counted.
    - tripDateRange: returns `{ start: min, end: max }` of defined `occurredAt` values; returns `null`
      for an empty array or when no entry has an `occurredAt`.
    - tripActivityCount: returns the count of entries with `type === 'activity'`.
  </behavior>
  <action>
    Create `src/services/tripService.ts`. Implement the four PURE helpers FIRST, with NO `db` import
    and NO `dexie` import in this region — they take `entries: LifeLogEntry[]` and import only the
    `LifeLogEntry` type from `./db`. `tripExpenseTotal(entries): number` filters `type === 'expense'`
    and reduces `amount ?? 0`; add a doc comment that callers round at display via
    `Math.round(x * 100) / 100` (raw float returned — see PITFALLS Pitfall 4). `tripExpensesByCategory(entries): Map<string, number>`
    skips non-expense entries and resolves the key as
    `typeof e.metadata.category === 'string' ? e.metadata.category : 'Uncategorized'`, summing
    `amount ?? 0` per key. `tripDateRange(entries): { start: number; end: number } | null` maps
    `occurredAt`, filters out null/undefined, returns `null` when empty, else
    `{ start: Math.min(...dates), end: Math.max(...dates) }`. `tripActivityCount(entries): number`
    counts `type === 'activity'`.

    Create `src/services/tripService.test.ts` with a `makeEntryData(overrides?)` factory (mirror the
    PATTERNS factory) and pure-helper describe blocks. These tests touch NO Dexie — no db open/close,
    no fake timers. Assert: the 15.30 rounding boundary; category grouping including the
    'Uncategorized' fallback for missing/non-string category; `tripDateRange([]) === null` and a
    correct `{start,end}` for two dated entries; `tripActivityCount` counts only activities.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/services/tripService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/services/tripService.ts` imports no Dexie symbols for the pure helpers — the four functions
      reference only the `LifeLogEntry` type. `grep -n "import.*db'" src/services/tripService.ts` shows
      only a type import (`import type { LifeLogEntry }`) at this stage.
    - Test: `Math.round(tripExpenseTotal([expense 10.10, expense 5.20, activity]) * 100) / 100 === 15.30`.
    - Test: `tripExpensesByCategory` groups by category and buckets missing/non-string category under
      `'Uncategorized'`.
    - Test: `tripDateRange([]) === null`; with two dated entries returns `{ start: min, end: max }`.
    - Test: `tripActivityCount` counts only `type === 'activity'` entries.
    - `npx tsc -b` clean; `npx vitest run src/services/tripService.test.ts` green.
  </acceptance_criteria>
  <done>Four pure stat helpers exist with zero Dexie dependency and pass their unit tests; rounding/grouping/null-range/count behaviors verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dexie repository fns + reactive hooks + integration tests (ENG-04 part 2)</name>
  <files>src/services/tripService.ts, src/services/tripService.test.ts</files>
  <read_first>
    - src/services/entriesRepository.ts (create; orderBy/reverse; filter full-scan; useEntries/useEntry
      reactive hooks with undefined-loading semantics)
    - src/services/activeMode.ts (activateMode 3-arg signature after 20-01; activeModeRepository.get)
    - src/services/activeMode.test.tsx (db.delete/open beforeEach; `vi.useFakeTimers({ toFake: ['Date'] })`;
      render/screen/act reactive-hook test pattern)
    - 20-PATTERNS.md sections "tripService.ts — Async repository / Domain-indexed query / Full-scan filter /
      Reactive hook", "Fake timer pattern (no Dexie stall)", "useLiveQuery hook shape"
  </read_first>
  <behavior>
    - createAndActivateTrip('Paris') creates a LifeLogEntry with `type:'trip'`, `domain:'trips'`,
      `title:'Paris'`, then calls `activateMode('trip','Paris', entry.id)`; returns the entry. After it,
      `activeModeRepository.get()` resolves `{ mode:'trip', label:'Paris', tripId: entry.id }`.
    - listTrips() returns only `type === 'trip'` entries, newest-first (reverse of recordedAt order).
    - listTripEntries(tripId) returns entries whose `metadata.tripId === tripId` and excludes others,
      using a single full-table filter (no per-trip loop).
    - useTrips() returns `undefined` before any write (loading), then the trip list after a create
      inside `act()`. useTripEntries(tripId) re-runs when `tripId` changes (tripId in the dep array).
  </behavior>
  <action>
    Append to `src/services/tripService.ts` the Dexie layer: import `useLiveQuery` from
    `dexie-react-hooks`, `db` from `./db`, `activateMode` from `./activeMode`, and `entriesRepository`
    from `./entriesRepository`. `createAndActivateTrip(name: string): Promise<LifeLogEntry>` calls
    `entriesRepository.create({ type:'trip', domain:'trips', title: name.trim() || 'Untitled Trip',
    recordedAt: Date.now(), tags: [], metadata: {}, syncedAt: null })`, then
    `await activateMode('trip', name, entry.id)`, then returns the entry. Do NOT add a duplicate-name
    guard — identical names are acceptable because tripId (the UUID) is the stable key.
    `listTrips()` returns `db.entries.where('domain').equals('trips').filter((e) => e.type === 'trip').reverse().toArray()`.
    `listTripEntries(tripId)` returns `db.entries.filter((e) => e.metadata.tripId === tripId).toArray()`
    — a SINGLE filter scan (metadata is unindexed; never loop a filter per trip, per PITFALLS Pitfall 6).
    `useTrips(): LifeLogEntry[] | undefined` is `useLiveQuery(() => listTrips(), [])` with NO default
    value (undefined = loading). `useTripEntries(tripId): LifeLogEntry[] | undefined` is
    `useLiveQuery(() => listTripEntries(tripId), [tripId])` — `tripId` MUST be in the dependency array.

    Append to `src/services/tripService.test.ts` Dexie integration + reactive-hook tests with
    `beforeEach(async () => { await db.delete(); await db.open() })` and `afterEach(() => vi.useRealTimers())`.
    Cover: createAndActivateTrip return shape + entry persisted in db.entries + activeModeRepository.get
    tripId; listTrips newest-first and type-filtered; listTripEntries matching/excluding by
    metadata.tripId (build a tripId-stamped entry via the trip flow or a stamped factory); useTrips
    loading-then-populated via render/screen/act. Any test asserting `recordedAt`/`Date.now()` MUST use
    `vi.useFakeTimers({ toFake: ['Date'] })` + `vi.setSystemTime(...)` — never bare `vi.useFakeTimers()`.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/services/tripService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test: `createAndActivateTrip('Paris')` resolves an entry with `type==='trip'`, `domain==='trips'`,
      `title==='Paris'`; the entry is retrievable from `db.entries`; `activeModeRepository.get()`
      resolves `{ mode:'trip', label:'Paris', tripId: entry.id }`.
    - Test: `listTrips()` returns only `type==='trip'` entries, newest-first.
    - Test: `listTripEntries(tripId)` includes only entries with `metadata.tripId === tripId`.
    - Test: `useTrips()` returns `undefined` on first render (loading branch) and re-renders to include
      a newly created trip inside `act()`.
    - `grep -c "\.filter(" src/services/tripService.ts` shows `listTripEntries` uses a single filter
      scan (no per-trip loop); `grep -n "\[tripId\]" src/services/tripService.ts` confirms the
      useTripEntries dependency array.
    - `grep -n "vi.useFakeTimers()" src/services/tripService.test.ts` returns NO bare usages.
    - `npx tsc -b` clean; `npx vitest run src/services/tripService.test.ts` green; full suite
      `npx vitest run` exits 0 with zero failures.
  </acceptance_criteria>
  <done>tripService exposes create-and-activate, list helpers, and reactive hooks over the existing engine; integration + hook tests pass; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> trip title | Trip name typed by the user, stored as `title` via entriesRepository.create |
| tripId (UUID) -> metadata.tripId filter | Query join key for listTripEntries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | trip title stored from user input | accept | Local-only single-user app; service layer is a pure data write (no injection sink); name trimmed, empty falls back to 'Untitled Trip' |
| T-20-04 | Denial of service | listTripEntries full-table scan | accept | Unindexed `metadata` requires a full scan; correct and imperceptible at personal-app scale (hundreds of entries); N+1 explicitly avoided (single-pass filter) per PITFALLS Pitfall 6 |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | Zero new dependencies this phase (REQUIREMENTS: "Zero new runtime dependencies"); no install tasks — no supply-chain checkpoint required |
</threat_model>

<verification>
- `npx tsc -b` clean; `npx vitest run` fully green (existing 592+ tests plus new tripService tests).
- Pure helpers contain zero Dexie calls (unit-testable without IndexedDB).
- `listTripEntries` is a single filter scan; `useTripEntries` has `[tripId]` in its dependency array.
- All new Dexie-touching tests use `vi.useFakeTimers({ toFake: ['Date'] })` where Date is controlled.
</verification>

<success_criteria>
- `tripService.createAndActivateTrip(name)` creates a `type='trip'` entry and activates the mode with
  that entry's UUID as `tripId`.
- `listTrips` / `listTripEntries` and the `useTrips` / `useTripEntries` reactive hooks behave per spec.
- Pure stat helpers (`tripExpenseTotal`, `tripExpensesByCategory`, `tripDateRange`, `tripActivityCount`)
  return correct values from in-memory arrays with no Dexie calls.
- All existing 592+ tests remain green.
</success_criteria>

<output>
Create `.planning/phases/20-trip-data-model-engine-extensions/20-02-SUMMARY.md` when done.
</output>
