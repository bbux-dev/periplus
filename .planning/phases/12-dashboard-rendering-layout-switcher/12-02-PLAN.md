---
phase: 12-dashboard-rendering-layout-switcher
plan: 2
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/index.css
  - src/components/dashboard/LayoutChips.tsx
  - src/components/dashboard/ShortcutRow.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/DashboardPage.test.tsx
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "A fresh install (empty Dexie) shows the default layouts seeded once, without overwriting an existing config on remount"
    - "The active layout's shortcuts render as full-width tappable <button> rows, each with its resolved Heroicons icon and name (DSL template as a dim secondary line)"
    - "A horizontally-scrollable chip switcher renders one chip per layout; the active chip has aria-pressed=true; a disabled '+ New' placeholder chip renders for Phase 15"
    - "Tapping a chip persists the selection via activeLayoutRepository.put and the rows update to that layout's shortcuts"
    - "The active layout selection persists across reloads; when the persisted name is absent, the first layout is used"
    - "Existing dashboard nav (Quick Capture, domain tiles, View All Entries) remains reachable"
  artifacts:
    - path: "src/pages/DashboardPage.tsx"
      provides: "Seeding effect + chips + rows wired to reactive config"
      contains: "DEFAULT_SHORTCUT_CONFIG"
    - path: "src/components/dashboard/LayoutChips.tsx"
      provides: "Scrollable aria-pressed chip switcher with disabled + New placeholder"
      exports: ["LayoutChips"]
    - path: "src/components/dashboard/ShortcutRow.tsx"
      provides: "Full-width tappable shortcut row with resolved icon + name + template"
      exports: ["ShortcutRow"]
    - path: "src/index.css"
      provides: ".no-scrollbar utility"
      contains: "no-scrollbar"
  key_links:
    - from: "src/pages/DashboardPage.tsx"
      to: "configRepository.get/put"
      via: "one-shot seeding useEffect with cancelled flag"
      pattern: "configRepository\\.(get|put)"
    - from: "src/pages/DashboardPage.tsx"
      to: "useShortcutConfig / useActiveLayoutName"
      via: "reactive reads + activeLayout derivation with ?? layouts[0] fallback"
      pattern: "useShortcutConfig|useActiveLayoutName"
    - from: "src/pages/DashboardPage.tsx"
      to: "activeLayoutRepository.put"
      via: "chip onSelect handler"
      pattern: "activeLayoutRepository\\.put"
    - from: "src/components/dashboard/ShortcutRow.tsx"
      to: "resolveShortcutIcon"
      via: "icon resolution at render"
      pattern: "resolveShortcutIcon"
---

<objective>
Render the Variant B (chips + rows) dashboard: a horizontally-scrollable layout chip switcher with
persisted selection, the active layout's shortcuts as full-width tappable rows with Heroicons icons,
and one-shot first-run seeding of DEFAULT_SHORTCUT_CONFIG — all wired to the reactive config from
Plan 01. The existing dashboard nav stays reachable.

Purpose: Make a fresh install useful with zero setup and let the user switch layouts with a persisted
selection (DASH-01, DASH-02, DASH-03 UI surface).
Output: `.no-scrollbar` CSS utility, `LayoutChips` + `ShortcutRow` components, extended `DashboardPage`,
and extended `DashboardPage.test.tsx`.

SCOPE GUARD (from CONTEXT/RESEARCH): RENDER + SWITCH only. The shortcut row onClick is a PLACEHOLDER
seam for Phase 13 — use a no-op handler with a `// TODO Phase 13` comment. Do NOT implement parseDSL
capture, the amount/fill-the-hole sheet, one-tap save, undo, or ReviewPage routing. The "+ New" chip
is a disabled placeholder for Phase 15. No import/export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dashboard-rendering-layout-switcher/12-RESEARCH.md
@.planning/phases/12-dashboard-rendering-layout-switcher/12-PATTERNS.md
@.planning/phases/12-dashboard-rendering-layout-switcher/12-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 01 (Wave 1) + existing primitives. Use directly. -->

From src/config/shortcutConfig.ts:
- export const DEFAULT_SHORTCUT_CONFIG: ShortcutConfig
- export function resolveShortcutIcon(key: string | undefined): HeroIcon   // never throws; BoltIcon fallback
- export interface Layout { name: string; icon?: string; shortcuts: Shortcut[] }
- export interface Shortcut { name: string; icon?: string; dslTemplate: string; confirm: boolean }

From src/services/configRepository.ts:
- export const configRepository = { get(): Promise<ShortcutConfig | undefined>; put(c): Promise<void> }
- export function useShortcutConfig(): ShortcutConfig | undefined   // undefined = loading OR none
- export const activeLayoutRepository = { get(): Promise<string | undefined>; put(name: string): Promise<void> }
- export function useActiveLayoutName(): string | undefined         // undefined = loading OR unset

From src/components/ui/cn.ts:
- export function cn(...inputs: ClassValue[]): string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .no-scrollbar utility + presentational LayoutChips and ShortcutRow components</name>
  <files>src/index.css, src/components/dashboard/LayoutChips.tsx, src/components/dashboard/ShortcutRow.tsx</files>
  <read_first>
    - src/index.css (file being modified; @theme token block lines 1-15 — append after it)
    - src/pages/EntryListPage.tsx lines 85-101 (analog: filter chip group with cn() active/inactive + aria-pressed) and lines 40-54 (two-line row body)
    - src/pages/DashboardPage.tsx lines 10-19 (analog: icon + label tappable row, min-h-[64px]) and lines 20-32 (muted-border variant)
    - src/components/ui/cn.ts lines 1-6 (cn helper)
    - 12-RESEARCH.md §Question 4 (chip markup, aria-pressed not role=tablist, + New disabled, no-scrollbar CSS) and §Pattern 2 (shortcut row markup)
    - 12-PATTERNS.md LayoutChips/ShortcutRow prop shapes + import paths
  </read_first>
  <action>
    Append the `.no-scrollbar` utility to `src/index.css` after the `@theme` block:
    `.no-scrollbar::-webkit-scrollbar { display: none; }` and `.no-scrollbar { scrollbar-width: none; }`
    with a brief comment.
    Create `src/components/dashboard/LayoutChips.tsx` exporting `LayoutChips` with props
    `{ layouts: Layout[]; activeLayoutName: string | undefined; onSelect: (name: string) => void }`.
    Render a scrollable container `<div aria-label="Layout switcher" className="flex gap-2 overflow-x-auto no-scrollbar pb-1">`
    with one plain `<button type="button" aria-pressed={...}>` per layout, active/inactive styling via
    `cn()` using `--color-primary`/`--color-primary-foreground` (active) vs `--color-border`/`hover:bg-[var(--color-muted)]`
    (inactive), each chip `shrink-0 whitespace-nowrap rounded-full border px-4 py-1.5 text-sm font-semibold`.
    Append a trailing disabled `<button type="button" disabled>+ New</button>` placeholder (dashed border,
    `// TODO Phase 15`). Do NOT use role="tablist".
    Create `src/components/dashboard/ShortcutRow.tsx` exporting `ShortcutRow` with props
    `{ shortcut: Shortcut; onClick: () => void }`. Render a full-width `<button type="button">` with
    classes `flex w-full items-center gap-3.5 min-h-[64px] px-4 rounded-lg border border-[var(--color-border)]
    bg-[var(--color-background)] hover:bg-[var(--color-muted)] active:opacity-75 transition-colors text-left`;
    resolve the icon via `const Icon = resolveShortcutIcon(shortcut.icon)` rendered `<Icon className="h-5 w-5" aria-hidden="true" />`;
    show `shortcut.name` (semibold) and `shortcut.dslTemplate` as a dim `font-mono text-xs text-[var(--color-border)]`
    secondary line. Render config-derived strings as React text nodes only — never dangerouslySetInnerHTML.
    Import `cn`, `resolveShortcutIcon`, and the `Layout`/`Shortcut` types via paths relative to
    `src/components/dashboard/` (per 12-PATTERNS.md).
  </action>
  <verify>
    <automated>pnpm tsc -b && grep -c "no-scrollbar" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "no-scrollbar" src/index.css` ≥ 2
    - `grep -c "export function LayoutChips\|export const LayoutChips" src/components/dashboard/LayoutChips.tsx` == 1
    - `grep -c "export function ShortcutRow\|export const ShortcutRow" src/components/dashboard/ShortcutRow.tsx` == 1
    - LayoutChips uses `aria-pressed` and NOT `role="tablist"` (`grep -c 'role="tablist"' src/components/dashboard/LayoutChips.tsx` == 0)
    - ShortcutRow calls `resolveShortcutIcon` and contains no `dangerouslySetInnerHTML`
    - `pnpm tsc -b` clean
  </acceptance_criteria>
  <done>.no-scrollbar utility exists; LayoutChips renders scrollable aria-pressed chips + disabled "+ New"; ShortcutRow renders a tappable button with resolved icon, name, and dim DSL template line. Both compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire DashboardPage — one-shot seeding, active-layout derivation, chips + rows, keep existing nav (DASH-01/02/03)</name>
  <files>src/pages/DashboardPage.tsx, src/pages/DashboardPage.test.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (file being modified; full file — outer wrapper, existing Quick Capture / domain / entries links to preserve)
    - src/pages/DashboardPage.test.tsx (file being modified; renderDashboard helper lines 7-13, existing link-count assertion ~line 31)
    - src/services/configRepository.test.tsx lines 9-12, 98-105 (Dexie reset beforeEach + act()/findByText reactive pattern)
    - 12-RESEARCH.md §Question 1 (one-shot seeding useEffect with cancelled flag + the two seeding tests), §Question 2 (activeLayout derivation), §Pitfall 3 & 5 (loading guard, link-count regression)
    - 12-PATTERNS.md DashboardPage section (imports to add, loading guard, seeding effect, derivation, test patterns incl. + New disabled and aria-pressed tests)
  </read_first>
  <behavior>
    - Test: fresh install (empty Dexie) seeds DEFAULT_SHORTCUT_CONFIG once — `findByRole('button', { name: 'DayToDay' })` resolves.
    - Test: an existing config is NOT overwritten on render — after pre-putting a custom config, the stored config still has the custom layout name.
    - Test: the active layout chip has `aria-pressed="true"`.
    - Test: the active layout's shortcut names render as buttons (DASH-01).
    - Test: the "+ New" chip renders disabled.
    - Test (DASH-02 switch): clicking a non-active chip updates rows to that layout's shortcuts (await the new shortcut name appearing) and persists via activeLayoutRepository (re-read returns the clicked name).
    - Regression: existing Quick Capture / domain / View All Entries links still render.
  </behavior>
  <action>
    Write/extend failing tests first (RED), then wire DashboardPage (GREEN).
    In `src/pages/DashboardPage.tsx` add imports: `useEffect` from react; `cn`; `configRepository,
    useShortcutConfig, activeLayoutRepository, useActiveLayoutName` from configRepository;
    `DEFAULT_SHORTCUT_CONFIG` from shortcutConfig; the new `LayoutChips` and `ShortcutRow` components.
    Add the one-shot seeding effect EXACTLY per 12-RESEARCH.md §Question 1 — `useEffect(() => { let
    cancelled = false; configRepository.get().then((existing) => { if (existing === undefined &&
    !cancelled) configRepository.put(DEFAULT_SHORTCUT_CONFIG) }); return () => { cancelled = true } }, [])`.
    Derive state: `const config = useShortcutConfig()`, `const persistedLayoutName = useActiveLayoutName()`,
    `const layouts = config?.layouts ?? []`, `const activeLayout = layouts.find(l => l.name ===
    persistedLayoutName) ?? layouts[0]`. Add an async chip handler `handleLayoutSelect(name)` that calls
    `activeLayoutRepository.put(name)`. Guard the shortcut section while `config === undefined` (return the
    section as null / nothing rendered for it — avoid layout shift; keep existing nav below). Render
    `<LayoutChips layouts={layouts} activeLayoutName={activeLayout?.name} onSelect={handleLayoutSelect} />`
    as the primary surface, then `activeLayout?.shortcuts.map(s => <ShortcutRow key={s.name} shortcut={s}
    onClick={() => { /* TODO Phase 13: capture seam — no-op for now */ }} />)`, then KEEP the existing
    Quick Capture link, domain tiles, and View All Entries link unchanged and reachable. Keep the existing
    `max-w-sm` outer wrapper.
    Extend `src/pages/DashboardPage.test.tsx`: add `act`, `beforeEach` (db.delete/db.open reset),
    `db`, `configRepository`, `activeLayoutRepository`, `ShortcutConfig` type, and `userEvent` imports as
    needed; reuse `renderDashboard()`. Add the behavior tests above. Update the existing link-count test to
    first `await screen.findByRole('button', { name: /DayToDay/i })` as a wait sentinel before asserting
    links (shortcut rows are `<button>`, NOT `<Link>`, so the link count is unchanged).
  </action>
  <verify>
    <automated>pnpm vitest run src/pages/DashboardPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DEFAULT_SHORTCUT_CONFIG\|cancelled" src/pages/DashboardPage.tsx` ≥ 2 (seeding effect present)
    - `grep -c "layouts.find\|?? layouts\[0\]" src/pages/DashboardPage.tsx` ≥ 1 (fallback derivation present)
    - `grep -c "activeLayoutRepository.put" src/pages/DashboardPage.tsx` ≥ 1 (chip persistence wired)
    - shortcut row onClick is a no-op TODO seam — no parseDSL / buildReviewDraft / ReviewPage / navigate import added (`grep -c "parseDSL\|buildReviewDraft\|ReviewPage" src/pages/DashboardPage.tsx` == 0)
    - existing Quick Capture / domain / View All Entries links preserved (regression test green)
    - All new + existing DashboardPage tests pass; `pnpm tsc -b` clean
  </acceptance_criteria>
  <done>Fresh install seeds defaults once (no overwrite on remount); chips render with active aria-pressed and a disabled "+ New"; clicking a chip persists selection and updates rows; the active layout's shortcuts render as icon+name rows; existing nav stays reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db.settings → React render | config-derived strings (layout/shortcut names, dslTemplate) rendered to DOM |
| UI → db.settings | active-layout name written on chip select |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | ShortcutRow / LayoutChips render | mitigate | Render all config-derived strings as React text nodes; NO dangerouslySetInnerHTML anywhere (asserted in acceptance criteria). |
| T-12-04 | Tampering | icon resolution | mitigate | Icons resolved only via resolveShortcutIcon → static SHORTCUT_ICON_MAP allow-list (BoltIcon fallback); no dynamic import / no string→component eval. |
| T-12-05 | Elevation/Tampering | shortcut row onClick | mitigate | onClick is an inert no-op TODO seam in Phase 12; no DSL execution, no save path wired (acceptance criteria asserts no parseDSL/ReviewPage import). |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §No New Packages). N/A; no install task. |

No `high`-severity threats — nothing to block on.
</threat_model>

<verification>
- `pnpm vitest run` (full suite) green
- `pnpm tsc -b` clean
- `git diff --stat src/services/db.ts` shows no changes
- Manual (non-blocking, per VALIDATION.md): horizontal chip scroll feel on a real phone
</verification>

<success_criteria>
- DASH-01: active layout's shortcuts render as full-width tappable button rows with resolved Heroicons icon + name.
- DASH-02: scrollable chip switcher; active chip aria-pressed=true; clicking a chip persists + updates rows; selection survives reload (persisted name; fallback to first layout).
- DASH-03: fresh install seeds DEFAULT_SHORTCUT_CONFIG exactly once; existing config never overwritten.
- Existing dashboard nav remains reachable; no capture/save/authoring behavior implemented (deferred to Phases 13/15).
</success_criteria>

<output>
Create `.planning/phases/12-dashboard-rendering-layout-switcher/12-02-SUMMARY.md` when done
</output>
