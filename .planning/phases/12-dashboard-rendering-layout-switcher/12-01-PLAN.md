---
phase: 12-dashboard-rendering-layout-switcher
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/shortcutConfig.ts
  - src/config/shortcutConfig.test.ts
  - src/services/configRepository.ts
  - src/services/configRepository.test.tsx
autonomous: true
requirements: [DASH-03, DASH-02]

must_haves:
  truths:
    - "A fresh-install seed source (DEFAULT_SHORTCUT_CONFIG) exists with DayToDay / Travel / WorkTrip layouts whose every dslTemplate parses via parseDSL and whose every icon key exists in SHORTCUT_ICON_MAP"
    - "The active layout name can be written to and read reactively from db.settings under a dedicated key, surviving across reloads"
    - "Reading the active layout name before any write returns undefined (caller falls back to first layout)"
  artifacts:
    - path: "src/config/shortcutConfig.ts"
      provides: "DEFAULT_SHORTCUT_CONFIG seed constant"
      contains: "DEFAULT_SHORTCUT_CONFIG"
    - path: "src/services/configRepository.ts"
      provides: "activeLayoutRepository + useActiveLayoutName"
      exports: ["activeLayoutRepository", "useActiveLayoutName"]
  key_links:
    - from: "src/services/configRepository.ts"
      to: "db.settings"
      via: "get/put on key 'activeLayoutName'"
      pattern: "db\\.settings\\.(get|put)"
    - from: "src/services/configRepository.ts"
      to: "useLiveQuery"
      via: "reactive read, empty deps, no default"
      pattern: "useLiveQuery"
---

<objective>
Deliver the data layer for Phase 12: a `DEFAULT_SHORTCUT_CONFIG` seed constant (DASH-03) and
active-layout persistence (`activeLayoutRepository` + `useActiveLayoutName`, DASH-02). No UI in this
plan — this plan only produces the constants, repository, hook, and their tests that the Wave 2 UI
plan consumes.

Purpose: Establish the reactive primitives the Dashboard wires together, with DSL/icon validity and
round-trip persistence proven by tests before any rendering exists.
Output: New `DEFAULT_SHORTCUT_CONFIG` export, new `activeLayoutRepository` + `useActiveLayoutName`
export, plus three new test describe-blocks across two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dashboard-rendering-layout-switcher/12-RESEARCH.md
@.planning/phases/12-dashboard-rendering-layout-switcher/12-PATTERNS.md

<interfaces>
<!-- Phase 11 contracts this plan extends. Use directly — no codebase exploration needed. -->

From src/config/shortcutConfig.ts (Phase 11):
- export interface Shortcut { name: string; icon?: string; dslTemplate: string; confirm: boolean }
- export interface Layout { name: string; icon?: string; shortcuts: Shortcut[] }
- export interface ShortcutConfig { version: 1; layouts: Layout[] }
- export const SHORTCUT_ICON_MAP: Record<string, HeroIcon>   // keys: BanknotesIcon, FilmIcon,
    MapPinIcon, BoltIcon, HomeIcon, BriefcaseIcon, GlobeAltIcon, ShoppingCartIcon, TruckIcon, ...
- export const DEFAULT_SHORTCUT_ICON = BoltIcon
- export function resolveShortcutIcon(key: string | undefined): HeroIcon

From src/services/configRepository.ts (Phase 11):
- const CONFIG_KEY = 'shortcutConfig'
- export const configRepository = { get(): Promise<ShortcutConfig | undefined>; put(c): Promise<void> }
- export function useShortcutConfig(): ShortcutConfig | undefined   // useLiveQuery, [], NO default

From src/services/db.ts: db.settings is a key/value store { key: string; value: unknown } — supports
arbitrary keys; DO NOT modify db.ts (storing a new key is a data write, not a schema change).

From src/services/dsl/parser.ts: parseDSL(line) → { status: 'ok' | 'ambiguous' | 'error'; issues: string[]; ... }
From src/services/configValidator.ts: validateShortcutConfig(config) → { ok: boolean; ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DEFAULT_SHORTCUT_CONFIG seed constant with DSL + icon + schema validity tests (DASH-03)</name>
  <files>src/config/shortcutConfig.ts, src/config/shortcutConfig.test.ts</files>
  <read_first>
    - src/config/shortcutConfig.ts (file being modified; types at lines 30-46, SHORTCUT_ICON_MAP at 59-85, resolveShortcutIcon at 87-89)
    - src/config/shortcutConfig.test.ts (file being modified; import block lines 1-8, nested-loop+custom-message test pattern lines 22-36)
    - src/config/navigation.ts lines 35-65 (analog: typed constant export + section-divider style)
    - 12-RESEARCH.md §Question 3 (authoritative DEFAULT_SHORTCUT_CONFIG content + per-template parse proof table)
    - src/services/dsl/parser.ts (parseDSL signature + status values; do NOT wire capture, validate only)
  </read_first>
  <behavior>
    - Test: every dslTemplate in every layout of DEFAULT_SHORTCUT_CONFIG returns parseDSL().status !== 'error'
      (failure message must name the layout, shortcut, template, and result.issues).
    - Test: every layout.icon and every shortcut.icon (when defined) is a key present in SHORTCUT_ICON_MAP.
    - Test: validateShortcutConfig(DEFAULT_SHORTCUT_CONFIG).ok === true.
  </behavior>
  <action>
    Write the three failing tests first (RED), then add the constant (GREEN). Append
    `DEFAULT_SHORTCUT_CONFIG: ShortcutConfig` to `src/config/shortcutConfig.ts` AFTER `resolveShortcutIcon`,
    preceded by a section divider comment `// ─── Default config (DASH-03) ───...`. Use the exact
    layouts/shortcuts/icons/templates from 12-RESEARCH.md §Question 3 (DayToDay / Travel / WorkTrip;
    icons HomeIcon, BoltIcon, ShoppingCartIcon, BanknotesIcon, FilmIcon, GlobeAltIcon, TruckIcon,
    MapPinIcon, BriefcaseIcon — all verified present in SHORTCUT_ICON_MAP). Multi-value tag params MUST
    be quoted (`?tags="work"`). The constant is plain inert data — no parseDSL/eval at module load.
    Extend `src/config/shortcutConfig.test.ts`: add `DEFAULT_SHORTCUT_CONFIG`, `parseDSL` (from
    `../services/dsl/parser`), and `validateShortcutConfig` (from `../services/configValidator`) imports;
    add the three describe blocks using the file's nested-loop + custom-message assertion style.
  </action>
  <verify>
    <automated>pnpm vitest run src/config/shortcutConfig.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DEFAULT_SHORTCUT_CONFIG" src/config/shortcutConfig.ts` ≥ 1
    - All three new describe blocks pass; the parse-validity loop asserts `.not.toBe('error')` for every template
    - `pnpm tsc -b` reports no new errors
  </acceptance_criteria>
  <done>DEFAULT_SHORTCUT_CONFIG is exported with 3 layouts; every default template parses (status ≠ error), every icon key exists in SHORTCUT_ICON_MAP, and the config passes validateShortcutConfig.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add activeLayoutRepository + useActiveLayoutName with round-trip and fallback tests (DASH-02)</name>
  <files>src/services/configRepository.ts, src/services/configRepository.test.tsx</files>
  <read_first>
    - src/services/configRepository.ts (file being modified; CONFIG_KEY repository pattern lines 10-31, useShortcutConfig hook lines 52-58, section dividers lines 5, 34)
    - src/services/configRepository.test.tsx (file being modified; Dexie reset beforeEach lines 9-12, get/put round-trip pattern lines 46-81, reactive hook test component + act()+findByText pattern lines 86-105)
    - 12-RESEARCH.md §Question 2 (authoritative repository + hook shape + fallback rationale)
  </read_first>
  <behavior>
    - Test: activeLayoutRepository.get() returns undefined before any write.
    - Test: after activeLayoutRepository.put('Travel'), get() returns 'Travel'; a second put('DayToDay') upserts (overwrites).
    - Test: useActiveLayoutName() renders 'Loading' (undefined) initially, then the persisted name after an act()-wrapped put (reactive).
  </behavior>
  <action>
    Write failing tests first (RED), then implement (GREEN). In `src/services/configRepository.ts`,
    append a new section `// ─── Active layout persistence (DASH-02) ───...` after the existing hook.
    Add `const ACTIVE_LAYOUT_KEY = 'activeLayoutName'`; export `activeLayoutRepository` with
    `get(): Promise<string | undefined>` (read `db.settings.get(ACTIVE_LAYOUT_KEY)`, cast
    `row?.value as string | undefined`) and `put(name: string): Promise<void>` (write
    `{ key: ACTIVE_LAYOUT_KEY, value: name }`). Export `useActiveLayoutName(): string | undefined` using
    `useLiveQuery(() => activeLayoutRepository.get(), [])` with NO default (undefined = loading or unset).
    Mirror the existing CONFIG_KEY pattern exactly — no new imports, do NOT modify db.ts.
    Extend `src/services/configRepository.test.tsx`: add `activeLayoutRepository, useActiveLayoutName`
    to the `./configRepository` import; add the three describe blocks reusing the existing
    `beforeEach(db.delete/db.open)` reset and the `act()` + `findByText` reactive assertion pattern.
  </action>
  <verify>
    <automated>pnpm vitest run src/services/configRepository.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const activeLayoutRepository\|export function useActiveLayoutName" src/services/configRepository.ts` == 2
    - `grep -c "useLiveQuery" src/services/configRepository.ts` ≥ 2 (existing + new hook)
    - db.ts is unchanged (no diff in src/services/db.ts)
    - All new tests pass; `pnpm tsc -b` clean
  </acceptance_criteria>
  <done>activeLayoutRepository (get/put on 'activeLayoutName') and useActiveLayoutName (reactive, no default) are exported and tested for undefined-before-write, put/get round-trip + upsert, and reactive update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code → IndexedDB (db.settings) | seed constant + active-layout string written to local storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | DEFAULT_SHORTCUT_CONFIG dslTemplates | accept | Templates are inert string literals; never `eval`'d. Validity is asserted via parseDSL in tests only; no capture wiring in this phase. |
| T-12-02 | Tampering | activeLayoutName value in db.settings | accept | Value is a layout name string from the config's own layouts (no free-form user input in Phase 12); single-user local PWA, no injection surface. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (RESEARCH §No New Packages). N/A; no install task in this plan. |

No `high`-severity threats — nothing to block on.
</threat_model>

<verification>
- `pnpm vitest run src/config src/services` green
- `pnpm tsc -b` clean
- `git diff --stat src/services/db.ts` shows no changes
</verification>

<success_criteria>
- DEFAULT_SHORTCUT_CONFIG exported; all default templates parse (≠ error); all icon keys in SHORTCUT_ICON_MAP; passes validateShortcutConfig.
- activeLayoutRepository + useActiveLayoutName exported; persistence round-trip + reactive read + undefined-before-write all verified.
- No modification to db.ts.
</success_criteria>

<output>
Create `.planning/phases/12-dashboard-rendering-layout-switcher/12-01-SUMMARY.md` when done
</output>
