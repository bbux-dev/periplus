---
phase: 01-foundation-app-shell
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/main.tsx
  - src/App.tsx
  - src/pages/WelcomePage.tsx
  - src/pages/WelcomePage.test.tsx
  - src/components/Counter.tsx
  - src/components/Counter.test.tsx
autonomous: true
requirements: [SHELL-01, DEMO-01]
must_haves:
  truths:
    - "The default route '/' renders a mobile-first welcome screen showing the title 'Life Log'"
    - "The welcome screen mounts the Counter"
    - "Tapping the + heroicon button increments the displayed value; the - button decrements it"
    - "The displayed counter value updates reactively via useLiveQuery after each click"
  artifacts:
    - path: "src/pages/WelcomePage.tsx"
      provides: "Mobile-first 'Life Log' welcome screen hosting the Counter"
      exports: ["WelcomePage"]
      contains: "Life Log"
    - path: "src/components/Counter.tsx"
      provides: "useLiveQuery-driven counter with +/- heroicon buttons"
      exports: ["Counter"]
      contains: "useLiveQuery"
    - path: "src/App.tsx"
      provides: "Route table mapping '/' to WelcomePage"
      contains: "WelcomePage"
  key_links:
    - from: "src/components/Counter.tsx"
      to: "src/services/db.ts"
      via: "db.counter.get(1) read + db.counter.put upsert"
      pattern: "db\\.counter\\.(get|put)"
    - from: "src/components/Counter.tsx"
      to: "dexie-react-hooks"
      via: "useLiveQuery reactive read"
      pattern: "useLiveQuery"
    - from: "src/App.tsx"
      to: "src/pages/WelcomePage.tsx"
      via: "Route element"
      pattern: "WelcomePage"
    - from: "src/main.tsx"
      to: "react-router-dom"
      via: "BrowserRouter wrapper"
      pattern: "BrowserRouter"
---

<objective>
Land the tracer vertical slice: a mobile-first "Life Log" welcome screen served at the default route (SHELL-01), hosting a Counter whose +/- heroicon buttons upsert into the Dexie store and re-render reactively via `useLiveQuery` (the UI half of DEMO-01). This closes the UI → Dexie → IndexedDB → live-read loop the milestone exists to prove.

Purpose: Wires the plan 02 primitives (Button, db) and react-router into a runnable, visible app that demonstrates end-to-end persistence and reactivity.
Output: A working welcome screen + counter, smoke-tested with RTL + fake-indexeddb.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-app-shell/01-RESEARCH.md
@.planning/phases/01-foundation-app-shell/01-VALIDATION.md

<interfaces>
<!-- Contracts from plan 02 (already implemented) + RESEARCH.md Patterns 1, 4, 6. -->

From src/services/db.ts (plan 02):
  export const db; db.counter.get(1): Promise<{id:number;value:number}|undefined>;
  db.counter.put({id:1, value:number}): upsert

From src/components/ui/Button.tsx (plan 02):
  export function Button(props): variant?: 'primary'|'secondary'|'ghost'; size?: 'sm'|'md'|'lg'|'icon';
  forwards onClick, aria-label, children

useLiveQuery (RESEARCH.md Pattern 4):
  const counter = useLiveQuery(() => db.counter.get(1), [], { id: 1, value: 0 });
  // third arg is the default while Dexie opens — avoids undefined flash
  const value = counter?.value ?? 0;
  increment: db.counter.put({ id: 1, value: value + 1 });  decrement: value - 1

Heroicons (RESEARCH.md Pattern 6):
  import { PlusIcon, MinusIcon } from '@heroicons/react/24/outline';  // <PlusIcon className="h-5 w-5" />

Routing (RESEARCH.md Pattern 1 / A3):
  BrowserRouter in main.tsx; App.tsx renders <Routes><Route path="/" element={<WelcomePage/>} /></Routes>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Welcome screen + routing (SHELL-01)</name>
  <read_first>
    - .planning/phases/01-foundation-app-shell/01-RESEARCH.md (Pattern 1 routing / SHELL-01; Recommended Project Structure)
    - src/main.tsx (current plan-01 version renders <App/> and imports index.css)
    - .planning/phases/01-foundation-app-shell/01-VALIDATION.md (## Manual-Only Verifications — phone-viewport check is manual)
  </read_first>
  <files>src/main.tsx, src/App.tsx, src/pages/WelcomePage.tsx, src/pages/WelcomePage.test.tsx</files>
  <behavior>
    - Rendering the app at path '/' shows text "Life Log".
    - WelcomePage renders an h1 (or heading role) containing "Life Log".
  </behavior>
  <action>
    Wrap the app in BrowserRouter: edit src/main.tsx to import BrowserRouter from 'react-router-dom' and wrap <App />. Edit src/App.tsx to render a <Routes> table with a single <Route path="/" element={<WelcomePage />} /> (replacing the plan-01 placeholder). Create src/pages/WelcomePage.tsx exporting `WelcomePage`: a mobile-first layout (full-height flex column, centered, generous touch spacing, constrained max-width so it reads well on a phone) with an h1 titled "Life Log". Leave a slot to mount <Counter /> (added in Task 2). Write WelcomePage.test.tsx rendering WelcomePage (wrap in MemoryRouter if needed) and asserting `screen.getByText('Life Log')` (or getByRole('heading', { name: /Life Log/ })) is present.
  </action>
  <verify>
    <automated>npx vitest run src/pages</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pages` passes (Life Log title asserted).
    - App.tsx maps '/' → WelcomePage; main.tsx wraps the tree in BrowserRouter.
    - `npx tsc -b` exits 0.
    - Note for verify-work: phone-viewport rendering (SC1) is a manual check per VALIDATION.md.
  </acceptance_criteria>
  <done>The default route renders a mobile-first "Life Log" welcome screen, verified by a passing RTL test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Counter component wired to Dexie + useLiveQuery (DEMO-01 UI)</name>
  <read_first>
    - .planning/phases/01-foundation-app-shell/01-RESEARCH.md (Pattern 4 useLiveQuery counter, Pattern 6 heroicons; Pitfall 5 default value avoids undefined flash)
    - src/services/db.ts (plan 02 — db.counter.get/put)
    - src/components/ui/Button.tsx (plan 02 — variant/size/onClick/aria-label)
    - src/test-setup.ts (fake-indexeddb/auto for the reactive read test)
  </read_first>
  <files>src/components/Counter.tsx, src/components/Counter.test.tsx, src/pages/WelcomePage.tsx</files>
  <behavior>
    - On mount the counter shows 0 (default while Dexie opens; no undefined crash).
    - Clicking the + button (aria-label "increment") makes the displayed value become 1, then 2 (reactive via useLiveQuery).
    - Clicking the - button (aria-label "decrement") decreases the displayed value.
    - The value displayed always reflects db.counter.get(1) after each put.
  </behavior>
  <action>
    Create src/components/Counter.tsx exporting `Counter`: read with `useLiveQuery(() => db.counter.get(1), [], { id: 1, value: 0 })` from dexie-react-hooks, derive `value = counter?.value ?? 0`. Render a Button (variant="ghost" size="icon" aria-label="decrement") with a MinusIcon, the numeric value (tabular-nums), and a Button (variant="ghost" size="icon" aria-label="increment") with a PlusIcon, both from '@heroicons/react/24/outline'. increment = `db.counter.put({ id: 1, value: value + 1 })`; decrement uses `value - 1`. Do NOT initialize the row in the db constructor — the first put creates it (Pitfall 5). Mount <Counter /> into WelcomePage's slot from Task 1. Write Counter.test.tsx (RTL + user-event, relies on plan-01 fake-indexeddb/auto): render Counter, assert initial "0", click increment via getByLabelText('increment'), await and assert "1", click again assert "2", click decrement assert "1". Reset db in beforeEach (await db.delete(); await db.open()).
  </action>
  <verify>
    <automated>npx vitest run src/components && npx tsc -b && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components` passes (initial 0, increment to 1→2, decrement reactive cases green).
    - Counter imports useLiveQuery and calls db.counter.put / db.counter.get(1).
    - Counter is mounted in WelcomePage; +/- buttons use @heroicons/react/24/outline icons via the Button primitive.
    - `npx tsc -b && npx vite build` exits 0.
    - Note for verify-work: cross-refresh persistence (SC4) is manual per VALIDATION.md (fake-indexeddb is in-memory).
  </acceptance_criteria>
  <done>The Counter's +/- heroicon buttons upsert the Dexie store and the displayed value updates reactively via useLiveQuery, closing the UI → Dexie → IndexedDB → live-read loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local-first, offline, single-user. Counter writes integers to same-origin IndexedDB; no network, no auth, no user-supplied strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering (DOM XSS) | Counter / WelcomePage rendering | accept | No user-supplied string input; counter value is an integer from +/- buttons; React escapes rendered children by default |
| T-01-02 | Information Disclosure | IndexedDB counter store | accept | Single-user local device storage; integer only, no PII/secrets, no transmission |
</threat_model>

<verification>
- `npx vitest run` (full suite) passes including src/pages and src/components.
- `npx tsc -b && npx vite build` exits 0.
- Manual (verify-work): phone-viewport welcome screen (SC1) and cross-refresh persistence (SC4) per VALIDATION.md.
</verification>

<success_criteria>
The default route shows a mobile-first "Life Log" welcome screen (SHELL-01); the Counter's +/- heroicon buttons increment/decrement a Dexie-persisted value that updates reactively via useLiveQuery (DEMO-01).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-app-shell/01-03-SUMMARY.md` when done.
</output>
