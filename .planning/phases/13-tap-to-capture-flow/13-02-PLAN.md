---
phase: 13-tap-to-capture-flow
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/dashboard/HoleSheet.tsx
  - src/components/dashboard/HoleSheet.test.tsx
  - src/components/dashboard/SavedToast.tsx
  - src/components/dashboard/SavedToast.test.tsx
autonomous: true
requirements: [CAP-02, CAP-03]

must_haves:
  truths:
    - "Tapping presets or the keypad updates a live DSL preview that matches the line the capture will persist"
    - "The Save button is disabled until every hole has a non-empty value"
    - "The fill sheet is a labelled modal dialog reachable and dismissible by the user"
    - "A Saved toast presents an Undo affordance the user can tap"
  artifacts:
    - path: "src/components/dashboard/HoleSheet.tsx"
      provides: "Fill-the-hole bottom sheet: amount keypad, presets, text inputs for non-amount holes, live DSL preview"
      contains: "role=\"dialog\""
      min_lines: 60
    - path: "src/components/dashboard/HoleSheet.test.tsx"
      provides: "RTL coverage: preview updates on keypad/presets, multi-hole ordering, Save disabled until valid (CAP-02)"
    - path: "src/components/dashboard/SavedToast.tsx"
      provides: "Presentational Saved · Undo toast (role=status); onUndo prop (CAP-03)"
      contains: "role=\"status\""
    - path: "src/components/dashboard/SavedToast.test.tsx"
      provides: "RTL coverage: renders Saved + Undo, clicking Undo fires onUndo"
  key_links:
    - from: "src/components/dashboard/HoleSheet.tsx"
      to: "src/services/captureService.ts"
      via: "buildDSLPreview(type, applyFills(baseValues, fills))"
      pattern: "buildDSLPreview\\("
---

<objective>
Build the two net-new UI primitives the capture flow needs: the fill-the-hole bottom sheet
(amount keypad + presets + live DSL preview) and the Saved · Undo toast.

Purpose: Isolate the novel UI constructs (no codebase analog) so they can be unit-tested in
isolation before the 13-03 wire-in. Both are pure presentational components driven by props.
Output: HoleSheet.tsx and SavedToast.tsx (+ RTL tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tap-to-capture-flow/13-RESEARCH.md
@.planning/phases/13-tap-to-capture-flow/13-PATTERNS.md

<interfaces>
<!-- From src/services/captureService.ts (13-01) — HoleSheet consumes these -->
```typescript
export const HOLE_TOKEN: '{}'
export interface HoleMap { positional: string[]; named: string[]; hasHoles: boolean }
export function applyFills(base: Record<string,string>, fills: Record<string,string>): Record<string,string>
export function buildDSLPreview(type: EntryType, mergedValues: Record<string,string>): string
```

<!-- HoleSheet prop contract (RESEARCH §4) -->
```typescript
interface HoleSheetProps {
  isOpen: boolean
  type: EntryType
  domain: EntryDomain
  baseValues: Record<string, string>   // clean template values (no holes)
  holeMap: HoleMap
  onSave: (filledValues: Record<string, string>) => void
  onCancel: () => void
}
```

<!-- SavedToast prop contract -->
```typescript
interface SavedToastProps { onUndo: () => void }
```

Styling tokens & conventions (PATTERNS "Shared Patterns"): use only `var(--color-*)`;
`type="button"` on every button; `active:opacity-75 transition-colors`; named exports only;
reuse `Button` from src/components/ui/Button and `cn` from src/components/ui/cn.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HoleSheet — bottom sheet with keypad, presets, live preview</name>
  <files>src/components/dashboard/HoleSheet.tsx, src/components/dashboard/HoleSheet.test.tsx</files>
  <read_first>
    - .planning/phases/13-tap-to-capture-flow/13-RESEARCH.md (§4 Fill-the-Hole Sheet, lines 354–478) — AUTHORITATIVE spec (no codebase analog)
    - src/components/dashboard/ShortcutRow.tsx (lines 13–28) — button styling tokens, type="button", active:opacity-75
    - src/pages/QuickCapturePage.tsx (lines 137–170) — live-preview font-mono + var(--color-primary) pattern; disabled-button pattern
    - src/components/ui/FormField.test.tsx (lines 1–28) — component test + userEvent analog
    - src/components/ui/Button — existing Button (variant primary, disabled prop)
    - src/services/captureService.ts (13-01) — buildDSLPreview, applyFills, HoleMap
  </read_first>
  <behavior>
    - Renders a panel with role="dialog" aria-modal="true" and an accessible label ("Fill in required fields").
    - For an amount hole: renders quick-amount preset buttons ($5/$10/$20/$50) and a 12-key keypad (1–9, '.', 0, backspace ⌫ with aria-label "Backspace").
    - Tapping keypad '1' then '2' for template baseValues {category:'food'} type 'expense' shows preview text "expense 12:food" (preview = buildDSLPreview(type, applyFills(baseValues, fills))).
    - Tapping the $20 preset sets the amount fill to 20 and the preview shows "expense 20:food".
    - For a non-amount text hole (e.g. merchant): renders a labelled <input type="text"> instead of the keypad.
    - Multi-hole sheet shows holes in holeMap order: positional first, then named (RESEARCH §4 ordering).
    - Save button (reuse Button variant primary) is disabled while any hole fill is empty; enabled once all holes have non-empty values.
    - Clicking Save calls onSave(fills) with the collected fills; clicking the overlay or Cancel calls onCancel.
    - On open, focus moves into the sheet (useEffect + ref.focus()).
  </behavior>
  <action>
    Create src/components/dashboard/HoleSheet.tsx as a named export accepting HoleSheetProps. Manage internal state fills: Record<string,string> keyed by hole field key. Derive orderedHoles = [...holeMap.positional.map(k => ({key:k, isAmount: k==='amount'})), ...holeMap.named.map(k => ({key:k, isAmount:false}))]. Render, per hole: amount holes get the right-aligned amount display + preset buttons ($5/$10/$20/$50) + a 3-col keypad grid of '1'..'9','.','0','⌫' (each <button type="button"> with aria-label); non-amount holes get a labelled text <input>. Compute merged = applyFills(baseValues, fills) and render buildDSLPreview(type, merged) in a font-mono, var(--color-primary) preview line that updates synchronously every keystroke (no debounce). Keypad handler appends digits / handles '.' (single decimal) / '⌫' backspace on the amount fill. Save button uses Button variant primary, disabled until orderedHoles.every(h => fills[h.key]?.trim()). Overlay div (fixed inset-0 bg-black/40 z-40, onClick onCancel, aria-hidden) + panel div (role="dialog" aria-modal="true" aria-label, fixed bottom-0 left-0 right-0 z-50, bg-[var(--color-background)] rounded-t-2xl). useEffect + ref.focus() on mount. Only var(--color-*) tokens; no hardcoded colors. NO router, NO IndexedDB, NO new deps — purely presentational.
    Write HoleSheet.test.tsx FIRST (RED) covering every <behavior> bullet via userEvent; no MemoryRouter, no db reset.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/dashboard/HoleSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'role="dialog"' src/components/dashboard/HoleSheet.tsx` >= 1 and `grep -c 'aria-modal' src/components/dashboard/HoleSheet.tsx` >= 1.
    - Source assertion: `grep -c "buildDSLPreview" src/components/dashboard/HoleSheet.tsx` >= 1 and `grep -c "export default" src/components/dashboard/HoleSheet.tsx` == 0.
    - No hardcoded colors: `grep -nE "#[0-9a-fA-F]{3,6}|rgb\(" src/components/dashboard/HoleSheet.tsx` returns nothing (bg-black/40 overlay is allowed).
    - RTL test proves: typing 1 then 2 yields preview "expense 12:food"; a preset sets the amount; Save is disabled with an empty hole and enabled when filled; clicking Save calls onSave; Cancel/overlay calls onCancel.
    - `pnpm exec vitest run src/components/dashboard/HoleSheet.test.tsx` green; `pnpm exec tsc -b` clean.
  </acceptance_criteria>
  <done>HoleSheet renders keypad/presets/text-input + live preview, gates Save, and is a labelled dismissible dialog; tests green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SavedToast — presentational Saved · Undo toast</name>
  <files>src/components/dashboard/SavedToast.tsx, src/components/dashboard/SavedToast.test.tsx</files>
  <read_first>
    - .planning/phases/13-tap-to-capture-flow/13-RESEARCH.md (§5 Saved · Undo Toast, lines 481–557) — AUTHORITATIVE spec (no codebase analog)
    - src/pages/QuickCapturePage.tsx (lines 106–113) — inline button hover/active token pattern
    - src/components/ui/FormField.test.tsx (lines 1–28) — component test + userEvent analog
    - PATTERNS.md "SavedToast.tsx — NET-NEW" + "Shared Patterns" — role=status semantics, color inversion note
  </read_first>
  <behavior>
    - Renders text "Saved" and an "Undo" button.
    - Container has role="status" and aria-live="polite".
    - Clicking the Undo button calls the onUndo prop exactly once.
    - Component is purely presentational — it owns NO timer and NO entry state (timer/state live in DashboardPage, wired in 13-03).
  </behavior>
  <action>
    Create src/components/dashboard/SavedToast.tsx as a named export accepting { onUndo }. Render a fixed-position container (fixed bottom-6 left-1/2 -translate-x-1/2 z-50) with role="status" aria-live="polite", bg-[var(--color-foreground)] text-[var(--color-background)] (intentional inversion for contrast), containing a "Saved" span and an Undo <button type="button" onClick={onUndo}> with underline + hover:opacity-80 active:opacity-60 transition-opacity. Only var(--color-*) tokens. No timer, no IndexedDB, no router.
    Write SavedToast.test.tsx FIRST (RED): assert role="status" present, "Saved" text and Undo button render, and userEvent.click(Undo) calls a vi.fn() onUndo.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/dashboard/SavedToast.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'role="status"' src/components/dashboard/SavedToast.tsx` >= 1; `grep -c "export default" src/components/dashboard/SavedToast.tsx` == 0.
    - No hardcoded colors: `grep -nE "#[0-9a-fA-F]{3,6}|rgb\(" src/components/dashboard/SavedToast.tsx` returns nothing.
    - RTL test: Saved text + Undo button present; clicking Undo invokes onUndo once.
    - `pnpm exec vitest run src/components/dashboard/SavedToast.test.tsx` green; `pnpm exec tsc -b` clean.
  </acceptance_criteria>
  <done>SavedToast renders a status-role Saved · Undo toast and fires onUndo on click; tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keypad / text input → HoleSheet fills | untrusted user text enters the capture pipeline as DATA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Injection | HoleSheet fills / preview | mitigate | fills are plain strings rendered as React-escaped text and later flow through buildReviewDraft/parseDSL (no eval); HoleSheet never executes input |
| T-13-05 | Information Disclosure | toast contents | accept | toast shows only "Saved" + Undo; no entry data or PII rendered |
| T-13-SC | Tampering | package installs | accept | No package installs this phase — RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/dashboard/HoleSheet.test.tsx src/components/dashboard/SavedToast.test.tsx` green.
- `pnpm exec tsc -b` clean.
- Both components are prop-driven and import nothing from router/IndexedDB.
</verification>

<success_criteria>
- CAP-02: HoleSheet provides keypad + presets + a live DSL preview matching the capture line, with Save gated until valid.
- CAP-03: SavedToast presents the Undo affordance (wired to deletion in 13-03).
</success_criteria>

<output>
Create `.planning/phases/13-tap-to-capture-flow/13-02-SUMMARY.md` when done.
</output>
