---
phase: 06-entry-list-detail-export
plan: 06
type: execute
wave: 3
depends_on: ["06-04", "06-05"]
files_modified:
  - src/App.tsx
  - src/App.test.tsx
autonomous: true
requirements: [VIEW-01, VIEW-03]
must_haves:
  truths:
    - "/entries renders the real EntryListPage (not the PlaceholderPage stub)"
    - "/entries/:id renders the real EntryDetailPage (not the PlaceholderPage stub)"
    - "App route tests pass against the real pages"
  artifacts:
    - path: "src/App.tsx"
      provides: "Phase 6 routes wired to real pages"
      contains: "EntryListPage"
  key_links:
    - from: "src/App.tsx"
      to: "EntryListPage / EntryDetailPage"
      via: "Route element wiring"
      pattern: "<EntryListPage|<EntryDetailPage"
---

<objective>
Wire the two Phase 6 routes in `App.tsx` from their `PlaceholderPage` stubs to the real `EntryListPage` and `EntryDetailPage`, and update `App.test.tsx` so its route-reachability assertions match the real page content. This is the integration step that makes the list and detail screens live.

Purpose: `App.tsx` is the single shared-conflict file both pages depend on; isolating its edit here (wave 3, after both pages exist) guarantees no same-wave plan touches it concurrently.
Output: `App.tsx` routes wired; `App.test.tsx` route assertions updated and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current App.tsx Phase 6 stubs (lines 23-25): -->
  <Route path="/entries"     element={<PlaceholderPage title="Entry List" />} />
  <Route path="/entries/:id" element={<PlaceholderPage title="Entry Detail" />} />
  <Route path="*"            element={<PlaceholderPage title="Page Not Found" />} />   // KEEP — catch-all still uses PlaceholderPage

<!-- New page components: -->
  EntryListPage   from './pages/EntryListPage'    (plan 06-04) — renders <h1>Entries</h1>
  EntryDetailPage from './pages/EntryDetailPage'  (plan 06-05) — renders 'Entry not found.' for unknown id

<!-- Current App.test.tsx route table (it.each, ~lines 38-46) asserts: -->
  { path: '/entries',     expectedHeading: /entry list/i }     // STALE after swap — EntryListPage heading is "Entries"
  { path: '/entries/abc', expectedHeading: /entry detail/i }   // STALE after swap — EntryDetailPage('abc') => not found
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap /entries and /entries/:id routes to the real pages and fix App route tests</name>
  <files>src/App.tsx, src/App.test.tsx</files>
  <read_first>
    - src/App.tsx (full file — swap the two Phase 6 Route elements, keep the catch-all PlaceholderPage)
    - src/App.test.tsx (full file — the it.each route table at ~lines 28-46 has stale /entries + /entries/abc heading expectations)
    - src/pages/EntryListPage.tsx (confirm the heading text is "Entries" — from 06-04)
    - src/pages/EntryDetailPage.tsx (confirm not-found text is "Entry not found." — from 06-05)
    - .planning/phases/06-entry-list-detail-export/06-RESEARCH.md (Pattern 11: App route swap; note PlaceholderPage import stays for the catch-all)
  </read_first>
  <action>
    In `src/App.tsx`, add `import { EntryListPage } from './pages/EntryListPage'` and `import { EntryDetailPage } from './pages/EntryDetailPage'`. Replace `element={<PlaceholderPage title="Entry List" />}` with `element={<EntryListPage />}` for the `/entries` route and `element={<PlaceholderPage title="Entry Detail" />}` with `element={<EntryDetailPage />}` for the `/entries/:id` route. Keep the `PlaceholderPage` import and the `path="*"` catch-all route unchanged — the 404 route still uses PlaceholderPage.
    In `src/App.test.tsx`, the `it.each` "all 6 routes reachable" table asserts stale headings for the two Phase 6 routes. Remove the `{ path: '/entries', expectedHeading: /entry list/i }` and `{ path: '/entries/abc', expectedHeading: /entry detail/i }` entries from that array, and add two dedicated tests: one rendering at `/entries` that asserts `await screen.findByRole('heading', { name: /entries/i })` (the real EntryListPage heading); one rendering at `/entries/abc` that asserts `await screen.findByText('Entry not found.')` (the real EntryDetailPage not-found guard for an id that does not exist). Ensure the db is in a clean/open state for the not-found render (the unknown id resolves to a graceful not-found, not a crash). Leave all other route assertions (dashboard, domain, capture, manual, review-redirect, 404, navigation flows) untouched.
  </action>
  <acceptance_criteria>
    - `App.tsx` contains `<EntryListPage />` and `<EntryDetailPage />` route elements and still imports `PlaceholderPage` for the `*` route.
    - `App.tsx` no longer renders `PlaceholderPage` for `/entries` or `/entries/:id` (grep `grep -c 'PlaceholderPage title="Entry' src/App.tsx` returns 0).
    - `App.test.tsx` no longer asserts `/entry list/i` or `/entry detail/i` headings; it asserts the EntryListPage `Entries` heading at `/entries` and `Entry not found.` text at `/entries/abc`.
    - `npx vitest run src/App.test.tsx` exits 0.
    - Full gate green: `npx vitest run && npx tsc -b && npx vite build`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/App.test.tsx && npx vitest run</automated>
  </verify>
  <done>/entries and /entries/:id render the real pages; App route tests assert the real content; the full suite + typecheck + build are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path → route render | Unknown/arbitrary paths must resolve to a graceful page, never a crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Denial of Service | route wiring | mitigate | `/entries/:id` with an unknown id renders EntryDetailPage's not-found guard; the `*` catch-all still renders the 404 PlaceholderPage. The render-time security gates live in the pages themselves (T-06-01/02, plan 06-05) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- `npx vitest run src/App.test.tsx` green.
- Phase gate: `npx vitest run && npx tsc -b && npx vite build` green (all 6 phases' tests pass together).
</verification>

<success_criteria>
The `/entries` and `/entries/:id` routes render the real EntryListPage and EntryDetailPage, App's route tests assert the real page content, and the full milestone test suite + typecheck + production build all pass.
</success_criteria>

<output>
Create `.planning/phases/06-entry-list-detail-export/06-06-SUMMARY.md` when done.
</output>
