---
phase: 06-entry-list-detail-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/entriesRepository.ts
  - src/services/entriesRepository.test.tsx
autonomous: true
requirements: [VIEW-03]
must_haves:
  truths:
    - "useEntry(id) returns undefined while Dexie is opening (loading)"
    - "useEntry(id) returns null when no entry matches the id (not found)"
    - "useEntry(id) returns the LifeLogEntry when a matching entry exists"
  artifacts:
    - path: "src/services/entriesRepository.ts"
      provides: "useEntry(id) reactive tri-state hook"
      exports: ["useEntry"]
  key_links:
    - from: "src/services/entriesRepository.ts useEntry"
      to: "db.entries.get(id)"
      via: "useLiveQuery with .then(e => e ?? null)"
      pattern: "db\\.entries\\.get\\(id\\)"
---

<objective>
Add a reactive `useEntry(id)` hook to `entriesRepository.ts` that distinguishes loading from not-found from found, so `EntryDetailPage` (plan 06-05) can render a clean loading state, a graceful not-found guard, and the full entry without an effect/state dance.

Purpose: VIEW-03 (Entry Detail) needs a single-entry reactive read. Mirroring the existing `useEntries()` pattern keeps the read API consistent and auto-updates the detail view if the entry changes.
Output: `useEntry(id)` exported from `entriesRepository.ts` (+ tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-entry-list-detail-export/06-RESEARCH.md

<interfaces>
<!-- Existing useEntries() pattern to mirror, from src/services/entriesRepository.ts -->
  export function useEntries(): LifeLogEntry[] | undefined
    — useLiveQuery(() => db.entries.orderBy('recordedAt').reverse().toArray(), [])

<!-- Existing one-shot get (do NOT use for the hook; reference only): -->
  entriesRepository.get(id): Promise<LifeLogEntry | undefined>

<!-- db.entries is an EntityTable<LifeLogEntry, 'id'>; db.entries.get(id) resolves to LifeLogEntry | undefined -->

<!-- Test reset pattern (from entriesRepository.test.tsx): -->
  beforeEach: await db.delete(); await db.open()
  makeEntryData(overrides) factory; act() + screen.findByText for useLiveQuery re-renders
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add useEntry(id) tri-state hook + reactive tests</name>
  <files>src/services/entriesRepository.ts, src/services/entriesRepository.test.tsx</files>
  <read_first>
    - src/services/entriesRepository.ts (full file — append the new hook next to useEntries)
    - src/services/entriesRepository.test.tsx (full file — mirror the EntryListTest component + act()/findByText pattern, the makeEntryData factory, and the beforeEach db reset)
    - src/services/db.ts (LifeLogEntry shape)
    - .planning/phases/06-entry-list-detail-export/06-RESEARCH.md (Pattern 5)
  </read_first>
  <behavior>
    - For an id that does not exist: hook resolves to null (after the loading undefined frame)
    - For an id of a just-created entry: hook resolves to that entry (entry.id === id, entry.title matches)
    - The hook re-renders reactively when the matching entry is created after first render
  </behavior>
  <action>
    In `src/services/entriesRepository.ts`, add and export `function useEntry(id: string): LifeLogEntry | null | undefined` implemented with `useLiveQuery(() => db.entries.get(id).then(e => e ?? null), [id])`. The `.then(e => e ?? null)` transform is required so the return type distinguishes `undefined` (Dexie still opening = loading), `null` (open, genuinely not found), and `LifeLogEntry` (found). Add a JSDoc block matching the style of `useEntries`, documenting the three states and warning callers to handle `undefined` and `null` explicitly. Do not change `useEntries` or any repository method.
    In `src/services/entriesRepository.test.tsx`, add a `describe('useEntry reactive hook')` block. Create a small `EntryDetailTest({ id })` component that calls `useEntry(id)` and renders `'Loading'` for undefined, `'Not found'` for null, and the entry title otherwise. Add tests: (1) unknown id renders `'Not found'` (use `await screen.findByText('Not found')`); (2) after `act(async () => entriesRepository.create(makeEntryData({ title: 'Detail Target' })))` and rendering `EntryDetailTest` with that created id, `await screen.findByText('Detail Target')` succeeds. Reuse the existing `makeEntryData` factory and `beforeEach` db reset.
  </action>
  <acceptance_criteria>
    - `entriesRepository.ts` exports `useEntry` with signature `(id: string) => LifeLogEntry | null | undefined`.
    - The implementation calls `db.entries.get(id)` inside `useLiveQuery` with dependency array `[id]` and the `.then(e => e ?? null)` transform.
    - New tests assert the not-found (`null`) path and the found path.
    - `npx vitest run src/services/entriesRepository.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/entriesRepository.test.tsx</automated>
  </verify>
  <done>useEntry(id) is exported with the documented tri-state return; both not-found and found tests pass; useEntries and all existing repository tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route param :id → IndexedDB lookup | The `:id` from the URL is untrusted; an arbitrary string must not crash the read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Denial of Service | useEntry(id) lookup | mitigate | `db.entries.get(id)` with any string returns undefined→null gracefully; consumer (06-05) coerces a missing param to '' so no Dexie call receives undefined |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase; nothing installed |
</threat_model>

<verification>
- `npx vitest run src/services/entriesRepository.test.tsx` green (existing + new useEntry tests).
- Full suite: `npx vitest run` green.
</verification>

<success_criteria>
`useEntry(id)` is a reactive export returning `undefined | null | LifeLogEntry`, with tests proving the not-found and found paths.
</success_criteria>

<output>
Create `.planning/phases/06-entry-list-detail-export/06-02-SUMMARY.md` when done.
</output>
