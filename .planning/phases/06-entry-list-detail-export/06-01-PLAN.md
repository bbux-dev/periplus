---
phase: 06-entry-list-detail-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/urlUtils.ts
  - src/services/urlUtils.test.ts
  - src/pages/ReviewPage.tsx
autonomous: true
requirements: [VIEW-03]
must_haves:
  truths:
    - "isSafeUrl returns true only for http: and https: URLs, false for everything else (javascript:, garbage, empty)"
    - "ReviewPage imports isSafeUrl from the shared module and its Phase 4 tests stay green"
  artifacts:
    - path: "src/services/urlUtils.ts"
      provides: "Shared isSafeUrl(raw) scheme validator"
      exports: ["isSafeUrl"]
    - path: "src/services/urlUtils.test.ts"
      provides: "Unit tests for isSafeUrl"
  key_links:
    - from: "src/pages/ReviewPage.tsx"
      to: "src/services/urlUtils.ts"
      via: "import { isSafeUrl }"
      pattern: "from '\\.\\./services/urlUtils'"
---

<objective>
Extract the module-private `isSafeUrl` helper from `ReviewPage.tsx` into a shared `src/services/urlUtils.ts` so both `ReviewPage` (write-side) and the new `EntryDetailPage` (read-side, plan 06-05) can validate `sourceUrl` schemes from one source of truth.

Purpose: `isSafeUrl` is the single security mitigation (T-06-01) that prevents a stored `javascript:` URL from ever becoming a clickable `<a href>`. Centralizing it removes duplication and guarantees both pages share identical scheme rules.
Output: `src/services/urlUtils.ts` (+ test), `ReviewPage.tsx` updated to import it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-entry-list-detail-export/06-RESEARCH.md

<interfaces>
<!-- isSafeUrl currently lives privately in ReviewPage.tsx (lines 12-20). Extract verbatim behavior. -->
Current implementation in src/pages/ReviewPage.tsx:
  function isSafeUrl(raw: string): boolean
  — try { const { protocol } = new URL(raw); return protocol === 'http:' || protocol === 'https:' } catch { return false }

ReviewPage.tsx consumes it at the save path:
  const safeSourceUrl = sourceUrl && isSafeUrl(sourceUrl) ? sourceUrl : undefined
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared urlUtils.ts with isSafeUrl + unit tests</name>
  <files>src/services/urlUtils.ts, src/services/urlUtils.test.ts</files>
  <read_first>
    - src/pages/ReviewPage.tsx (lines 12-20: the isSafeUrl source of truth to extract)
    - .planning/phases/06-entry-list-detail-export/06-RESEARCH.md (Pattern 7)
    - src/services/entriesRepository.test.tsx (Vitest + describe/it import style to mirror)
  </read_first>
  <behavior>
    - isSafeUrl('https://example.com') === true
    - isSafeUrl('http://example.com') === true
    - isSafeUrl('javascript:alert(1)') === false
    - isSafeUrl('ftp://example.com') === false
    - isSafeUrl('not a url') === false
    - isSafeUrl('') === false
  </behavior>
  <action>
    Create `src/services/urlUtils.ts` exporting a single named function `isSafeUrl(raw: string): boolean` with identical behavior to the current ReviewPage private helper: parse `new URL(raw)`, return `true` only when `protocol` is `'http:'` or `'https:'`, and return `false` from a `catch` when the URL constructor throws. Include the existing JSDoc line describing it as a guard against `javascript:` XSS vectors.
    Create `src/services/urlUtils.test.ts` using Vitest (`import { describe, it, expect } from 'vitest'`) covering each case in the behavior block. Test the function purely — no mocks, no DOM, no router.
  </action>
  <acceptance_criteria>
    - `src/services/urlUtils.ts` exports `isSafeUrl` as a named export with signature `(raw: string) => boolean`.
    - All six behavior cases are asserted in `urlUtils.test.ts`.
    - `npx vitest run src/services/urlUtils.test.ts` exits 0 (all green).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/urlUtils.test.ts</automated>
  </verify>
  <done>urlUtils.ts exports isSafeUrl; all six unit cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update ReviewPage to import isSafeUrl from urlUtils (no behavior change)</name>
  <files>src/pages/ReviewPage.tsx</files>
  <read_first>
    - src/pages/ReviewPage.tsx (full file — remove the local def, add the import)
    - src/pages/ReviewPage.test.tsx (lines ~289-330: the WR-02 javascript: + valid-https tests that MUST stay green)
  </read_first>
  <action>
    In `src/pages/ReviewPage.tsx`, delete the module-private `isSafeUrl` function (lines ~12-20) and add `import { isSafeUrl } from '../services/urlUtils'` alongside the other imports. Do NOT change the save-path usage (`const safeSourceUrl = sourceUrl && isSafeUrl(sourceUrl) ? sourceUrl : undefined`) — only the source of the function changes. Make no other edits to ReviewPage.
  </action>
  <acceptance_criteria>
    - `ReviewPage.tsx` no longer defines a local `function isSafeUrl` (grep `grep -c 'function isSafeUrl' src/pages/ReviewPage.tsx` returns 0).
    - `ReviewPage.tsx` contains `import { isSafeUrl } from '../services/urlUtils'`.
    - `npx vitest run src/pages/ReviewPage.test.tsx` exits 0 — including the WR-02 tests "does not persist a javascript: sourceUrl" and "preserves a valid https: sourceUrl unchanged".
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/ReviewPage.test.tsx src/services/urlUtils.test.ts</automated>
  </verify>
  <done>ReviewPage imports the shared isSafeUrl; all Phase 4 ReviewPage tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored entry data → DOM render | A `sourceUrl` persisted in IndexedDB is untrusted at render time and may carry a `javascript:` scheme |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering/Spoofing | isSafeUrl scheme gate | mitigate | This plan provides the reusable gate: `isSafeUrl` returns true only for http:/https:, so callers (ReviewPage write-side, EntryDetailPage render-side) never treat a `javascript:` URL as safe |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase; nothing installed |
</threat_model>

<verification>
- `npx vitest run src/services/urlUtils.test.ts src/pages/ReviewPage.test.tsx` green.
- Full suite unaffected: `npx vitest run` green.
</verification>

<success_criteria>
isSafeUrl is a shared export in `src/services/urlUtils.ts`, ReviewPage imports it, and every Phase 4 ReviewPage test (including the WR-02 javascript: drop test) still passes.
</success_criteria>

<output>
Create `.planning/phases/06-entry-list-detail-export/06-01-SUMMARY.md` when done.
</output>
