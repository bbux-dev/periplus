---
phase: 06-entry-list-detail-export
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/exportEntries.ts
  - src/services/exportEntries.test.ts
autonomous: true
requirements: [EXP-01]
must_haves:
  truths:
    - "buildExportJson(entries, exportedAt) returns deterministic JSON with version 1, the injected exportedAt, and all entries"
    - "buildExportJson never reads Date.now() internally — exportedAt is injected"
    - "triggerDownload builds a JSON Blob, clicks a temporary download anchor, and revokes the object URL"
  artifacts:
    - path: "src/services/exportEntries.ts"
      provides: "Pure JSON shaper + browser download shim"
      exports: ["buildExportJson", "triggerDownload", "ExportEnvelope"]
    - path: "src/services/exportEntries.test.ts"
      provides: "Pure-function tests + mocked download-trigger tests"
  key_links:
    - from: "src/services/exportEntries.ts triggerDownload"
      to: "URL.createObjectURL / HTMLAnchorElement"
      via: "Blob + createObjectURL + anchor.click() + revokeObjectURL"
      pattern: "URL\\.createObjectURL"
---

<objective>
Create `src/services/exportEntries.ts` with a deterministic, unit-testable pure JSON shaper (`buildExportJson`) and a thin, mockable browser download shim (`triggerDownload`), satisfying EXP-01 (export all entries as JSON).

Purpose: Splitting export into a pure function (no I/O, no Date) and a side-effectful shim keeps the data shape fully testable without mocks while the unavoidable browser-only download path stays small and mocked in jsdom.
Output: `exportEntries.ts` (buildExportJson + triggerDownload + ExportEnvelope) and full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-entry-list-detail-export/06-RESEARCH.md

<interfaces>
<!-- LifeLogEntry from src/services/db.ts (import type only): -->
  interface LifeLogEntry { id; domain; type; title; description?; occurredAt?; recordedAt; sourceUrl?; amount?; location?; tags: string[]; metadata: Record<string,unknown>; syncedAt: number | null }

<!-- Export envelope shape to produce (RESEARCH Pattern 9): -->
  interface ExportEnvelope { version: 1; exportedAt: number; entries: LifeLogEntry[] }

<!-- jsdom note: URL.createObjectURL is NOT implemented — must be vi.spyOn-mocked in tests (RESEARCH Pattern 10). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildExportJson pure function + ExportEnvelope + pure tests</name>
  <files>src/services/exportEntries.ts, src/services/exportEntries.test.ts</files>
  <read_first>
    - src/services/db.ts (LifeLogEntry type to import as `import type`)
    - .planning/phases/06-entry-list-detail-export/06-RESEARCH.md (Pattern 9 + the buildExportJson test sketch)
    - src/services/entriesRepository.test.tsx (Vitest import style)
  </read_first>
  <behavior>
    - buildExportJson([oneEntry], 1700000000000) → JSON.parse(result).version === 1, .exportedAt === 1700000000000, .entries length 1, .entries[0].id matches
    - buildExportJson([], 999) → JSON.parse(result).entries deep-equals [] and .version === 1
    - The same inputs always produce byte-identical output (deterministic — no Date.now() inside)
  </behavior>
  <action>
    Create `src/services/exportEntries.ts`. Add `import type { LifeLogEntry } from './db'`. Export an `interface ExportEnvelope { version: 1; exportedAt: number; entries: LifeLogEntry[] }`. Export a pure function `buildExportJson(entries: LifeLogEntry[], exportedAt: number): string` that constructs an `ExportEnvelope` literal `{ version: 1, exportedAt, entries }` and returns `JSON.stringify(envelope, null, 2)`. Document that `exportedAt` is injected by the caller and the function must NEVER call `Date.now()` internally (determinism requirement). 
    In `src/services/exportEntries.test.ts` add a `describe('buildExportJson')` block covering the behavior cases with no mocks: build with a fixed `exportedAt`, `JSON.parse` the result, and assert version/exportedAt/entries. Use a minimal `LifeLogEntry`-shaped literal cast `as LifeLogEntry[]` for fixtures (id, domain, type, title, recordedAt, tags, metadata, syncedAt).
  </action>
  <acceptance_criteria>
    - `exportEntries.ts` exports `buildExportJson` and `ExportEnvelope`.
    - `buildExportJson` body contains no reference to `Date.now` (grep `grep -c 'Date.now' src/services/exportEntries.ts` returns 0).
    - Output is `JSON.stringify(..., null, 2)` (2-space pretty-printed).
    - `npx vitest run src/services/exportEntries.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/exportEntries.test.ts</automated>
  </verify>
  <done>buildExportJson produces the versioned envelope deterministically with injected exportedAt; pure tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: triggerDownload shim + jsdom-mocked tests</name>
  <files>src/services/exportEntries.ts, src/services/exportEntries.test.ts</files>
  <read_first>
    - src/services/exportEntries.ts (the file from Task 1 — append triggerDownload)
    - .planning/phases/06-entry-list-detail-export/06-RESEARCH.md (Pattern 10 + Pitfall 3: vi.restoreAllMocks; Pitfall 5: synchronous click)
  </read_first>
  <action>
    In `src/services/exportEntries.ts`, export `function triggerDownload(json: string, filename = 'life-log.json'): void` that: creates `new Blob([json], { type: 'application/json' })`, calls `URL.createObjectURL(blob)`, creates a temporary anchor via `document.createElement('a')`, sets `a.href` to the object URL and `a.download` to `filename`, appends it to `document.body`, calls `a.click()`, removes it from `document.body`, then calls `URL.revokeObjectURL(url)`. Document it as a side-effectful shim.
    In `src/services/exportEntries.test.ts` add a `describe('triggerDownload')` block with `afterEach(() => vi.restoreAllMocks())`. Mock `URL.createObjectURL` via `vi.spyOn(URL, 'createObjectURL').mockReturnValue('blob:fake')`, `vi.spyOn(URL, 'revokeObjectURL').mockImplementation(() => {})`, and `vi.spyOn(HTMLAnchorElement.prototype, 'click').mockImplementation(...)`. Assert: createObjectURL called once with `expect.any(Blob)`; anchor click called once; revokeObjectURL called with the returned `'blob:fake'`. Add a second test that captures `this` inside the click mock and asserts the anchor's `download` equals a passed-in filename and its `href` contains the mocked blob URL. Treat triggerDownload as synchronous (no await).
  </action>
  <acceptance_criteria>
    - `exportEntries.ts` exports `triggerDownload(json, filename?)`.
    - triggerDownload calls `URL.createObjectURL`, `anchor.click()`, and `URL.revokeObjectURL` (grep `grep -c 'revokeObjectURL' src/services/exportEntries.ts` returns ≥ 1).
    - Tests use `vi.spyOn` on `URL.createObjectURL` / `HTMLAnchorElement.prototype.click` and call `vi.restoreAllMocks()` in `afterEach`.
    - The download-attribute test asserts `download === 'my-log.json'` for `triggerDownload('{}', 'my-log.json')`.
    - `npx vitest run src/services/exportEntries.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/exportEntries.test.ts</automated>
  </verify>
  <done>triggerDownload builds a Blob, clicks a download anchor, and revokes the URL; mocked tests pass and do not leak spies across files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB entries → local file | Export serializes local data to a file on the user's own device; no network egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | JSON export | accept | Export writes the user's own local data to their own device via a Blob download; no transmission, no server, no third party — no new disclosure surface |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase; Blob/URL are browser built-ins |
</threat_model>

<verification>
- `npx vitest run src/services/exportEntries.test.ts` green (pure + mocked-trigger).
- Full suite: `npx vitest run` green — confirms URL spies were restored and did not leak.
</verification>

<success_criteria>
`exportEntries.ts` exposes a deterministic `buildExportJson(entries, exportedAt)` and a mockable `triggerDownload(json, filename)`, both fully tested.
</success_criteria>

<output>
Create `.planning/phases/06-entry-list-detail-export/06-03-SUMMARY.md` when done.
</output>
