---
phase: 21-app-shell-routing-rewrite-atomic-drop
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - src/services/activeMode.ts
  - src/services/activeMode.test.tsx
  - src/components/layout/AppShell.tsx
  - src/App.tsx
  - src/App.test.tsx
autonomous: true
requirements: [UI-02, UI-04]
must_haves:
  truths:
    - "activeMode.ts no longer exports listModes and no longer imports ShortcutConfig"
    - "AppShell has zero imports of NAVIGATION, useShortcutConfig, listModes, or LayoutChips"
    - "AppShell hamburger menu contains exactly Home, Previous Trips, and Settings links"
    - "AppShell app bar shows the active trip name when activeMode.mode === 'trip'"
    - "App.tsx exposes only trip-flow routes plus the * PlaceholderPage catch-all"
    - "Navigating to an unknown path renders the PlaceholderPage 404 without crashing"
  artifacts:
    - path: "src/components/layout/AppShell.tsx"
      provides: "Trip-only top-level navigation shell (UI-02)"
      contains: "useActiveMode"
    - path: "src/App.tsx"
      provides: "Trip-only router with 404 catch-all (UI-04)"
      contains: "path=\"*\""
    - path: "src/App.test.tsx"
      provides: "Trip-router coverage replacing old route tests"
      contains: "TripHomePage"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/TripHomePage.tsx"
      via: "import + Route path='/'"
      pattern: "TripHomePage"
    - from: "src/App.tsx"
      to: "src/pages/PlaceholderPage.tsx"
      via: "Route path='*' catch-all"
      pattern: "path=\"\\*\""
    - from: "src/components/layout/AppShell.tsx"
      to: "src/services/activeMode.ts"
      via: "useActiveMode() for app-bar trip name"
      pattern: "useActiveMode"
---

<objective>
Rewrite the navigational shell to trip-only and finish decoupling `activeMode.ts` from the dead shortcut subsystem. Removing `listModes` and rewriting `AppShell` happen together because AppShell is the only kept caller of `listModes`; doing them in one plan keeps `tsc -b` green across the commit. `App.tsx` is then rewritten to expose only trip-flow routes (importing the pages built in plan 21-02) plus the `*` catch-all, and `App.test.tsx` is replaced with trip-router coverage. This runs BEFORE the atomic deletion (21-04) so the new shell compiles against new code while the old pages still exist.

Purpose: Deliver the trip-only AppShell (UI-02) and the trip-only router with graceful 404 (UI-04).
Output: Decoupled activeMode.ts; rewritten AppShell.tsx, App.tsx, App.test.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-CONTEXT.md
@.planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
From src/services/activeMode.ts (KEEP, minus listModes): `useActiveMode()`, `activateMode`, `defaultInstanceLabel`, `activeModeRepository`, `ActiveMode` interface (with optional tripId). Line 3 `import type { ShortcutConfig }` and the listModes function (lines ~92-95) are REMOVED.
listModes callers (verified): src/components/layout/AppShell.tsx (line 16 import, line 29 call) and src/services/activeMode.test.tsx (line 9 import, lines 132-142 tests). Both are handled in this plan. All other listModes/NAVIGATION/shortcut references live in DROP-list files.
Pages available from plan 21-02: TripHomePage (named export), CreateTripPage (named export), SettingsPage (named export, export-only). PlaceholderPage (KEPT) accepts a `title` prop and serves as the 404 + temporary placeholders for Phase 22-24 routes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove listModes from activeMode.ts and rewrite AppShell to trip-only nav</name>
  <files>src/services/activeMode.ts, src/services/activeMode.test.tsx, src/components/layout/AppShell.tsx</files>
  <read_first>
    - src/services/activeMode.ts (line 3 ShortcutConfig import; the listModes function ~lines 92-95; keep everything else)
    - src/services/activeMode.test.tsx (line 9 listModes import; the listModes describe block lines 132-142 to delete)
    - src/components/layout/AppShell.tsx (current file — keep sticky header, hamburger toggle, Escape/outside-click handlers, useActiveMode center display; strip all NAVIGATION/useShortcutConfig/listModes/LayoutChips/mode-switcher/domain-tree)
    - .planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-PATTERNS.md sections "AppShell.tsx" (KEEP/DROP imports, state, handlers, app-bar + hamburger structure) and "activeMode.ts — remove listModes"
  </read_first>
  <action>
    In activeMode.ts: delete the line-3 `import type { ShortcutConfig } from '../config/shortcutConfig'` and delete the entire `listModes` function. Keep the ActiveMode interface (with tripId?), activeModeRepository, useActiveMode, defaultInstanceLabel, and activateMode untouched. In activeMode.test.tsx: remove `listModes` from the import on line 9 and delete the `describe('listModes', ...)` block (lines ~132-142) and any makeConfig helper used solely by it — the rest of the file stays green. Rewrite AppShell.tsx per PATTERNS: keep imports `useState/useEffect/useRef`, `useLocation/useNavigate/Link`, `HomeIcon/Bars3Icon`, `cn`, and `useActiveMode`; DROP imports of NAVIGATION, Input, useShortcutConfig, and activateMode/defaultInstanceLabel/listModes. Keep state `pathname`, `navigate`, `open`, `wrapperRef`, `activeMode`; drop expanded/modeSubmenuOpen/pendingMode/pendingLabel and the toggleDomain/selectPendingMode/confirmPendingMode/resetModeMenu handlers. Keep the Escape + outside-click effects verbatim. Keep the sticky header: left home button hidden at '/', center showing `activeMode.label` only when `activeMode?.mode === 'trip'`, right hamburger toggle. The dropdown nav contains exactly three Link elements: Home -> '/', Previous Trips -> '/trips', Settings -> '/settings', each calling setOpen(false) onClick. Keep the `{children}` slot. Remove every mode-switcher and domain-tree section.
  </action>
  <verify>
    <automated>cd /home/bbux/git/life-log && npx tsc -b 2>&1 | tail -5 && npx vitest run src/services/activeMode.test.tsx 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "listModes|ShortcutConfig" src/services/activeMode.ts` returns NO hits.
    - `grep -nE "listModes" src/services/activeMode.test.tsx` returns NO hits.
    - `grep -nE "NAVIGATION|useShortcutConfig|listModes|LayoutChips" src/components/layout/AppShell.tsx` returns NO hits (UI-02 dead-import gate).
    - `grep -c "Link" src/components/layout/AppShell.tsx` confirms the three nav Links exist; the hamburger menu renders exactly Home, Previous Trips, Settings.
    - `grep -n "activeMode?.mode === 'trip'" src/components/layout/AppShell.tsx` confirms the app-bar trip-name conditional.
    - `npx tsc -b` clean; `npx vitest run src/services/activeMode.test.tsx` green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite App.tsx to trip-only routes and replace App.test.tsx</name>
  <files>src/App.tsx, src/App.test.tsx</files>
  <read_first>
    - src/App.tsx (current file — keep the AppShell wrapper + Routes/Route structure; replace all route elements and page imports)
    - .planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-PATTERNS.md section "App.tsx" (imports to keep, core route structure, catch-all)
    - src/pages/PlaceholderPage.tsx (title prop — used for 404 and Phase 22-23 placeholders)
    - src/App.test.tsx (current file — to be fully replaced with trip-router coverage)
  </read_first>
  <action>
    Rewrite App.tsx imports to: `Routes, Route` from react-router-dom; `AppShell`; `PlaceholderPage`; `SettingsPage`; and the new `TripHomePage` and `CreateTripPage`. Do NOT import any deleted page (no DashboardPage/DomainPage/CaptureUrlPage/ReviewPage/ManualEntryPage/EntryList/Detail/Edit/QuickCapture/ManageShortcuts/ShortcutForm). Inside the AppShell-wrapped Routes, define: `/` -> TripHomePage; `/create-trip` -> CreateTripPage; `/settings` -> SettingsPage; and clearly-labeled temporary PlaceholderPage routes for Phase 22-24 targets that do not yet exist: `/expense` (title "Log Expense"), `/activity` (title "Log Activity"), `/activity/:type` (title "Activity Form"), `/trips` (title "Previous Trips"), `/trips/:tripId` (title "Trip Detail"); finally the `*` catch-all -> `<PlaceholderPage title="Page Not Found" />`. Keep `export default App`. Replace App.test.tsx entirely with trip-router coverage using MemoryRouter + initialEntries: assert (a) '/' resolves to TripHomePage content (loading or routed page renders without crashing), (b) '/settings' renders the Settings export UI, (c) an unknown path like '/foo' renders the Page Not Found placeholder, (d) the test file imports NONE of the deleted pages. Use the Dexie reset beforeEach if a route under test reads Dexie; use `vi.useFakeTimers({ toFake: ['Date'] })` only if needed.
  </action>
  <verify>
    <automated>cd /home/bbux/git/life-log && npx tsc -b 2>&1 | tail -5 && npx vitest run src/App.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "DashboardPage|DomainPage|CaptureUrlPage|ReviewPage|ManualEntryPage|EntryListPage|EntryDetailPage|EntryEditPage|QuickCapturePage|ManageShortcutsPage|ShortcutFormPage" src/App.tsx src/App.test.tsx` returns NO hits.
    - `grep -n "path=\"\\*\"" src/App.tsx` and `grep -n "Page Not Found" src/App.tsx` both return hits (graceful 404, UI-04).
    - `grep -nE "TripHomePage|CreateTripPage" src/App.tsx` returns hits; '/' maps to TripHomePage.
    - `npx vitest run src/App.test.tsx` is green and includes an unknown-path -> Page Not Found assertion.
    - `npx tsc -b` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path -> router | Arbitrary/unknown paths reach the router (e.g. deep links, typos) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Denial of Service | App.tsx router | mitigate | `*` catch-all renders PlaceholderPage 404 — unknown paths never crash the app (asserted by App.test.tsx) |
| T-21-05 | Information Disclosure | AppShell app bar | accept | Active trip label is the user's own data, React-escaped text only |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npx tsc -b` clean after both tasks.
- AppShell dead-import grep gate passes (no NAVIGATION/useShortcutConfig/listModes/LayoutChips).
- App.test.tsx green with unknown-path -> 404 coverage and no deleted-page imports.
- activeMode.ts has no listModes/ShortcutConfig references.
</verification>

<success_criteria>
- activeMode.ts decoupled from the shortcut subsystem (no listModes/ShortcutConfig).
- AppShell is trip-only: Home / Previous Trips / Settings + active-trip-name app bar (UI-02).
- App.tsx exposes only trip-flow routes + graceful 404 catch-all (UI-04).
- App.test.tsx replaced with trip-router coverage; no deleted-page imports.
</success_criteria>

<output>
Create `.planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-03-SUMMARY.md` when done
</output>
