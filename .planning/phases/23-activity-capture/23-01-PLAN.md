---
phase: 23-activity-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/activityTypes.ts
  - src/components/ui/StarRating.tsx
  - src/components/ui/StarRating.test.tsx
autonomous: true
requirements: [ACT-04]
must_haves:
  truths:
    - "ACTIVITY_TYPES exports the 5 ordered labels Hike, Show, Restaurant, Cafe, Other"
    - "StarRating renders 5 <button> stars, each with an aria-label like '3 stars'"
    - "Tapping star N calls onChange(N); tapping the currently-selected star calls onChange(0) to clear"
    - "ArrowRight increases the rating, ArrowLeft decreases it"
  artifacts:
    - path: "src/config/activityTypes.ts"
      provides: "ACTIVITY_TYPES const (5 ordered labels) + ActivityType union type"
      contains: "ACTIVITY_TYPES"
    - path: "src/components/ui/StarRating.tsx"
      provides: "Accessible 1-5 star control (controlled value/onChange)"
      exports: ["StarRating"]
    - path: "src/components/ui/StarRating.test.tsx"
      provides: "Coverage for render, tap-set, tap-clear, arrow keys, aria-labels"
  key_links:
    - from: "src/components/ui/StarRating.tsx"
      to: "@heroicons/react/24/solid + /24/outline"
      via: "StarIcon (filled) / StarIcon as StarIconOutline (empty)"
      pattern: "@heroicons/react/24/(solid|outline)"
    - from: "src/components/ui/StarRating.tsx"
      to: "rendered DOM"
      via: "<button> elements (NOT <div>/<span>) per star"
      pattern: "<button"
---

<objective>
Create the two leaf dependencies the activity flow is built on: the `ACTIVITY_TYPES`
constant (single source of truth for the type picker and the form) and the accessible
`StarRating` component (ACT-04). Both are self-contained — no routing, no Dexie, no save path.

Purpose: Downstream plans (23-02 type page, 23-03 form) import these directly instead of
re-deriving the type list or hand-rolling an inaccessible rating control.
Output: `src/config/activityTypes.ts`, `src/components/ui/StarRating.tsx`, and its test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-activity-capture/23-CONTEXT.md
@.planning/phases/23-activity-capture/23-PATTERNS.md

<interfaces>
<!-- Analog already in the codebase — copy its shape exactly. -->
src/config/expenseCategories.ts exports `EXPENSE_CATEGORIES` as a `... as const` tuple +
a derived union type. activityTypes.ts mirrors that shape with no React imports.

Heroicons (already a dependency, ^2.2.0):
  import { StarIcon } from '@heroicons/react/24/solid'
  import { StarIcon as StarIconOutline } from '@heroicons/react/24/outline'

cn helper: src/components/ui/cn.ts (default-style className combiner used across components/ui).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ACTIVITY_TYPES constant</name>
  <read_first>
    - src/config/expenseCategories.ts (exact analog: `as const` tuple + derived union type)
    - .planning/phases/23-activity-capture/23-PATTERNS.md section "src/config/activityTypes.ts"
  </read_first>
  <files>src/config/activityTypes.ts</files>
  <action>
    Create activityTypes.ts mirroring expenseCategories.ts. Export `ACTIVITY_TYPES` as a
    `readonly` tuple via `as const` with EXACTLY these five labels in this order: Hike, Show,
    Restaurant, Cafe, Other. Export a derived union type `ActivityType` = `(typeof
    ACTIVITY_TYPES)[number]`. No React, component, or service imports — keep it import-light so
    both pages and tests can import it cheaply. Add a short header comment noting the route slug
    is `label.toLowerCase()` (e.g. 'Hike' -> '/activity/hike') and the form recovers the
    canonical label via `ACTIVITY_TYPES.find(t => t.toLowerCase() === slug)`.
  </action>
  <acceptance_criteria>
    - `grep -c "as const" src/config/activityTypes.ts` returns >= 1
    - `grep -E "'Hike'|'Show'|'Restaurant'|'Cafe'|'Other'" src/config/activityTypes.ts` shows all five labels
    - `grep -c "export type ActivityType" src/config/activityTypes.ts` returns 1
    - `npx tsc -b` compiles clean
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <done>ACTIVITY_TYPES (5 ordered labels) and ActivityType type are exported; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StarRating component (ACT-04) + tests</name>
  <read_first>
    - .planning/research/STACK.md section "StarRating" (lines ~100-158, the full implementation pattern)
    - .planning/phases/23-activity-capture/23-PATTERNS.md sections "src/components/ui/StarRating.tsx" and "StarRating.test.tsx"
    - src/components/ui/FormField.test.tsx (ui-component test shell shape)
    - src/components/ui/cn.ts (className combiner)
  </read_first>
  <files>src/components/ui/StarRating.tsx, src/components/ui/StarRating.test.tsx</files>
  <behavior>
    - Renders exactly 5 buttons, each role=button with aria-label "1 star", "2 stars" ... "5 stars"
    - Clicking the "3 stars" button when value=0 calls onChange(3)
    - Clicking the "3 stars" button when value=3 calls onChange(0) (tap selected = clear)
    - Focused "2 stars" button + ArrowRight calls onChange(3); ArrowLeft from value=4 calls onChange(3)
    - aria-pressed is true on the button matching value, false otherwise
  </behavior>
  <action>
    Implement StarRating as a controlled component with props `value: number` (0 = unset) and
    `onChange: (n: number) => void`. Render a container `<div role="group" aria-label="Rating">`
    holding five `<button type="button">` elements built by mapping over [1,2,3,4,5]. Each button:
    `aria-label={`${n} star${n > 1 ? 's' : ''}`}`, `aria-pressed={value === n}`, `onClick` calls
    `onChange(value === n ? 0 : n)` (tap the selected star to clear), and `onKeyDown` handles
    ArrowRight -> `onChange(Math.min(5, n + 1))` and ArrowLeft -> `onChange(Math.max(0, n - 1))`,
    calling `e.preventDefault()` in both so the page does not scroll. Use `<button>` elements
    (NEVER `<div>`/`<span>`) so iOS fires tap events without a cursor:pointer workaround. Min tap
    target 44x44 px via `h-11 w-11` classes through `cn`. Render the filled Heroicons `StarIcon`
    (24/solid) when `n <= value` else `StarIconOutline` (24/outline), each with `aria-hidden="true"`
    so the accessible name comes from the button label only. Write StarRating.test.tsx covering all
    five behaviors above using @testing-library/react + userEvent (no Dexie, no fake timers needed).
    NO fenced code is reproduced from the pattern doc verbatim without adapting imports.
  </action>
  <acceptance_criteria>
    - `grep -c "<button" src/components/ui/StarRating.tsx` returns >= 1 and `grep -c "<div role" src/components/ui/StarRating.tsx` shows the group container uses a div role="group", not clickable star divs
    - `grep -E "aria-label=" src/components/ui/StarRating.tsx` references `star` (per-star labels)
    - `grep -E "ArrowRight|ArrowLeft" src/components/ui/StarRating.tsx` shows both arrow keys handled
    - `grep -E "@heroicons/react/24/(solid|outline)" src/components/ui/StarRating.tsx` shows both icon variants imported
    - `npx vitest run src/components/ui/StarRating.test.tsx` passes (render, tap-set, tap-clear, ArrowRight, ArrowLeft, aria-pressed)
    - `npx tsc -b` compiles clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/ui/StarRating.test.tsx</automated>
  </verify>
  <done>StarRating renders 5 accessible button stars; tap-set, tap-clear, and arrow-key behaviors pass; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap/keyboard -> StarRating onChange | Controlled value bounded to 0-5 by the component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | StarRating value | mitigate | onChange clamps via Math.min(5, ...)/Math.max(0, ...); tap path only emits 0..5 |
| T-23-02 | Denial of Service | arrow-key handler | accept | local, single-user PWA; preventDefault only suppresses scroll, no loops |

No package installs in this plan (Heroicons + cn already dependencies — zero new deps), so no
supply-chain (T-23-SC) checkpoint is required.
</threat_model>

<verification>
- `npx vitest run src/components/ui/StarRating.test.tsx` green
- `npx tsc -b` clean
- ACTIVITY_TYPES importable with the five ordered labels
</verification>

<success_criteria>
ACTIVITY_TYPES constant + StarRating component exist, StarRating is keyboard-operable and
screen-reader-labeled with `<button>` stars, all StarRating tests pass, and the wider suite + tsc stay green.
</success_criteria>

<output>
Create `.planning/phases/23-activity-capture/23-01-SUMMARY.md` when done.
</output>
