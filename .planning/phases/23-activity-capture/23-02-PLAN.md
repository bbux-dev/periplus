---
phase: 23-activity-capture
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/pages/ActivityTypePage.tsx
  - src/pages/ActivityTypePage.test.tsx
  - src/App.tsx
autonomous: true
requirements: [ACT-01]
must_haves:
  truths:
    - "Navigating to /activity renders 5 large tap-target buttons: Hike, Show, Restaurant, Cafe, Other"
    - "Tapping a type button navigates to /activity/<slug> (lowercase label)"
    - "A Back control returns the user toward Trip Home"
  artifacts:
    - path: "src/pages/ActivityTypePage.tsx"
      provides: "Activity type picker page driven by ACTIVITY_TYPES"
      exports: ["ActivityTypePage"]
    - path: "src/pages/ActivityTypePage.test.tsx"
      provides: "Coverage: 5 buttons render + Hike navigates to /activity/hike"
    - path: "src/App.tsx"
      provides: "Route /activity -> ActivityTypePage (replaces PlaceholderPage)"
      contains: "ActivityTypePage"
  key_links:
    - from: "src/pages/ActivityTypePage.tsx"
      to: "src/config/activityTypes.ts"
      via: "import ACTIVITY_TYPES, map over it for the button grid"
      pattern: "ACTIVITY_TYPES"
    - from: "src/pages/ActivityTypePage.tsx"
      to: "react-router navigate"
      via: "navigate(`/activity/${type.toLowerCase()}`)"
      pattern: "navigate\\(`/activity/"
    - from: "src/App.tsx"
      to: "src/pages/ActivityTypePage.tsx"
      via: "<Route path=\"/activity\" element={<ActivityTypePage />} />"
      pattern: "path=\"/activity\""
---

<objective>
Build `ActivityTypePage` at `/activity` (ACT-01): a constant-driven grid of large tap targets
for the five activity types, each navigating to the form pre-seeded with the chosen type. Wire
the `/activity` route in App.tsx, replacing its PlaceholderPage stub.

Purpose: This is the entry point of the activity flow — Trip Home's "Activity" CTA already
navigates here.
Output: `src/pages/ActivityTypePage.tsx`, its test, and the updated `/activity` route in App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-activity-capture/23-CONTEXT.md
@.planning/phases/23-activity-capture/23-PATTERNS.md
@.planning/phases/23-activity-capture/23-01-SUMMARY.md

<interfaces>
From src/config/activityTypes.ts (created in 23-01):
  export const ACTIVITY_TYPES = ['Hike','Show','Restaurant','Cafe','Other'] as const
  export type ActivityType = (typeof ACTIVITY_TYPES)[number]

Navigation analog — src/pages/TripHomePage.tsx already does `navigate('/activity')` on its
Activity CTA, so /activity is the confirmed destination this page serves.

Back-control analog — src/pages/CreateTripPage.tsx (useBackOrHome('/') + ChevronLeftIcon Back button).
Grid analog — src/components/dashboard/ExpenseSheet.tsx category grid (button grid + cn classes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivityTypePage component + test</name>
  <read_first>
    - .planning/phases/23-activity-capture/23-PATTERNS.md sections "src/pages/ActivityTypePage.tsx" and "ActivityTypePage.test.tsx"
    - src/pages/CreateTripPage.tsx (Back button via useBackOrHome, page chrome)
    - src/pages/TripHomePage.tsx (useNavigate + page layout classes)
    - src/components/dashboard/ExpenseSheet.tsx (category button-grid styling to adapt)
  </read_first>
  <files>src/pages/ActivityTypePage.tsx, src/pages/ActivityTypePage.test.tsx</files>
  <action>
    Create and export `ActivityTypePage`. Import `ACTIVITY_TYPES` from ../config/activityTypes,
    `useNavigate` from react-router-dom, `useBackOrHome` from ../hooks/useBackOrHome, `cn` from
    ../components/ui/cn, and `ChevronLeftIcon` from @heroicons/react/24/outline. Render a mobile-first
    page (mirror CreateTripPage's outer container + Back button via `useBackOrHome('/')`) with a
    heading like "Log Activity" and a grid (`grid-cols-2 gap-3`) produced by mapping over
    ACTIVITY_TYPES into one `<button type="button">` per label. Each button is a large tap target
    (e.g. `h-20 rounded-xl text-base font-semibold`) and its onClick calls
    `navigate(`/activity/${type.toLowerCase()}`)`. Do NOT hardcode the five labels inline — they
    MUST come from ACTIVITY_TYPES so the list stays single-sourced. Write ActivityTypePage.test.tsx
    using MemoryRouter: (a) renders 5 buttons (Hike, Show, Restaurant, Cafe, Other found by role
    name); (b) clicking Hike inside a Routes setup with a probe element at "/activity/:type"
    navigates and shows the probe. Reset Dexie in beforeEach (db.delete/open) and vi.useRealTimers
    in afterEach per the established test harness, even though this page does no Dexie itself.
  </action>
  <acceptance_criteria>
    - `grep -c "ACTIVITY_TYPES.map" src/pages/ActivityTypePage.tsx` returns >= 1 (grid is constant-driven, not hardcoded)
    - `grep -E "navigate\(.?/activity/" src/pages/ActivityTypePage.tsx` shows slug navigation using toLowerCase
    - `grep -c "useBackOrHome" src/pages/ActivityTypePage.tsx` returns >= 1 (Back control present)
    - `npx vitest run src/pages/ActivityTypePage.test.tsx` passes (5 buttons render + Hike navigates to /activity/hike)
    - `npx tsc -b` compiles clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/ActivityTypePage.test.tsx</automated>
  </verify>
  <done>ActivityTypePage renders 5 constant-driven tap targets and navigates to /activity/<slug>; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire /activity route in App.tsx</name>
  <read_first>
    - src/App.tsx (current placeholder route at line ~21: `/activity` -> PlaceholderPage)
    - .planning/phases/23-activity-capture/23-PATTERNS.md section "src/App.tsx (router modification)"
  </read_first>
  <files>src/App.tsx</files>
  <action>
    Add `import { ActivityTypePage } from './pages/ActivityTypePage'`. Replace the existing
    `<Route path="/activity" element={<PlaceholderPage title="Log Activity" />} />` with
    `<Route path="/activity" element={<ActivityTypePage />} />`. Do NOT touch the
    `/activity/:type` route yet (that is wired in 23-03) and do NOT remove or alter the other
    placeholder routes (`/expense`, `/trips`, `/trips/:tripId`, and the `*` catch-all all stay
    until their own phases). Leave the PlaceholderPage import in place (still used by remaining stubs).
  </action>
  <acceptance_criteria>
    - `grep -c "import { ActivityTypePage }" src/App.tsx` returns 1
    - `grep -E 'path="/activity"[^/]*ActivityTypePage' src/App.tsx` confirms the route element is ActivityTypePage
    - `grep -c "PlaceholderPage title=\"Log Activity\"" src/App.tsx` returns 0 (placeholder removed)
    - `grep -c "path=\"\\*\"" src/App.tsx` returns 1 (catch-all preserved) and `grep -c "path=\"/trips\"" src/App.tsx` returns 1 (other stubs intact)
    - `npx tsc -b` compiles clean
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <done>/activity renders ActivityTypePage; other placeholder routes and catch-all untouched; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL slug from a tapped button | Slug is always `label.toLowerCase()` of a known ACTIVITY_TYPES entry; the form re-validates it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | /activity/:type slug | mitigate | downstream form recovers canonical label via ACTIVITY_TYPES.find and falls back to 'Other' for unknown slugs (handled in 23-03) |
| T-23-04 | Spoofing | route wiring | accept | local single-user PWA; no auth boundary crossed |

No package installs in this plan (zero new deps), so no supply-chain (T-23-SC) checkpoint required.
</threat_model>

<verification>
- `npx vitest run src/pages/ActivityTypePage.test.tsx` green
- `npx tsc -b` clean
- Visiting /activity (after 23-03 wires the form) shows the five tap targets and routes onward
</verification>

<success_criteria>
ActivityTypePage at /activity shows the five activity types as large tap targets sourced from
ACTIVITY_TYPES, each navigating to /activity/<slug>; route is wired without breaking other stubs; suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/23-activity-capture/23-02-SUMMARY.md` when done.
</output>
