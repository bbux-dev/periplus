---
phase: 22-trip-home-expense-capture
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/components/dashboard/ExpenseSheet.tsx
  - src/components/dashboard/ExpenseSheet.test.tsx
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EXP-04, EXP-05, EXP-06]
must_haves:
  truths:
    - "Tapping Expense renders a bottom-sheet modal (role=dialog, aria-modal) with Amount, an 8-button Category grid, optional Vendor, optional Notes"
    - "Save is blocked (visible alert, no write) when Amount is empty/NaN/<=0 OR no Category is selected"
    - "A saved expense has domain='trips', type='expense', metadata.tripId = active trip UUID, metadata.category, and occurredAt = today's LOCAL midnight epoch"
    - "Escape and backdrop click dismiss the sheet"
  artifacts:
    - path: "src/components/dashboard/ExpenseSheet.tsx"
      provides: "Expense capture bottom-sheet; builds draft and calls draftToEntry(draft,'expense','trips',activeMode) then entriesRepository.create"
      exports: ["ExpenseSheet"]
      min_lines: 80
    - path: "src/components/dashboard/ExpenseSheet.test.tsx"
      provides: "Tests for domain/tripId/local-date stamping, validation gates, dismiss"
  key_links:
    - from: "src/components/dashboard/ExpenseSheet.tsx"
      to: "src/services/captureService.ts"
      via: "draftToEntry + todayLocalMidnightEpoch"
      pattern: "draftToEntry\\(.*'expense',\\s*'trips'"
    - from: "src/components/dashboard/ExpenseSheet.tsx"
      to: "src/config/expenseCategories.ts"
      via: "import EXPENSE_CATEGORIES"
      pattern: "EXPENSE_CATEGORIES"
    - from: "src/components/dashboard/ExpenseSheet.tsx"
      to: "src/services/entriesRepository.ts"
      via: "entriesRepository.create"
      pattern: "entriesRepository\\.create"
---

<objective>
Build `ExpenseSheet` — the fast expense-logging bottom sheet (Expense -> amount -> category -> save).
Clone the proven `HoleSheet` modal shell (backdrop, dialog panel, focus, body scroll lock, Cancel/Save)
and replace its shortcut-specific internals (keypad/presets/DSL preview) with an Amount field
(`inputMode="decimal"`, autofocus), an 8-button Category grid, and optional Vendor + Notes inputs.

Purpose: this is the product's defining UX. Saving MUST go through the single stamped path —
`draftToEntry(draft,'expense','trips',activeMode)` then `entriesRepository.create` — so the entry
lands with `domain='trips'`, an auto-stamped `metadata.tripId`, and a LOCAL-date `occurredAt`.
Output: `src/components/dashboard/ExpenseSheet.tsx`, `src/components/dashboard/ExpenseSheet.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-trip-home-expense-capture/22-CONTEXT.md
@.planning/phases/22-trip-home-expense-capture/22-PATTERNS.md
@src/components/dashboard/HoleSheet.tsx
@src/components/dashboard/SavedToast.tsx

<interfaces>
<!-- Contracts the executor needs — use directly, no codebase exploration required. -->

From src/services/captureService.ts:
  export interface ReviewDraft {
    sourceUrl?: string; title?: string; location?: string; description?: string;
    occurredAt?: number; amount?: number; tags?: string[];
    metadata: Record<string, unknown>;
  }
  export function draftToEntry(draft: ReviewDraft, type: EntryType, domain: EntryDomain,
                               activeMode?: ActiveMode | null): Omit<LifeLogEntry, 'id'>
    // auto-stamps metadata.mode/modeLabel and metadata.tripId from activeMode when activeMode.mode is set
  export function todayLocalMidnightEpoch(): number   // LOCAL midnight epoch ms (NOT UTC)

From src/services/entriesRepository.ts:
  entriesRepository.create(entry: Omit<LifeLogEntry,'id'>): Promise<LifeLogEntry>   // generates UUID, returns saved entry

From src/services/activeMode.ts:
  export interface ActiveMode { mode: string; label: string; tripId?: string }

From src/config/expenseCategories.ts (Plan 22-01):
  export const EXPENSE_CATEGORIES: readonly [...8 strings]   // Hotel, Rental Car, Flight, Taxi/Uber, Food, Gas, Parking, Other

From src/config/entryFields.ts (ENTRY_FIELDS.expense field->entry mapping):
  amount -> core.amount ; category -> metadata.category ; merchant(=Vendor) -> metadata.merchant ; description(=Notes) -> core.description
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the ExpenseSheet component</name>
  <files>src/components/dashboard/ExpenseSheet.tsx</files>
  <read_first>
    - src/components/dashboard/HoleSheet.tsx — the shell to clone (backdrop lines 114-119, dialog panel 122-131, focus useEffect 49-53, early return 108, action buttons 208-227).
    - .planning/phases/22-trip-home-expense-capture/22-PATTERNS.md section "src/components/dashboard/ExpenseSheet.tsx" (lines ~24-430) — KEEP/ADD/REPLACE map, the validation gate, the amount/category/vendor/notes markup, and the canonical `handleSave` (lines ~396-430).
    - .planning/phases/22-trip-home-expense-capture/22-CONTEXT.md sections "Expense save", "Dates", "Validation", "Expense sheet UX".
  </read_first>
  <behavior>
    - Renders only when isOpen; on open the Amount input is auto-focused and all fields reset to empty.
    - canSave is true only when amount.trim() !== '' AND !isNaN(parseFloat(amount)) AND parseFloat(amount) > 0 AND category !== ''.
    - handleSave with valid input builds a ReviewDraft { amount: parsedAmount, occurredAt: todayLocalMidnightEpoch(), description?(Notes), metadata: { category, merchant?(Vendor) } }, calls draftToEntry(draft,'expense','trips',activeMode), awaits entriesRepository.create, then calls onSave(savedEntry).
    - handleSave with invalid input sets a role="alert" error message and does NOT call onSave / create.
    - Backdrop click and onCancel dismiss without saving.
  </behavior>
  <action>
    Create the `ExpenseSheet` component (named export) in src/components/dashboard/ExpenseSheet.tsx
    following the PATTERNS map. Props interface: `{ isOpen: boolean; activeMode: ActiveMode | null | undefined;
    onSave: (entry: LifeLogEntry) => void; onCancel: () => void }`. State: amount, category, vendor, notes,
    error (all string), saving (boolean), plus a panelRef. KEEP from HoleSheet: the imports of useEffect/useRef/useState,
    Button, cn; the focus-management useEffect; the `if (!isOpen) return null` guard; the backdrop div (onClick=onCancel,
    aria-hidden); and the Cancel/Save button row. CHANGE the dialog panel's aria-label to "Log expense" and use the
    safe-area bottom padding from PATTERNS. ADD: a reset-on-open useEffect that clears all fields + error; a body
    scroll-lock useEffect (document.body.style.overflow='hidden' while open, restore on cleanup). REPLACE HoleSheet
    internals with: an Amount label+input (id="expense-amount", type="text", inputMode="decimal", autoFocus, required
    marker) bound to amount; an 8-button Category grid that maps EXPENSE_CATEGORIES (imported from
    '../../config/expenseCategories') to large tap-target <button type="button"> elements with aria-pressed and
    toggle-on-click selection; optional Vendor (id="expense-vendor") and Notes (id="expense-notes") text inputs; and
    an error display `{error && <p role="alert">...}`. Compute `canSave` per the behavior block. The Save button is
    disabled when `!canSave || saving` and its label flips to "Saving…" while saving.

    Implement `handleSave` per PATTERNS lines ~396-430: re-validate (parsedAmount NaN/<=0 or no category -> setError
    and return), set saving true, build the ReviewDraft INLINE (do NOT call buildReviewDraft — the direct draft is
    simpler here), include description only when Notes is non-empty and metadata.merchant only when Vendor is non-empty,
    set occurredAt: todayLocalMidnightEpoch() (NEVER new Date().toISOString() — PITFALLS Pitfall 3 UTC off-by-one),
    then `const entryData = draftToEntry(draft, 'expense', 'trips', activeMode)`. CRITICAL: pass the string literal
    'trips' as domain — NEVER defaultDomainForType('expense') (returns 'expenditures'). Do NOT set metadata.tripId by
    hand — draftToEntry stamps it from activeMode.tripId automatically. Await entriesRepository.create(entryData),
    capture the returned entry, call onSave(saved), and clear saving in a finally. Wire the Save button onClick to
    `() => void handleSave()`. Do NOT import or call useActiveMode inside the sheet — activeMode arrives as a prop so
    no hook is called conditionally. Do NOT modify src/services/captureService.ts (read-only import); its stale
    ReviewPage doc-comment is out of scope for this plan.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "draftToEntry(" src/components/dashboard/ExpenseSheet.tsx` >= 1 and the call passes 'expense','trips' (grep: `grep -E "draftToEntry\(.*'expense',\s*'trips'" src/components/dashboard/ExpenseSheet.tsx` matches).
    - `grep -c "defaultDomainForType" src/components/dashboard/ExpenseSheet.tsx` returns 0 (forbidden helper absent).
    - `grep -c "todayLocalMidnightEpoch" src/components/dashboard/ExpenseSheet.tsx` >= 1; `grep -c "toISOString" src/components/dashboard/ExpenseSheet.tsx` returns 0.
    - `grep -c "metadata.tripId\|tripId:" src/components/dashboard/ExpenseSheet.tsx` returns 0 (tripId is never set by hand).
    - `grep -c "EXPENSE_CATEGORIES" src/components/dashboard/ExpenseSheet.tsx` >= 1 (grid maps the shared constant).
    - `grep -c 'role="dialog"\|aria-modal\|role="alert"\|inputMode="decimal"' src/components/dashboard/ExpenseSheet.tsx` confirms dialog a11y + alert + decimal keypad present.
    - `npx tsc -b` exits clean.
  </acceptance_criteria>
  <done>ExpenseSheet renders the modal, validates, and saves a trips-domain expense via the single stamped path.</done>
</task>

<task type="auto">
  <name>Task 2: ExpenseSheet tests — stamping, validation, dismiss</name>
  <files>src/components/dashboard/ExpenseSheet.test.tsx</files>
  <read_first>
    - .planning/phases/22-trip-home-expense-capture/22-PATTERNS.md section "Test Patterns / ExpenseSheet.test.tsx" (lines ~718-826) — boilerplate, fake-timer setup, and the four canonical test bodies.
    - src/services/tripService.ts — `createAndActivateTrip(name)` returns the saved trip entry; use it to set up an active trip in tests.
    - .planning/STATE.md memory note: full fake timers stall Dexie — tests MUST use `vi.useFakeTimers({ toFake: ['Date'] })`.
  </read_first>
  <action>
    Create src/components/dashboard/ExpenseSheet.test.tsx using the PATTERNS boilerplate: import render/screen/act
    from @testing-library/react, userEvent, vitest helpers, db, activeModeRepository, entriesRepository,
    createAndActivateTrip. beforeEach `await db.delete(); await db.open()`; afterEach `vi.useRealTimers()`. Do NOT
    re-import fake-indexeddb/auto (already hoisted in src/test-setup.ts). Write these tests:
    (1) "saves expense with domain trips, tripId stamped, and local-date occurredAt": use
    `vi.useFakeTimers({ toFake: ['Date'] })` and `vi.setSystemTime(new Date('2026-06-19T23:30:00'))` (23:30 local
    catches the UTC off-by-one), create+activate a trip 'Paris', get activeMode, render ExpenseSheet isOpen with an
    onSave that captures the entry, type '42.50' into the amount field, click the 'Food' category button, click Save;
    assert saved.domain === 'trips', saved.metadata.tripId === tripEntry.id, saved.metadata.category === 'Food',
    saved.amount === 42.5, and saved.occurredAt === Date.parse('2026-06-19T00:00:00') (LOCAL midnight, NOT UTC Jun 20).
    (2) "does not call onSave when amount is missing": onSave = vi.fn(), select Food, click Save, assert onSave NOT
    called and a role="alert" is present. (3) "does not call onSave when no category selected": type '10' amount, click
    Save, assert onSave NOT called. (4) "calls onCancel when backdrop clicked": render with onCancel = vi.fn(), click
    the aria-hidden backdrop, assert onCancel called once. Wrap any save click that triggers async Dexie writes so the
    promise settles before assertions (await the userEvent click; if needed assert savedEntry via await/findBy).
  </action>
  <verify>
    <automated>npx vitest run src/components/dashboard/ExpenseSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/dashboard/ExpenseSheet.test.tsx` passes all 4 tests.
    - `grep -c "toFake: \['Date'\]" src/components/dashboard/ExpenseSheet.test.tsx` >= 1 (Dexie-safe fake timers).
    - `grep -c "domain).toBe('trips')\|domain).toEqual('trips')\|.domain).toBe(\"trips\")" src/components/dashboard/ExpenseSheet.test.tsx` confirms the domain assertion exists.
    - `grep -c "2026-06-19T00:00:00" src/components/dashboard/ExpenseSheet.test.tsx` >= 1 (local-midnight assertion present).
    - `grep -c "tripId" src/components/dashboard/ExpenseSheet.test.tsx` >= 1 (auto-stamp assertion present).
  </acceptance_criteria>
  <done>Tests prove domain='trips', auto-stamped tripId, local-date occurredAt, and both validation gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user form input -> ExpenseSheet -> draftToEntry -> IndexedDB | Free-text Amount/Vendor/Notes from the user crosses into a persisted entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | Amount parsing in handleSave | mitigate | parseFloat + canSave gate (non-empty, !NaN, >0); NaN/<=0 blocks save with a role="alert" message — no malformed amount is ever written |
| T-22-04 | Tampering | Wrong domain write | mitigate | domain hardcoded literal 'trips'; defaultDomainForType forbidden (grep gate == 0); acceptance test asserts domain==='trips' |
| T-22-05 | Information disclosure | Stored-XSS via Vendor/Notes | accept | Values stored as plain strings and rendered only through React JSX escaping; no dangerouslySetInnerHTML in the trips flow; local single-user app, no cross-user surface |
| T-22-06 | Tampering | npm/pip installs | accept | Zero new dependencies; ExpenseSheet reuses existing Button/cn/services only |
</threat_model>

<verification>
- `npx vitest run src/components/dashboard/ExpenseSheet.test.tsx` — all green.
- `npx tsc -b` — clean.
- Grep gates: domain literal 'trips' present, defaultDomainForType absent, toISOString absent, tripId never hand-set.
</verification>

<success_criteria>
ExpenseSheet delivers the Expense -> amount -> category -> save fast path with full modal a11y, validation
gates, and a saved entry carrying domain='trips', auto-stamped tripId, and a local-date occurredAt —
all proven by tests.
</success_criteria>

<output>
Create `.planning/phases/22-trip-home-expense-capture/22-02-SUMMARY.md` when done.
</output>
