---
phase: 18-active-mode-model-instance-stamping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/activeMode.ts
  - src/services/activeMode.test.tsx
  - src/services/captureService.ts
  - src/services/captureService.test.ts
  - src/pages/ReviewPage.tsx
  - src/pages/ReviewPage.test.tsx
  - src/hooks/useShortcutCapture.ts
  - src/hooks/useShortcutCapture.test.ts
autonomous: true
requirements: [MODE-01, MODE-02, STAMP-01]

must_haves:
  truths:
    - "The active mode + free-text instance label persist in Dexie settings and survive a reload"
    - "Activating a mode with no explicit label uses a sensible default <Mode>-<Mon>-<Year>"
    - "Modes derive from the existing shortcut-config layouts (existing layouts become modes)"
    - "Every capture made while a mode is active is stamped with metadata.mode and metadata.modeLabel via draftToEntry"
    - "When no mode is active, captures write neither mode nor modeLabel (no empty/placeholder keys)"
  artifacts:
    - path: "src/services/activeMode.ts"
      provides: "ActiveMode type, activeModeRepository (get/put under settings key 'activeMode'), useActiveMode() hook, defaultInstanceLabel(), activateMode(), listModes(config)"
      contains: "activeModeRepository"
    - path: "src/services/captureService.ts"
      provides: "draftToEntry gains an optional activeMode arg that stamps metadata.mode/modeLabel only when mode is non-empty"
      contains: "modeLabel"
  key_links:
    - from: "src/hooks/useShortcutCapture.ts"
      to: "src/services/captureService.ts"
      via: "draftToEntry(draft, type, domain, activeMode)"
      pattern: "draftToEntry\\([^)]*activeMode"
    - from: "src/pages/ReviewPage.tsx"
      to: "src/services/activeMode.ts"
      via: "useActiveMode()"
      pattern: "useActiveMode\\("
---

<objective>
Build the active-mode data/service layer and stamp captures with mode provenance — no navigation UI
(Phase 19). Persist `{ mode, label }` in Dexie `settings` (mirroring `activeLayoutRepository`); add a
reactive `useActiveMode()` hook, a `defaultInstanceLabel()` helper, `activateMode()`, and
`listModes(config)`. Thread an optional `activeMode` arg through `draftToEntry` so both one-tap save
and ReviewPage stamp `metadata.mode` + `metadata.modeLabel` — but ONLY when a mode is active.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-active-mode-model-instance-stamping/18-CONTEXT.md

<interfaces>
```typescript
// NEW src/services/activeMode.ts
export interface ActiveMode { mode: string; label: string }
export const activeModeRepository: {
  get(): Promise<ActiveMode | undefined>   // settings key 'activeMode'
  put(active: ActiveMode): Promise<void>
}
export function useActiveMode(): ActiveMode | undefined   // undefined = loading or none set
export function defaultInstanceLabel(mode: string, now?: Date): string   // `${mode}-${Mon}-${Year}`
export function activateMode(mode: string, label?: string): Promise<void> // label?.trim() || default
export function listModes(config: ShortcutConfig): string[]               // config.layouts.map(l=>l.name)

// captureService.ts — draftToEntry signature gains a trailing optional arg:
export function draftToEntry(draft, type, domain, activeMode?: ActiveMode | null): Omit<LifeLogEntry,'id'>
```
Conventions: mirror configRepository.ts's `activeLayoutRepository` + `useActiveLayoutName` EXACTLY
(undefined-loading semantics, no default value in useLiveQuery). Named exports only. TDD. Dexie +
fake-Date caveat: in tests that both write to Dexie and need a fixed "now", fake ONLY Date
(`vi.useFakeTimers({ toFake: ['Date'] })`) so awaited IndexedDB writes don't hang.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: activeMode.ts — repository, hook, helpers</name>
  <files>src/services/activeMode.ts, src/services/activeMode.test.tsx</files>
  <read_first>
    - src/services/configRepository.ts (activeLayoutRepository + useActiveLayoutName — mirror EXACTLY)
    - src/config/shortcutConfig.ts (ShortcutConfig/Layout)
    - src/services/configRepository.test.tsx (db reset harness; reactive-hook test via render + act)
  </read_first>
  <behavior>
    - `activeModeRepository.get()` reads settings key 'activeMode' → ActiveMode|undefined; `.put()` upserts it.
    - `useActiveMode()` is a useLiveQuery hook returning ActiveMode|undefined (undefined = Dexie opening
      or nothing saved — NO default value, matching useActiveLayoutName).
    - `defaultInstanceLabel('Travel', new Date('2026-06-18T12:00:00'))` === 'Travel-Jun-2026'
      (short month via toLocaleString('en-US',{month:'short'}), full year).
    - `activateMode('Travel')` persists `{mode:'Travel', label:'Travel-Jun-2026'}` (default label);
      `activateMode('Travel','Oregon')` persists label 'Oregon'; blank/whitespace label → default.
    - `listModes(config)` returns the layout names in order.
  </behavior>
  <action>
    Create activeMode.ts mirroring the activeLayoutRepository block of configRepository.ts (import db,
    useLiveQuery, ShortcutConfig type). Write activeMode.test.tsx FIRST (RED): repository round-trip via
    db; get() undefined when nothing saved; defaultInstanceLabel format (fake Date); activateMode default
    + explicit + blank-label cases (assert persisted value via repository.get); listModes from a config
    with two layouts; a reactive useActiveMode render test (mirror the useActiveLayoutName test) showing it
    updates after a put (wrap the write in act()).
  </action>
  <verify><automated>pnpm exec vitest run src/services/activeMode.test.tsx</automated></verify>
  <acceptance_criteria>
    - `grep -c "activeModeRepository" src/services/activeMode.ts` >= 1; `grep -c "export default" src/services/activeMode.ts` == 0.
    - Tests prove: persistence round-trip, undefined-when-empty, default label format, activateMode label rules, listModes.
    - Tests green; `pnpm exec tsc -b` clean.
  </acceptance_criteria>
  <done>active-mode repository/hook/helpers exist and are unit-tested, mirroring the active-layout pattern; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: draftToEntry stamps the active mode</name>
  <files>src/services/captureService.ts, src/services/captureService.test.ts</files>
  <read_first>
    - src/services/captureService.ts (draftToEntry L168-198; metadata handling)
    - src/services/activeMode.ts (ActiveMode type from Task 1)
  </read_first>
  <behavior>
    - draftToEntry gains a trailing optional `activeMode?: ActiveMode | null`.
    - When `activeMode?.mode` is a non-empty string, the returned metadata is
      `{ ...(draft.metadata ?? {}), mode: activeMode.mode, modeLabel: activeMode.label }`.
    - When activeMode is null/undefined OR its mode is empty, metadata is unchanged (`draft.metadata ?? {}`) —
      NO mode/modeLabel keys written.
    - All other draftToEntry behavior is identical (recordedAt, optional-field spread, etc.).
  </behavior>
  <action>
    Add the optional param; compute the metadata object as above (type-only import of ActiveMode from
    './activeMode'). Extend captureService.test.ts FIRST (RED): with an active mode the entry metadata
    contains mode+modeLabel and preserves prior draft.metadata keys; with undefined and with
    `{mode:'',label:''}` no mode/modeLabel keys appear; existing draftToEntry tests stay green.
  </action>
  <verify><automated>pnpm exec vitest run src/services/captureService.test.ts</automated></verify>
  <acceptance_criteria>
    - `grep -c "modeLabel" src/services/captureService.ts` >= 1.
    - Tests prove stamp-when-active, no-stamp-when-absent, prior-metadata preserved.
    - Tests green; `pnpm exec tsc -b` clean.
  </acceptance_criteria>
  <done>draftToEntry conditionally stamps mode/modeLabel from an optional activeMode arg; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: capture sites pass the active mode</name>
  <files>src/pages/ReviewPage.tsx, src/pages/ReviewPage.test.tsx, src/hooks/useShortcutCapture.ts, src/hooks/useShortcutCapture.test.ts</files>
  <read_first>
    - src/pages/ReviewPage.tsx (handleSave draftToEntry call L122; add useActiveMode() at top)
    - src/hooks/useShortcutCapture.ts (direct-save L118, handleSheetSave L143; add useActiveMode())
    - src/services/activeMode.ts (useActiveMode)
  </read_first>
  <behavior>
    - ReviewPage reads `const activeMode = useActiveMode()` and passes it as the 4th arg to draftToEntry
      in handleSave.
    - useShortcutCapture reads `const activeMode = useActiveMode()` and passes it to draftToEntry in BOTH
      the confirm:false direct-save branch and handleSheetSave (include activeMode in their useCallback deps).
    - The confirm:true branch is unchanged (navigates to ReviewPage which stamps on save).
    - Net effect: with an active mode persisted, a one-tap save, a sheet save, and a ReviewPage save all
      produce entries stamped with that mode+label; with no active mode, none are stamped.
  </behavior>
  <action>
    Wire useActiveMode() into both files and thread the value into the draftToEntry calls (keep the Phase 16
    withDefaultOccurredAt wrap intact — stamp is the 4th arg). Update the useCallback dependency arrays.
    Extend the two test files FIRST (RED): in useShortcutCapture.test.ts, persist an active mode via
    activeModeRepository.put before tapping a confirm:false zero-hole shortcut and assert the created entry's
    metadata.mode/modeLabel; repeat for a sheet save; and assert no stamp when no active mode is set. In
    ReviewPage.test.tsx, persist an active mode, render+save, and assert the saved entry is stamped (and not
    stamped when none is set). Keep all existing tests green.
  </action>
  <verify><automated>pnpm exec vitest run src/hooks/useShortcutCapture.test.ts src/pages/ReviewPage.test.tsx</automated></verify>
  <acceptance_criteria>
    - `grep -c "useActiveMode" src/pages/ReviewPage.tsx` >= 1 and `grep -c "useActiveMode" src/hooks/useShortcutCapture.ts` >= 1.
    - draftToEntry calls in both files pass the activeMode arg; confirm:true branch untouched.
    - RTL/hook tests prove stamping on all three save paths and no stamp when inactive.
    - Tests green; `pnpm exec tsc -b` clean.
  </acceptance_criteria>
  <done>All three capture save paths stamp metadata.mode/modeLabel when a mode is active; tests green.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run src/services/activeMode.test.tsx src/services/captureService.test.ts src/hooks/useShortcutCapture.test.ts src/pages/ReviewPage.test.tsx` green.
- Full suite `pnpm exec vitest run` green (no regression).
- `pnpm exec tsc -b` clean; `pnpm exec vite build` clean.
</verification>

<success_criteria>
- MODE-01: modes derive from the existing layouts (listModes); independent named lists.
- MODE-02: active mode + free-text instance label persist in Dexie settings; default label when none given.
- STAMP-01: captures stamped with metadata.mode/modeLabel via the single draftToEntry path; no stamp when inactive.
</success_criteria>

<output>
Create `.planning/phases/18-active-mode-model-instance-stamping/18-01-SUMMARY.md` when done.
</output>
