---
milestone: v0.5.0
audited: 2026-06-19
status: tech_debt
scores:
  requirements: 40/40
  phases: 5/5
  integration: clean
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 24-previous-trips-trip-detail-report
    items:
      - "EditEntryModal renders all fields as type='text' regardless of FieldDescriptor.inputType — date/number/amount fields don't trigger the right mobile keyboard on the edit path (values still round-trip correctly). Affects RPT-05."
      - "Stale 'currency' field in ENTRY_FIELDS.expense (inherited from the old general expense type) shows an empty 'Currency' input in the edit form; harmless (never set; buildEntryUpdate drops it) but visible."
  - phase: 20-trip-data-model-engine-extensions
    items:
      - "Orphaned exports: tripService.useTrips / listTrips are unused in the production call graph (PreviousTripsPage uses the single-pass summarizeTrips per PREV-04). Tested but dead in prod — keep for future use or prune."
  - phase: 21-app-shell-routing-rewrite-atomic-drop
    items:
      - "Stale JSDoc comments in captureService.ts reference the deleted ReviewPage.tsx/handleSave; SavedToast.tsx references the deleted DashboardPage. Documentation-only, no compile/runtime impact."
nyquist:
  overall: disabled
  note: "workflow.nyquist_validation set to false for this milestone — milestone-level research replaced per-phase RESEARCH/VALIDATION. Test coverage enforced via TDD plans + per-phase verification + code review."
---

# Milestone v0.5.0 — Trips MVP UI Refactor — Audit

**Status:** tech_debt (no blockers; minor deferred polish)
**Requirements:** 40/40 satisfied · **Phases:** 5/5 verified · **Integration:** clean · **E2E flows:** 5/5

## Summary

The UI was rewritten to a trip-only logger over the preserved headless engine. All 40 v0.5.0
requirements are satisfied and verified, cross-phase integration is clean (the `metadata.tripId`
join key is written identically by `ExpenseSheet` and `ActivityFormPage` via the shared
`draftToEntry` path and read identically by `summarizeTrips`/`useTripEntries`/`listTripEntries`),
and all five end-to-end flows wire together. `tsc -b` is clean; `vite build` succeeds; 361 tests
pass. 51 dead files (11 non-trip pages + the DSL/shortcut/layout subsystem) were removed atomically
in Phase 21. Zero new runtime dependencies.

## Requirements Coverage (40/40)

| Category | REQ-IDs | Phase | Status |
|----------|---------|-------|--------|
| Engine | ENG-01..04 | 20 | ✓ satisfied (verified 4/4) |
| UI rewrite + empty state | UI-01..05, TRIP-02, TRIP-04 | 21 | ✓ satisfied (verified 7/7) |
| Trip Home + Expense | TRIP-01/03, HOME-01..05, EXP-01..06 | 22 | ✓ satisfied (verified 13/13) |
| Activity | ACT-01..06 | 23 | ✓ satisfied (verified 6/6) |
| Previous Trips + Report | PREV-01..04, RPT-01..06 | 24 | ✓ satisfied (verified 10/10) |

No unsatisfied or orphaned requirements.

## Phase Verifications

| Phase | Status | Notes |
|-------|--------|-------|
| 20 Trip Data Model + Engine Extensions | passed | 4/4 must-haves |
| 21 App Shell + Routing Rewrite + Atomic Drop | passed | 7/7; 51 files dropped, suite green |
| 22 Trip Home + Expense Capture | passed | 13/13 |
| 23 Activity Capture | passed | 6/6 |
| 24 Previous Trips + Trip Detail + Report | human_needed | 10/10 logic verified; 3 browser-only visual checks deferred (user accepted) |

## End-to-End Flows (5/5 verified by integration checker)

1. First run → Create Trip → active mode → Trip Home — WIRED
2. Trip Home → Expense sheet → stamped save (domain='trips', tripId) → reactive total/recent — WIRED
3. Trip Home → Activity type → form (+ star rating, Other free-text) → stamped activity — WIRED
4. Previous Trips (single-pass) → drill in by UUID → category report + timeline → edit/delete → reactive recompute — WIRED
5. Engine consistency: shared `draftToEntry` write key == report/list read key (`metadata.tripId`) — WIRED

## Tech Debt (non-blocking — see frontmatter)

- **Edit-form input types** (Phase 24): EditEntryModal ignores `FieldDescriptor.inputType` — minor mobile-UX degradation on edit; values round-trip correctly.
- **Stale `currency` field** (Phase 24): an empty Currency input appears in the edit form; harmless.
- **Orphaned `useTrips`/`listTrips`** (Phase 20): unused in production (single-pass path used instead).
- **Stale doc comments** (Phase 21): captureService/SavedToast JSDoc reference deleted pages.

## Deferred (by design, future milestone)

Explicit "End Trip"/archive, "Delete Trip" + cascade, per-day grouping, budgets, charts,
multi-currency, receipt OCR, CSV/PDF export, in-trip filter/search.
