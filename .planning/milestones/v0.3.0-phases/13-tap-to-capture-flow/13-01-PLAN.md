---
phase: 13-tap-to-capture-flow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/services/captureService.ts
  - src/services/captureService.test.ts
  - src/pages/ReviewPage.tsx
autonomous: true
requirements: [CAP-01, CAP-04]

must_haves:
  truths:
    - "A complete (zero-hole) template produces a LifeLogEntry identical in shape to one saved through ReviewPage"
    - "A template with an empty positional slot is reported as a hole"
    - "A named param whose value is the {} token is reported as a named hole and stripped from the saved values"
    - "The DSL preview line built from merged values is the same line the capture path persists"
  artifacts:
    - path: "src/services/captureService.ts"
      provides: "draftToEntry, detectHoles, cleanValues, applyFills, buildDSLPreview, HOLE_TOKEN"
      exports: ["draftToEntry", "detectHoles", "cleanValues", "applyFills", "buildDSLPreview", "HOLE_TOKEN", "HoleMap"]
      min_lines: 60
    - path: "src/services/captureService.test.ts"
      provides: "Unit coverage for CAP-01 (draftToEntry shape) and CAP-04 ({} token detect/strip), plus hole detection + buildDSLPreview"
    - path: "src/pages/ReviewPage.tsx"
      provides: "handleSave refactored to finalize via draftToEntry (single entry-construction source)"
      contains: "draftToEntry"
  key_links:
    - from: "src/pages/ReviewPage.tsx"
      to: "src/services/captureService.ts"
      via: "draftToEntry(formDraft, type, domain)"
      pattern: "draftToEntry\\("
    - from: "src/services/captureService.ts"
      to: "src/config/entryFields.ts"
      via: "POSITIONAL_SCHEMA hole comparison"
      pattern: "POSITIONAL_SCHEMA\\[type\\]"
---

<objective>
Build the pure capture-logic layer that every Phase-13 path depends on, and refactor
ReviewPage so direct-save and Review produce byte-identical entries.

Purpose: A single, tested source of truth for hole detection, the `{}` named-hole token,
DSL preview reconstruction, and draft→entry finalization. Without this, the direct-save path
(13-03) would duplicate ReviewPage's construction logic and drift (RESEARCH Pitfall 3).
Output: `src/services/captureService.ts` (+ tests) and a refactored `ReviewPage.handleSave`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tap-to-capture-flow/13-RESEARCH.md
@.planning/phases/13-tap-to-capture-flow/13-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. No exploration needed. -->

From src/services/extractMetadataFromUrl.ts (the ReviewDraft contract draftToEntry consumes):
```typescript
export interface ReviewDraft {
  sourceUrl?: string
  title?: string
  location?: string
  description?: string
  occurredAt?: number   // epoch ms
  amount?: number
  tags?: string[]
  metadata: Record<string, unknown>
}
```

From src/services/db.ts (target shape — read the file for the full LifeLogEntry type):
- `draftToEntry` returns `Omit<LifeLogEntry, 'id'>`; `EntryDomain`, `EntryType` are exported here.

From src/config/entryFields.ts:
```typescript
export const POSITIONAL_SCHEMA: Record<EntryType, string[]>  // e.g. expense -> ['amount','category']
export function buildReviewDraft(fields, values: Record<string,string>): ReviewDraft
export const ENTRY_FIELDS: Record<EntryType, ...>
```

Authoritative entry-construction contract — src/pages/ReviewPage.tsx handleSave (lines 95–134):
title `title.trim() || 'Untitled'`; recordedAt `Date.now()`; tags `string[]`; metadata `?? {}`;
syncedAt `null`; optional sourceUrl/location/description/amount/occurredAt only when truthy / not-NaN.
sourceUrl is gated by `isSafeUrl` (src/services/urlUtils.ts) BEFORE inclusion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: captureService.ts — hole detection, {} token, preview, draftToEntry</name>
  <files>src/services/captureService.ts, src/services/captureService.test.ts</files>
  <read_first>
    - src/services/exportEntries.ts (lines 1–30) — pure-function module style: named exports only, no class, no default export; inject side effects as params (PATTERNS "Pure-function service")
    - src/config/entryFields.ts (POSITIONAL_SCHEMA lines ~105–113, buildReviewDraft body) — schema shape + draft mapping
    - src/pages/ReviewPage.tsx (lines 95–134) — the authoritative entry-construction contract draftToEntry mirrors
    - src/services/extractMetadataFromUrl.ts (lines 19–28) — ReviewDraft type
    - src/services/db.ts — LifeLogEntry, EntryDomain, EntryType
    - src/config/entryFields.test.ts (lines 1–5, 59–68) — pure-function test style analog (CAP per VALIDATION)
    - src/services/entriesRepository.test.tsx (lines 14–25) — factory-helper pattern (build makeReviewDraft)
    - .planning/phases/13-tap-to-capture-flow/13-RESEARCH.md (Key Technical Findings 1–4) — exact algorithms
  </read_first>
  <behavior>
    - HOLE_TOKEN === '{}' is exported and documented as the named-hole convention (CAP-04).
    - cleanValues({category:'food', merchant:'{}'}) === {category:'food'} (strips only '{}' entries; CAP-04).
    - detectHoles('expense', {category:'food'}) → positional: ['amount'], named: [], hasHoles: true (CAP-02).
    - detectHoles('expense', {amount:'5', category:'coffee'}) → hasHoles: false (CAP-01 zero-hole).
    - detectHoles('expense', {category:'food', merchant:'{}'}) → positional:['amount'], named:['merchant'] (CAP-04).
    - detectHoles('expense', {}) (bare template, no positional region) → positional:['amount','category'], hasHoles:true (RESEARCH Pitfall 2 — schema comparison, NOT warning strings).
    - Hole ordering is deterministic: positional holes in POSITIONAL_SCHEMA order, then named holes in discovery order.
    - applyFills(baseValues, fills) returns the merged record ({...base, ...fills}); used by BOTH preview and save.
    - buildDSLPreview('expense', {amount:'12', category:'food'}) === 'expense 12:food'; named params render as ?k=v (quoted when value contains space/:/,/?).
    - draftToEntry(makeReviewDraft({title:' x ', amount:12.5, tags:['a']}), 'expense', 'expenditures') yields {domain:'expenditures', type:'expense', title:'x', amount:12.5, tags:['a'], metadata:{}, syncedAt:null, recordedAt: <number>} and OMITS amount/occurredAt when null or NaN, omits location/description/sourceUrl when falsy.
    - draftToEntry preserves draft.sourceUrl when truthy (so the refactored ReviewPage keeps its URL); shortcut drafts (no sourceUrl) omit it.
  </behavior>
  <action>
    Create src/services/captureService.ts as a pure-function module (named exports, no default, no class — mirror exportEntries.ts). Export const HOLE_TOKEN = '{}' with a doc comment naming it the CAP-04 named-hole convention. Implement and export:
    - cleanValues(values): filter out entries whose value === HOLE_TOKEN.
    - detectHoles(type, rawValues): compute namedHoles by filtering rawValues entries equal to HOLE_TOKEN; compute cleanVals via cleanValues; positionalHoles = POSITIONAL_SCHEMA[type].filter(k => !cleanVals[k]); return HoleMap { positional, named, hasHoles } where hasHoles = positional.length + named.length > 0. Export the HoleMap interface. Use the POSITIONAL_SCHEMA comparison EXCLUSIVELY — do NOT inspect parser warning strings (RESEARCH Pitfall 2).
    - applyFills(baseValues, fills): return { ...baseValues, ...fills }.
    - buildDSLPreview(type, mergedValues): join POSITIONAL_SCHEMA[type] mapped to (mergedValues[k] ?? '') with ':'; append named entries (keys not in schema, truthy value) as ?k=v joined by ',', quoting any value matching /[ :,?]/ ; return `${type} ${positionals}${namedStr}`.
    - draftToEntry(draft, type, domain): build Omit<LifeLogEntry,'id'> mirroring ReviewPage lines 109–123 EXACTLY — title draft.title?.trim() || 'Untitled', recordedAt Date.now(), tags draft.tags ?? [], metadata draft.metadata ?? {}, syncedAt null; spread-include sourceUrl/location/description when truthy and amount/occurredAt when != null and !Number.isNaN. Add a comment cross-referencing ReviewPage.handleSave as the shared contract.
    Write src/services/captureService.test.ts FIRST (RED) covering every bullet in <behavior>; add makeReviewDraft(overrides?) factory helper. No db reset / no MemoryRouter (pure functions). Implements CAP-01 (draftToEntry) and CAP-04 ({} token); buildDSLPreview/detectHoles serve CAP-02 consumed by 13-02/13-03.
    NO new runtime dependencies. NO eval. Do NOT call parseDSL inside captureService — callers pass already-parsed values (PATTERNS note).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/services/captureService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/services/captureService.test.ts exists and is RED before implementation, GREEN after.
    - `grep -c "export" src/services/captureService.ts` shows named exports for HOLE_TOKEN, cleanValues, detectHoles, applyFills, buildDSLPreview, draftToEntry, HoleMap; `grep -c "export default" src/services/captureService.ts` returns 0.
    - Source assertion: detectHoles uses `POSITIONAL_SCHEMA[type].filter` and NOT any `warnings` reference — `grep -v '^[[:space:]]*//' src/services/captureService.ts | grep -c "warning"` returns 0.
    - `pnpm exec vitest run src/services/captureService.test.ts` passes with tests covering zero-hole, positional hole, bare-template hole, named-{} hole+strip, multi-hole ordering, buildDSLPreview line equality, and draftToEntry optional-field omission.
    - `pnpm exec tsc -b` is clean.
  </acceptance_criteria>
  <done>captureService.ts exports the six functions + HoleMap; all behavior tests green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor ReviewPage.handleSave to finalize via draftToEntry</name>
  <files>src/pages/ReviewPage.tsx</files>
  <read_first>
    - src/pages/ReviewPage.tsx (lines 95–134) — current handleSave; the construction block to replace
    - src/pages/ReviewPage.test.tsx — existing ReviewPage behavior tests that MUST still pass
    - src/services/captureService.ts (from Task 1) — draftToEntry signature
    - src/services/urlUtils.ts (isSafeUrl) — the gate that MUST stay in front of sourceUrl
  </read_first>
  <behavior>
    - A save with the same form values produces an entry whose shape/fields are identical to draftToEntry's output (no divergence — RESEARCH Pitfall 3).
    - sourceUrl is still gated by isSafeUrl: an unsafe scheme (e.g. javascript:) is NOT persisted; a safe https URL is persisted.
    - All existing ReviewPage.test.tsx assertions (title default, tags split, amount/date parsing, in-flight guard, save-failure error) continue to pass unchanged in observable behavior.
  </behavior>
  <action>
    In ReviewPage.handleSave, keep the existing pre-processing (safeSourceUrl via isSafeUrl, parsedAmount via parseFloat, parsedDate via Date.parse, parsedTags split). Then assemble a ReviewDraft from form state — title, location_ (→location), description, the isSafe-validated sourceUrl (only when safe, else undefined), amount = !isNaN(parsedAmount) ? parsedAmount : undefined, occurredAt = !isNaN(parsedDate) ? parsedDate : undefined, tags = parsedTags, metadata = initialDraft.metadata ?? {} — and call entriesRepository.create(draftToEntry(formDraft, type as EntryType, domain as EntryDomain)). Remove the inline entry object literal so draftToEntry is the single construction site. Preserve the savingRef in-flight guard, try/catch, saveError, navigate(`/d/${domain}`), and isSaving handling exactly. Import draftToEntry from ../services/captureService.
    Note: isSafeUrl stays at the ReviewPage boundary (applied before the URL reaches the draft); draftToEntry then passes through draft.sourceUrl when truthy. This preserves the existing security behavior without moving the gate (T-13-04).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/pages/ReviewPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "draftToEntry" src/pages/ReviewPage.tsx` >= 1 and the inline `const entry = {` construction literal in handleSave is removed (`grep -c "domain: domain as EntryDomain" src/pages/ReviewPage.tsx` returns 0).
    - Source assertion: `grep -c "isSafeUrl" src/pages/ReviewPage.tsx` >= 1 (gate preserved).
    - `pnpm exec vitest run src/pages/ReviewPage.test.tsx` passes with no test modifications required by behavior change.
    - `pnpm exec tsc -b` is clean.
  </acceptance_criteria>
  <done>ReviewPage builds entries through draftToEntry; isSafeUrl gate intact; existing ReviewPage tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shortcut `dslTemplate` → captureService | template string is parsed (by callers) into values; values flow as DATA into draftToEntry |
| form input → ReviewPage handleSave | user-entered sourceUrl crosses into persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | dslTemplate handling | mitigate | captureService never calls eval; it operates on already-parsed `values` only (`grep` confirms no eval) |
| T-13-02 | Injection | `{}` named-hole token | mitigate | cleanValues strips every `{}` entry before buildReviewDraft so it persists as DATA, never as a value (RESEARCH Pitfall 1) |
| T-13-04 | Tampering | sourceUrl persistence in ReviewPage | mitigate | isSafeUrl gate retained ahead of the draft; unsafe schemes are dropped before draftToEntry |
| T-13-SC | Tampering | package installs | accept | No package installs this phase — RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `pnpm exec vitest run src/services/captureService.test.ts src/pages/ReviewPage.test.tsx` green.
- `pnpm exec tsc -b` clean.
- captureService exports are pure (no IndexedDB, no router) and reusable by 13-02/13-03.
</verification>

<success_criteria>
- CAP-01: draftToEntry produces a correct, ReviewPage-identical entry shape (unit-proven).
- CAP-04: `{}` token detected as a named hole and stripped before any draft is built (unit-proven).
- detectHoles + buildDSLPreview + applyFills available and tested for downstream consumption.
- ReviewPage now finalizes via draftToEntry with the isSafeUrl gate preserved.
</success_criteria>

<output>
Create `.planning/phases/13-tap-to-capture-flow/13-01-SUMMARY.md` when done.
</output>
