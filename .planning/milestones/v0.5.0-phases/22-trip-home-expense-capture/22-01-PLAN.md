---
phase: 22-trip-home-expense-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/money.ts
  - src/config/money.test.ts
  - src/config/expenseCategories.ts
autonomous: true
requirements: [HOME-03, EXP-03]
must_haves:
  truths:
    - "formatUSD renders a number as a 2-decimal USD string, float-safe (10.10 + 5.20 -> $15.30)"
    - "EXPENSE_CATEGORIES is the 8 ordered categories, importable without pulling in any React component"
  artifacts:
    - path: "src/config/money.ts"
      provides: "formatUSD currency formatter"
      exports: ["formatUSD"]
    - path: "src/config/money.test.ts"
      provides: "float-safety unit tests for formatUSD"
    - path: "src/config/expenseCategories.ts"
      provides: "EXPENSE_CATEGORIES constant + ExpenseCategory type"
      exports: ["EXPENSE_CATEGORIES", "ExpenseCategory"]
  key_links:
    - from: "src/components/dashboard/ExpenseSheet.tsx"
      to: "src/config/expenseCategories.ts"
      via: "import EXPENSE_CATEGORIES (Plan 22-02)"
      pattern: "EXPENSE_CATEGORIES"
    - from: "src/pages/TripHomePage.tsx"
      to: "src/config/money.ts"
      via: "import formatUSD (Plan 22-03)"
      pattern: "formatUSD"
---

<objective>
Create the two foundational, dependency-free building blocks the rest of Phase 22
imports: a float-safe `formatUSD` currency formatter and the single-source-of-truth
`EXPENSE_CATEGORIES` constant (the 8 ordered expense categories).

Purpose: `formatUSD` backs every money display (HOME-03 running total + recent-entry
amounts); `EXPENSE_CATEGORIES` backs the ExpenseSheet category grid (EXP-03) AND is
reused by Phase 24's category-grouped report — so it lives in a plain config module,
not inside a React component, to keep it import-light.
Output: `src/config/money.ts`, `src/config/money.test.ts`, `src/config/expenseCategories.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-trip-home-expense-capture/22-CONTEXT.md
@.planning/phases/22-trip-home-expense-capture/22-PATTERNS.md

<interfaces>
<!-- Consumers downstream (executors do NOT need to read them now): -->
<!-- Plan 22-02 ExpenseSheet imports EXPENSE_CATEGORIES from src/config/expenseCategories.ts -->
<!-- Plan 22-03 TripHomePage imports formatUSD from src/config/money.ts -->
<!-- tripService.tripExpenseTotal(entries: LifeLogEntry[]) -> number  (raw float; always wrap in formatUSD at display) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatUSD currency formatter + float-safety tests</name>
  <files>src/config/money.ts, src/config/money.test.ts</files>
  <read_first>
    - .planning/phases/22-trip-home-expense-capture/22-PATTERNS.md section "src/config/money.ts" (lines ~434-470) — the exact Intl.NumberFormat pattern and rounding guard.
    - src/config/entryFields.ts (top 5 lines) — only to mirror the file-header import-comment style used across src/config.
  </read_first>
  <behavior>
    - formatUSD(123.4) === "$123.40"
    - formatUSD(0) === "$0.00"
    - formatUSD(1234.56) === "$1,234.56"  (thousands separator)
    - formatUSD(10.10 + 5.20) === "$15.30"  (float-safety: 15.299999999999999 rounds, not "$15.29")
    - formatUSD(15.299999999999999) === "$15.30"
  </behavior>
  <action>
    Create src/config/money.ts exporting a single named function `formatUSD(n: number): string`.
    Build ONE module-level `Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD',
    minimumFractionDigits: 2, maximumFractionDigits: 2 })` instance (construction is cheap, reuse is
    free) and have formatUSD return that formatter applied to `Math.round(n * 100) / 100` — the
    pre-round is the float-safety guard against IEEE-754 display artifacts (PITFALLS Pitfall 4). No
    default export, named export only, mirroring the src/config module style. Then create
    src/config/money.test.ts (plain .ts — pure function, no JSX, no Dexie) with a vitest describe block
    asserting each case in the behavior block above. Do NOT use fake timers or fake-indexeddb here;
    this test is fully synchronous and pure.
  </action>
  <verify>
    <automated>npx vitest run src/config/money.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/config/money.test.ts` passes with the 5 behavior cases above green.
    - `grep -c 'export function formatUSD' src/config/money.ts` returns 1.
    - `grep -c 'Math.round' src/config/money.ts` returns at least 1 (rounding guard present).
    - money.ts contains NO `import` from React, '../services', or '../components' (it is dependency-free).
    - `npx tsc -b` exits clean.
  </acceptance_criteria>
  <done>formatUSD exists, is float-safe, and its unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: EXPENSE_CATEGORIES shared constant + type</name>
  <files>src/config/expenseCategories.ts</files>
  <read_first>
    - .planning/phases/22-trip-home-expense-capture/22-PATTERNS.md section "Constants" (lines ~174-200) — the exact 8 ordered category strings and the `as const` / `ExpenseCategory` type pattern.
    - .planning/phases/22-trip-home-expense-capture/22-CONTEXT.md "Expense categories (exact, ordered)" — the authoritative order.
  </read_first>
  <action>
    Create src/config/expenseCategories.ts. Export `EXPENSE_CATEGORIES` as a readonly tuple
    (`as const`) of exactly these 8 strings IN THIS ORDER: Hotel, Rental Car, Flight, Taxi/Uber,
    Food, Gas, Parking, Other. Also export `type ExpenseCategory = (typeof EXPENSE_CATEGORIES)[number]`.
    This is the single source of truth — Plan 22-02's ExpenseSheet and Phase 24's report both import
    it from here. NOTE: this intentionally lives in src/config (not co-located inside ExpenseSheet as
    the PATTERNS.md sketch shows) so it can be imported without pulling in a React component — the
    phase structure decision overrides the PATTERNS co-location sketch. Named exports only, no default.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'Hotel'\|'Rental Car'\|'Flight'\|'Taxi/Uber'\|'Food'\|'Gas'\|'Parking'\|'Other'" src/config/expenseCategories.ts` confirms all 8 strings present.
    - `grep -c 'as const' src/config/expenseCategories.ts` returns at least 1.
    - `grep -c 'export type ExpenseCategory' src/config/expenseCategories.ts` returns 1.
    - expenseCategories.ts imports NO React and NO '../components' (component-free).
    - `npx tsc -b` exits clean.
  </acceptance_criteria>
  <done>EXPENSE_CATEGORIES + ExpenseCategory exported from a component-free config module in the exact locked order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| number -> display string | formatUSD receives an in-memory float and produces a display string; no external/untrusted input crosses here in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information disclosure | formatUSD float artifacts | mitigate | Pre-round `Math.round(n*100)/100` so no misleading sub-cent value is ever shown (PITFALLS Pitfall 4) |
| T-22-02 | Tampering | npm/pip installs | accept | Zero new dependencies in this phase; only Intl (platform built-in) used — no supply-chain surface |
</threat_model>

<verification>
- `npx vitest run src/config/money.test.ts` — green.
- `npx tsc -b` — clean.
- Both new config modules are dependency-free (no React/service/component imports).
</verification>

<success_criteria>
formatUSD is float-safe and unit-tested; EXPENSE_CATEGORIES + ExpenseCategory exist as the single
source of truth in a component-free config module, ready for import by 22-02 and 22-03.
</success_criteria>

<output>
Create `.planning/phases/22-trip-home-expense-capture/22-01-SUMMARY.md` when done.
</output>
