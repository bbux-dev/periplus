---
phase: 24-previous-trips-trip-detail-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/tripService.ts
  - src/services/tripService.test.tsx
  - src/pages/PreviousTripsPage.tsx
  - src/pages/PreviousTripsPage.test.tsx
  - src/App.tsx
autonomous: true
requirements: [PREV-01, PREV-02, PREV-03, PREV-04]
user_setup: []

must_haves:
  truths:
    - "User sees all trips listed newest-first"
    - "Each trip row shows name, date range, currency-formatted total expense, and activity count"
    - "Tapping a trip row navigates to /trips/<trip UUID>"
    - "All per-trip stats come from ONE db.entries.toArray() pass (no per-trip filter loop)"
  artifacts:
    - path: "src/services/tripService.ts"
      provides: "summarizeTrips(allEntries) pure single-pass helper + TripSummary interface"
      contains: "export function summarizeTrips"
    - path: "src/pages/PreviousTripsPage.tsx"
      provides: "Previous Trips list page at /trips"
      contains: "summarizeTrips"
    - path: "src/App.tsx"
      provides: "/trips route wired to PreviousTripsPage"
      contains: "PreviousTripsPage"
  key_links:
    - from: "src/pages/PreviousTripsPage.tsx"
      to: "db.entries.toArray"
      via: "single useLiveQuery -> summarizeTrips"
      pattern: "db\\.entries\\.toArray\\(\\)\\.then\\(summarizeTrips\\)"
    - from: "src/pages/PreviousTripsPage.tsx"
      to: "/trips/:tripId"
      via: "navigate by trip.id (UUID)"
      pattern: "navigate\\(`/trips/\\$\\{trip\\.id\\}`\\)"
---

<objective>
Deliver the Previous Trips list: a single-pass `summarizeTrips` helper in tripService.ts and a
`PreviousTripsPage` at `/trips` that renders every trip newest-first with name, date range,
float-safe total expense, and activity count, each row drilling into `/trips/<trip.id>` by UUID.

Purpose: PREV-01..04 — the read entry point for v0.5.0 trip history, with the N+1 performance
pitfall (Pitfall 6) designed out at the helper-signature level.
Output: `summarizeTrips` + tests, `PreviousTripsPage` + tests, `/trips` route wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-previous-trips-trip-detail-report/24-CONTEXT.md
@.planning/phases/24-previous-trips-trip-detail-report/24-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Existing contracts the executor builds against — no codebase exploration needed. -->

From src/services/tripService.ts (already present in the same file you are editing):
  export function tripExpenseTotal(entries: LifeLogEntry[]): number   // raw float; round at display
  export function tripExpensesByCategory(entries: LifeLogEntry[]): Map<string, number>
  export function tripDateRange(entries: LifeLogEntry[]): { start: number; end: number } | null
  export function tripActivityCount(entries: LifeLogEntry[]): number
  export async function createAndActivateTrip(name: string): Promise<LifeLogEntry>   // test factory
  // db, LifeLogEntry already imported in this file

From src/config/money.ts:
  export function formatUSD(n: number): string   // guards NaN, rounds Math.round(n*100)/100

From src/services/db.ts:
  LifeLogEntry has: id, domain, type, title, recordedAt, tags, metadata, syncedAt,
                    optional amount, occurredAt, description, location
  type 'trip' and 'activity' are already in the EntryType union.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add summarizeTrips single-pass helper + tests</name>
  <files>src/services/tripService.ts, src/services/tripService.test.tsx</files>
  <read_first>
    - src/services/tripService.ts (the existing pure helpers tripExpenseTotal / tripExpensesByCategory / tripDateRange / tripActivityCount — reuse them, do not reimplement)
    - src/services/tripService.test.tsx (the pure-helper test section + the makeEntryData factory + the Dexie reset beforeEach/afterEach block)
    - 24-PATTERNS.md section "summarizeTrips helper" and "summarizeTrips tests"
    - PITFALLS.md Pitfall 4 (float money) and Pitfall 6 (single-pass / no N+1)
  </read_first>
  <behavior>
    - Two trips, newest-first by trip.recordedAt: summaries[0] is the higher recordedAt trip
    - A trip with one expense (amount 42) and one activity: total === 42, activityCount === 1
    - An empty trip: total === 0, dateRange === null, activityCount === 0
    - Float-safe: amounts 10.1 + 5.2 -> formatUSD(summary.total) === '$15.30'
    - summarizeTrips([]) === [] (the signature takes LifeLogEntry[], so it CANNOT call listTripEntries — contract-enforced single pass)
    - Child entries are grouped by metadata.tripId; entries whose metadata.tripId is missing/non-string are ignored
  </behavior>
  <action>
    In src/services/tripService.ts add an exported `TripSummary` interface with fields `trip: LifeLogEntry`, `entries: LifeLogEntry[]`, `total: number` (raw float from tripExpenseTotal), `dateRange: { start: number; end: number } | null`, and `activityCount: number`. Add an exported PURE function `summarizeTrips(allEntries: LifeLogEntry[]): TripSummary[]` that: (1) partitions allEntries into trip records (`e.type === 'trip'`) and child entries (`e.type !== 'trip'`); (2) groups child entries into a `Map<string, LifeLogEntry[]>` keyed by `metadata.tripId` in ONE for-loop, skipping entries whose `metadata.tripId` is not a string (mirror the Map accumulator pattern from entriesRepository.listDistinctValues); (3) maps each trip record to a TripSummary, looking up its grouped slice (default `[]`) and computing `total` via tripExpenseTotal, `dateRange` via tripDateRange, `activityCount` via tripActivityCount over that slice; (4) returns the summaries sorted newest-first by `trip.recordedAt` descending. Take NO Dexie handle and make ZERO db calls — the helper only receives the already-loaded array. No new imports are required (db, LifeLogEntry, and all pure helpers are already in the file). Import formatUSD into the test file from ../config/money for the float-safe assertion. Add a `describe('summarizeTrips', ...)` block to tripService.test.tsx covering every behavior above, reusing the existing makeEntryData factory and constructing LifeLogEntry objects with explicit `id` fields (the pure section needs no Dexie, so no fake timers required there).
  </action>
  <acceptance_criteria>
    - `grep -n 'export function summarizeTrips' src/services/tripService.ts` returns a match
    - `grep -n 'export interface TripSummary' src/services/tripService.ts` returns a match
    - summarizeTrips makes no Dexie call — verify SCOPED to the function body (a whole-file grep is meaningless since listTrips/listTripEntries legitimately use db/toArray): `awk '/export function summarizeTrips/,/^}/' src/services/tripService.ts | grep -nE 'db\.|listTripEntries|toArray|await'` returns NOTHING. The pure signature `summarizeTrips(allEntries: LifeLogEntry[]): TripSummary[]` is the real contract — no db handle in scope.
    - `npx vitest run src/services/tripService.test.tsx` passes, including the new summarizeTrips block
    - `npx tsc -b` is clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/services/tripService.test.tsx && npx tsc -b</automated>
  </verify>
  <done>summarizeTrips + TripSummary exported, pure single-pass, all new tests green, tsc clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PreviousTripsPage + test, wire /trips route</name>
  <files>src/pages/PreviousTripsPage.tsx, src/pages/PreviousTripsPage.test.tsx, src/App.tsx</files>
  <read_first>
    - src/pages/TripHomePage.tsx (useLiveQuery "no default" loading skeleton + entry-row list render + formatUSD usage)
    - src/pages/TripHomePage.test.tsx (MemoryRouter + Routes test wrapper, act() Dexie-write pattern, fake-timers toFake:['Date'] usage, createAndActivateTrip factory)
    - src/App.tsx (the placeholder /trips route to replace, lines 26-28)
    - 24-PATTERNS.md sections "PreviousTripsPage" + "PreviousTripsPage.test" + "App.tsx Route Wiring"
    - PITFALLS.md Pitfall 6 (single-pass) and MEMORY note (fake timers toFake:['Date'])
  </read_first>
  <behavior>
    - Renders a neutral "Loading…" skeleton while the useLiveQuery result is undefined (Dexie opening)
    - Two trips created at different system times render newest-first (newer trip's listitem precedes older)
    - A trip with a $42.50 expense child shows '$42.50' via formatUSD
    - Each trip row is a keyboard-operable button (role="button", Enter/click) that navigates to /trips/<trip.id>
    - Empty state (no trips) renders without crashing (e.g. a short "No trips yet" message)
  </behavior>
  <action>
    Create `src/pages/PreviousTripsPage.tsx` exporting `PreviousTripsPage`. Use a single `useLiveQuery(() => db.entries.toArray().then(summarizeTrips), [])` with NO default value so `undefined` means "Dexie opening" — render the neutral loading skeleton (mirror TripHomePage's min-h-screen centered "Loading…" block) in that case. This is the ONLY entries read on the page — do not call listTripEntries or useTripEntries. Render the summaries as a `<ul>` of `<li>` rows; each row is `role="button"`, `tabIndex={0}`, with `onClick` and `onKeyDown` (Enter) calling `navigate(\`/trips/${trip.id}\`)` using the trip UUID (never the name). Each row shows `trip.title`, a formatted date range, and `formatUSD(total)` plus the activity count (e.g. "3 activities"). Add a local `formatDateRange(range)` helper that returns a placeholder string (such as "—") when range is null and otherwise formats start/end via `new Date(ms).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`, collapsing to a single date when start === end — never use toISOString. When summaries is an empty array, show a brief "No trips yet" message. Import useLiveQuery from dexie-react-hooks, useNavigate from react-router-dom, db from ../services/db, summarizeTrips from ../services/tripService, formatUSD from ../config/money. Then in src/App.tsx replace the placeholder `/trips` route element with `<PreviousTripsPage />` and add the import; leave the `/trips/:tripId` placeholder untouched (Plan 24-02 owns it). Create `src/pages/PreviousTripsPage.test.tsx` covering every behavior, using the Dexie reset beforeEach (db.delete + db.open), afterEach vi.useRealTimers, `vi.useFakeTimers({ toFake: ['Date'] })` + vi.setSystemTime for the newest-first ordering, await act for db.entries.add writes, and a MemoryRouter+Routes wrapper with a `/trips/:tripId` probe route to assert navigation.
  </action>
  <acceptance_criteria>
    - `grep -n 'db.entries.toArray().then(summarizeTrips)' src/pages/PreviousTripsPage.tsx` returns a match (single-pass)
    - PreviousTripsPage performs no N+1: `grep -nE 'listTripEntries|useTripEntries' src/pages/PreviousTripsPage.tsx` returns NOTHING
    - drill-in uses UUID: `grep -n 'navigate(`/trips/${trip.id}`)' src/pages/PreviousTripsPage.tsx` returns a match
    - no UTC date helper: `grep -n 'toISOString' src/pages/PreviousTripsPage.tsx` returns NOTHING
    - `grep -n 'PreviousTripsPage' src/App.tsx` returns both an import and the route element
    - `npx vitest run src/pages/PreviousTripsPage.test.tsx` passes; full `npx vitest run` stays green; `npx tsc -b` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/PreviousTripsPage.test.tsx && npx tsc -b</automated>
  </verify>
  <done>PreviousTripsPage renders newest-first single-pass rows, UUID drill-in, /trips wired, tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered trip/entry data → DOM | Titles, categories, amounts stored locally and rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | money display in trip rows | mitigate | All totals rendered via formatUSD (Math.round(x*100)/100) — no raw float to DOM (Pitfall 4) |
| T-24-02 | Information disclosure | trip-row title/category text | accept | React auto-escapes text children; data is local, single-user, no PII boundary |
| T-24-03 | Denial of service | trip list derivation | mitigate | Single db.entries.toArray() pass; no per-trip N+1 filter loop (Pitfall 6) |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — reuses existing deps only |
</threat_model>

<verification>
- `npx vitest run` is fully green
- `npx tsc -b` is clean
- PreviousTripsPage uses exactly one db read and drills in by UUID
</verification>

<success_criteria>
- summarizeTrips is a pure single-pass helper with passing unit tests
- PreviousTripsPage lists trips newest-first with name, date range, float-safe total, activity count
- Rows navigate to /trips/<trip.id> by UUID; /trips route wired in App.tsx
- PREV-01..04 satisfied
</success_criteria>

<output>
Create `.planning/phases/24-previous-trips-trip-detail-report/24-01-SUMMARY.md` when done
</output>
