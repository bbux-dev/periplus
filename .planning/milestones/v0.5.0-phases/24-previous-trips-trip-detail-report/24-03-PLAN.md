---
phase: 24-previous-trips-trip-detail-report
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - src/components/EditEntryModal.tsx
  - src/components/EditEntryModal.test.tsx
  - src/pages/TripDetailPage.tsx
  - src/pages/TripDetailPage.test.tsx
autonomous: true
requirements: [RPT-05, RPT-06]
user_setup: []

must_haves:
  truths:
    - "User can edit any timeline entry from Trip Detail via a form seeded from the entry"
    - "Editing preserves metadata.tripId / mode / modeLabel (merge, not replace)"
    - "User can delete a timeline entry only after a confirmation prompt"
    - "Report subtotals, grand total, and timeline recompute reactively after edit or delete"
  artifacts:
    - path: "src/components/EditEntryModal.tsx"
      provides: "Edit form modal reusing ENTRY_FIELDS + formValuesFromEntry + buildEntryUpdate"
      contains: "buildEntryUpdate"
    - path: "src/pages/TripDetailPage.tsx"
      provides: "Edit + Delete controls on each timeline row"
      contains: "EditEntryModal"
  key_links:
    - from: "src/components/EditEntryModal.tsx"
      to: "entriesRepository.update"
      via: "buildEntryUpdate(fields, entry, formValues, {}) -> update(entry.id, changes)"
      pattern: "entriesRepository\\.update\\("
    - from: "src/pages/TripDetailPage.tsx"
      to: "entriesRepository.delete"
      via: "confirm() gate before delete"
      pattern: "entriesRepository\\.delete\\("
---

<objective>
Add inline edit + delete to the Trip Detail timeline: an `EditEntryModal` that seeds a form from
the selected entry (reusing ENTRY_FIELDS / formValuesFromEntry / buildEntryUpdate over
entriesRepository.update), and per-row Edit/Delete controls on TripDetailPage where Delete is
confirmation-gated. All views recompute reactively via useLiveQuery after each mutation.

Purpose: RPT-05, RPT-06 — entry-level editing/deletion (NO Delete Trip), with merge-preserving
metadata and confirm-gated deletes.
Output: `EditEntryModal` + tests, TripDetailPage timeline wired with Edit/Delete + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-previous-trips-trip-detail-report/24-CONTEXT.md
@.planning/phases/24-previous-trips-trip-detail-report/24-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Existing contracts the executor builds against. ACTUAL signatures verified from source. -->

From src/config/entryFields.ts:
  export const ENTRY_FIELDS: Record<EntryType, FieldDescriptor[]>
  export function formValuesFromEntry(fields: FieldDescriptor[], entry: LifeLogEntry): Record<string, string>
  export function buildEntryUpdate(
    fields: FieldDescriptor[],
    entry: LifeLogEntry,
    formValues: Record<string, string>,
    extraMetadata: Record<string, string>,
  ): Partial<Omit<LifeLogEntry, 'id'>>
  // buildEntryUpdate MERGES metadata onto a copy of entry.metadata: unknown keys and
  // mode/modeLabel/tripId survive untouched unless edited; recordedAt/syncedAt/domain/type
  // are NEVER written. Pass extraMetadata = {} for the trip edit flow.

From src/services/entriesRepository.ts:
  entriesRepository.update(id: string, changes: Partial<Omit<LifeLogEntry,'id'>>): Promise<number>
  entriesRepository.delete(id: string): Promise<void>
  entriesRepository.get(id: string): Promise<LifeLogEntry | undefined>   // for tests

From src/components/ui:
  Button (variant: 'primary' | 'secondary', size?), FormField (id, label, required, value, onChange, placeholder)

From src/services/tripService.ts:
  useTripEntries(tripId) is reactive (useLiveQuery) — TripDetailPage re-renders after update/delete with no manual refresh.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build EditEntryModal (seed + merge-preserving save + confirm delete) + test</name>
  <files>src/components/EditEntryModal.tsx, src/components/EditEntryModal.test.tsx</files>
  <read_first>
    - src/config/entryFields.ts (ENTRY_FIELDS, formValuesFromEntry, buildEntryUpdate — note the 4-arg signature: fields, entry, formValues, extraMetadata)
    - src/services/entriesRepository.ts (update / delete / get)
    - src/components/dashboard/ExpenseSheet.tsx (modal shell: backdrop + role="dialog" aria-modal + Escape + savingRef double-submit guard)
    - src/pages/ActivityFormPage.tsx (FormField-per-descriptor render)
    - 24-PATTERNS.md section "Edit form / modal" and shared patterns "Modal Shell" + "Double-Submit Guard"
    - 24-CONTEXT.md inline edit/delete decision (do NOT hand-roll the merge — buildEntryUpdate is authoritative)
  </read_first>
  <behavior>
    - Form is seeded from the entry: editing an expense pre-fills amount/category/etc from formValuesFromEntry
    - Saving an amount change persists via entriesRepository.update and calls onClose
    - After save, metadata.tripId, metadata.mode, and metadata.modeLabel survive unchanged (merge-preserve)
    - recordedAt / domain / type are NOT changed by an edit
    - Delete triggers window.confirm; if confirmed, entriesRepository.delete is called then onClose; if cancelled, neither happens
    - Double save clicks call update at most once (savingRef guard)
  </behavior>
  <action>
    Create `src/components/EditEntryModal.tsx` exporting `EditEntryModal` with props `{ entry: LifeLogEntry; onClose: () => void }`. Compute `const fields = ENTRY_FIELDS[entry.type]` and seed `useState(() => formValuesFromEntry(fields, entry))` for the form values record. Render one FormField per descriptor (id `edit-<key>`, label, required, value from formValues, onChange updating that key, placeholder from the descriptor) inside a modal shell mirroring ExpenseSheet: a fixed backdrop that calls onClose on click, plus a `role="dialog"` `aria-modal="true"` `aria-label={\`Edit ${entry.type}\`}` container with `tabIndex={-1}` and an Escape-to-close onKeyDown. Add a `handleSave` guarded by a synchronous `savingRef` (return early if already saving; mirror the WR-02 pattern) that calls `const changes = buildEntryUpdate(fields, entry, formValues, {})` then `await entriesRepository.update(entry.id, changes)` and `onClose()`; surface a generic error message on throw. Pass `{}` as extraMetadata so only known fields change and metadata is merge-preserved — do NOT hand-roll any metadata merge. Add a `handleDelete` that calls `window.confirm('Delete this entry? This cannot be undone.')` and returns early if false, otherwise `await entriesRepository.delete(entry.id)` then `onClose()`. Provide Cancel (onClose), Save (primary, disabled while saving), and Delete (secondary) buttons. Import useState/useRef from react, ENTRY_FIELDS/formValuesFromEntry/buildEntryUpdate from ../config/entryFields, entriesRepository from ../services/entriesRepository, FormField + Button from ./ui, and the LifeLogEntry type from ../services/db. Create `src/components/EditEntryModal.test.tsx` with the Dexie reset beforeEach (db.delete + db.open), afterEach vi.useRealTimers, using entriesRepository.create to seed an expense whose metadata includes tripId/mode/modeLabel; cover: seeded form value, save persists amount change while preserving metadata.tripId/mode/modeLabel (read back via entriesRepository.get), confirm-gated delete (spy window.confirm true vs false), and the double-submit guard.
  </action>
  <acceptance_criteria>
    - `grep -n 'buildEntryUpdate(fields, entry, formValues, {})' src/components/EditEntryModal.tsx` matches (authoritative merge, extraMetadata empty)
    - `grep -n 'entriesRepository.update' src/components/EditEntryModal.tsx` and `grep -n 'entriesRepository.delete' src/components/EditEntryModal.tsx` both match
    - delete is confirm-gated: `grep -n 'confirm(' src/components/EditEntryModal.tsx` matches and precedes the delete call
    - no hand-rolled merge: `grep -nE 'metadata:\s*\{|\.\.\.entry.metadata' src/components/EditEntryModal.tsx` returns NOTHING (merge lives only in buildEntryUpdate)
    - a11y shell present: `grep -n 'aria-modal' src/components/EditEntryModal.tsx` matches
    - test asserts metadata.tripId/mode/modeLabel survive an amount edit
    - `npx vitest run src/components/EditEntryModal.test.tsx` passes; `npx tsc -b` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/EditEntryModal.test.tsx && npx tsc -b</automated>
  </verify>
  <done>EditEntryModal seeds form, saves via buildEntryUpdate merge, confirm-gated delete; tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Edit/Delete into TripDetailPage timeline + reactive-recompute test</name>
  <files>src/pages/TripDetailPage.tsx, src/pages/TripDetailPage.test.tsx</files>
  <read_first>
    - src/pages/TripDetailPage.tsx (the timeline rows created in Plan 24-02 — attach controls without disturbing report/timeline derivation)
    - src/components/EditEntryModal.tsx (the modal built in Task 1 — props { entry, onClose })
    - src/services/entriesRepository.ts (delete signature)
    - 24-CONTEXT.md inline edit/delete decision (reactive via useLiveQuery; entry-level only, NO Delete Trip)
    - 24-PATTERNS.md "TripDetailPage" inline-delete handler + "Delete confirm + reactive recompute test"
  </read_first>
  <behavior>
    - Each timeline row has an Edit control that opens EditEntryModal for that entry
    - Each timeline row has a Delete control that confirms then deletes via entriesRepository.delete
    - After an edit that changes an amount, the report subtotal + grand total update reactively (no manual refresh)
    - After a delete, the entry disappears and report/timeline recompute reactively
    - No "Delete Trip" control exists anywhere on the page
  </behavior>
  <action>
    In `src/pages/TripDetailPage.tsx` add a `const [editingEntry, setEditingEntry] = useState<LifeLogEntry | null>(null)` and give each timeline row an Edit button (`onClick={() => setEditingEntry(e)}`) and a Delete button (`onClick={() => void handleDelete(e.id)}`). Implement `handleDelete(id)` that calls `window.confirm('Delete this entry? This cannot be undone.')`, returns early if false, otherwise `await entriesRepository.delete(id)` — do not refetch; useTripEntries is reactive so the report and timeline recompute automatically. Render `<EditEntryModal entry={editingEntry} onClose={() => setEditingEntry(null)} />` when editingEntry is non-null (modal closes itself after save/delete, and the reactive query refreshes the views). Keep the existing report + timeline derivation from 24-02 intact; only add controls + modal mount. Add imports for useState (if not already), EditEntryModal from ../components/EditEntryModal, and entriesRepository from ../services/entriesRepository. Do NOT add any trip-level delete. Extend `src/pages/TripDetailPage.test.tsx` with: a delete test (spy window.confirm -> true, click Delete, assert the amount text disappears via waitFor — proves reactive recompute) and an edit test (open the modal via Edit, change the amount field, click Save, assert the new subtotal/grand total appears reactively). Reuse the existing MemoryRouter+Routes wrapper and Dexie reset/fake-timer patterns.
  </action>
  <acceptance_criteria>
    - `grep -n 'EditEntryModal' src/pages/TripDetailPage.tsx` matches (import + mount)
    - `grep -n 'entriesRepository.delete' src/pages/TripDetailPage.tsx` matches and is confirm-gated (`grep -n 'confirm(' src/pages/TripDetailPage.tsx` matches)
    - no Delete Trip: `grep -niE 'delete\s*trip|deleteTrip|removeTrip' src/pages/TripDetailPage.tsx` returns NOTHING
    - report derivation unchanged: TripDetailPage still renders `<ExpenseReport` and still uses useTripEntries (`grep -n 'ExpenseReport' src/pages/TripDetailPage.tsx` and `grep -n 'useTripEntries' src/pages/TripDetailPage.tsx` both match)
    - delete + edit reactive-recompute tests present and passing
    - `npx vitest run src/pages/TripDetailPage.test.tsx` passes; full `npx vitest run` green; `npx tsc -b` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/TripDetailPage.test.tsx && npx tsc -b</automated>
  </verify>
  <done>Timeline rows have working Edit/Delete; delete confirm-gated; report/timeline recompute reactively; no Delete Trip; tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edit form input → entry update | User-edited field values written back to a stored entry |
| delete control → permanent removal | Destructive mutation of local data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Tampering | metadata on edit | mitigate | buildEntryUpdate merges onto entry.metadata copy; tripId/mode/modeLabel survive; recordedAt/syncedAt/domain/type never written — covered by metadata-preservation test |
| T-24-08 | Repudiation/Destruction | entry delete | mitigate | window.confirm gate before entriesRepository.delete; no accidental deletes; entry-level only (no cascade) |
| T-24-09 | Tampering | edit-form numeric input | mitigate | Amounts parsed via buildReviewDraft (NaN skipped); displayed via formatUSD on recompute (Pitfall 4) |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — reuses existing deps only |
</threat_model>

<verification>
- `npx vitest run` fully green; `npx tsc -b` clean
- Edit preserves metadata.tripId/mode/modeLabel; delete is confirm-gated
- Report subtotals/grand total/timeline recompute reactively after edit and delete
- No Delete Trip control exists
</verification>

<success_criteria>
- EditEntryModal: seeded form, merge-preserving save via buildEntryUpdate, confirm-gated delete
- TripDetailPage timeline rows expose Edit + Delete; views recompute reactively
- Entry-level only — no Delete Trip
- RPT-05, RPT-06 satisfied
</success_criteria>

<output>
Create `.planning/phases/24-previous-trips-trip-detail-report/24-03-SUMMARY.md` when done
</output>
