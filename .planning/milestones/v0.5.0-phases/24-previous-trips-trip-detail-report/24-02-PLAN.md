---
phase: 24-previous-trips-trip-detail-report
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/components/ExpenseReport.tsx
  - src/components/ExpenseReport.test.tsx
  - src/pages/TripDetailPage.tsx
  - src/pages/TripDetailPage.test.tsx
  - src/App.tsx
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04]
user_setup: []

must_haves:
  truths:
    - "Trip Detail shows a category-grouped expense report with per-category subtotals"
    - "Category rows expand to show their individual expense entries"
    - "Trip Detail shows a float-safe grand total equal to the sum of subtotals"
    - "Trip Detail shows a chronological timeline of all trip entries (expenses + activities)"
    - "Two identically-named trips resolve independently because detail loads by UUID"
  artifacts:
    - path: "src/components/ExpenseReport.tsx"
      provides: "Category-grouped expense report component with expandable rows + grand total"
      contains: "EXPENSE_CATEGORIES"
    - path: "src/pages/TripDetailPage.tsx"
      provides: "Trip detail page at /trips/:tripId (report + timeline)"
      contains: "useParams"
    - path: "src/App.tsx"
      provides: "/trips/:tripId route wired to TripDetailPage"
      contains: "TripDetailPage"
  key_links:
    - from: "src/pages/TripDetailPage.tsx"
      to: "useTripEntries(tripId)"
      via: "useParams tripId -> reactive query"
      pattern: "useTripEntries\\("
    - from: "src/components/ExpenseReport.tsx"
      to: "tripExpensesByCategory / tripExpenseTotal"
      via: "derive subtotals + grand total, render via formatUSD"
      pattern: "tripExpensesByCategory\\("
---

<objective>
Deliver Trip Detail: an `ExpenseReport` component that groups expenses by category (ordered by
EXPENSE_CATEGORIES, expandable to individual expenses, float-safe subtotals + grand total) and a
`TripDetailPage` at `/trips/:tripId` that loads the trip's entries by UUID, renders the report,
and shows a chronological timeline of all entries (expenses + activities).

Purpose: RPT-01..04 — the read/report surface for a single trip, with float-safe money (Pitfall 4)
and UUID-based isolation (Pitfall 5) baked into acceptance criteria.
Output: `ExpenseReport` + tests, `TripDetailPage` + tests, `/trips/:tripId` route wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-previous-trips-trip-detail-report/24-CONTEXT.md
@.planning/phases/24-previous-trips-trip-detail-report/24-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Existing contracts the executor builds against. -->

From src/services/tripService.ts:
  export function useTripEntries(tripId: string): LifeLogEntry[] | undefined  // tripId in dep array
  export function tripExpensesByCategory(entries: LifeLogEntry[]): Map<string, number>
  export function tripExpenseTotal(entries: LifeLogEntry[]): number  // raw float; round at display
  // non-string/missing metadata.category falls under the 'Uncategorized' bucket key

From src/config/expenseCategories.ts:
  export const EXPENSE_CATEGORIES = ['Hotel','Rental Car','Flight','Taxi/Uber','Food','Gas','Parking','Other'] as const

From src/config/money.ts:
  export function formatUSD(n: number): string

From src/services/db.ts:
  LifeLogEntry: id, domain, type, title, recordedAt, tags, metadata, syncedAt, amount?, occurredAt?, description?, location?
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ExpenseReport component + test</name>
  <files>src/components/ExpenseReport.tsx, src/components/ExpenseReport.test.tsx</files>
  <read_first>
    - src/services/tripService.ts (tripExpensesByCategory returns Map; non-string category -> 'Uncategorized'; tripExpenseTotal raw float)
    - src/config/expenseCategories.ts (EXPENSE_CATEGORIES canonical order)
    - src/config/money.ts (formatUSD rounds + guards NaN)
    - src/components/dashboard/ExpenseSheet.tsx (category-iteration / accordion render analog)
    - 24-PATTERNS.md section "ExpenseReport" (props interface, EXPENSE_CATEGORIES filter, expand toggle, Uncategorized bucket, grand-total footer)
    - PITFALLS.md Pitfall 4 (float money)
  </read_first>
  <behavior>
    - Given expenses Food $10.10 and Hotel $5.20, renders subtotal '$10.10' and '$5.20' and grand total '$15.30' (float-safe, not $15.2999...)
    - Categories render in EXPENSE_CATEGORIES order; categories with no expense entries are NOT rendered
    - An expense with no/blank category renders under an 'Uncategorized' bucket
    - A category row is a button with aria-expanded; clicking it reveals that category's individual expense entries (merchant or title + amount)
    - Grand total equals the sum of the displayed subtotals
  </behavior>
  <action>
    Create `src/components/ExpenseReport.tsx` exporting `ExpenseReport` with props `{ entries: LifeLogEntry[] }`. Internally derive `tripExpensesByCategory(entries)` (a Map) and `tripExpenseTotal(entries)` for the grand total — the component owns its derivation; callers pass raw entries. Build the visible category rows by filtering EXPENSE_CATEGORIES to those present in the map (canonical order, skip empties), and additionally render an 'Uncategorized' bucket row when the map has that key. Each category row is a `<button type="button">` with `aria-expanded`, showing the category name and `formatUSD(subtotal)`; track expanded categories in a `useState<Set<string>>` toggled per row. When expanded, render that category's individual expense entries (filter entries to `type === 'expense'` whose resolved category equals the row, resolving missing/non-string category to 'Uncategorized'), each showing the merchant (`metadata.merchant` when a string) else the title, plus `formatUSD(e.amount ?? 0)`. Render a grand-total footer row showing `formatUSD(grandTotal)`. EVERY monetary value goes through formatUSD — never render a raw summation. Import useState from react, tripExpensesByCategory + tripExpenseTotal from ../services/tripService, EXPENSE_CATEGORIES from ../config/expenseCategories, formatUSD from ../config/money, and the LifeLogEntry type from ../services/db. Create `src/components/ExpenseReport.test.tsx` (RTL, no Dexie needed — pass plain LifeLogEntry arrays) covering every behavior, including a userEvent click expanding a category to reveal its entries.
  </action>
  <acceptance_criteria>
    - `grep -n 'tripExpensesByCategory' src/components/ExpenseReport.tsx` and `grep -n 'tripExpenseTotal' src/components/ExpenseReport.tsx` both match
    - `grep -n 'EXPENSE_CATEGORIES' src/components/ExpenseReport.tsx` matches (canonical ordering source)
    - no raw float to DOM: every amount/subtotal/total render site is wrapped in formatUSD (review: `grep -nE 'amount|subtotal|grandTotal|total' src/components/ExpenseReport.tsx` shows each display use inside formatUSD(...))
    - test asserts grand total '$15.30' for inputs 10.10 + 5.20 (float-safe)
    - `npx vitest run src/components/ExpenseReport.test.tsx` passes; `npx tsc -b` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/ExpenseReport.test.tsx && npx tsc -b</automated>
  </verify>
  <done>ExpenseReport renders ordered, expandable, float-safe category report with grand total; tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TripDetailPage (report + timeline) + test, wire /trips/:tripId route</name>
  <files>src/pages/TripDetailPage.tsx, src/pages/TripDetailPage.test.tsx, src/App.tsx</files>
  <read_first>
    - src/pages/TripHomePage.tsx (useLiveQuery loading skeleton + entry-row render analog)
    - src/pages/ActivityFormPage.tsx (useParams pattern)
    - src/services/tripService.ts (useTripEntries — tripId already in its dep array)
    - src/components/ExpenseReport.tsx (the component built in Task 1)
    - src/App.tsx (the placeholder /trips/:tripId route to replace, line 28)
    - 24-PATTERNS.md sections "TripDetailPage", "TripDetailPage.test", "App.tsx Route Wiring"
    - PITFALLS.md Pitfall 5 (identical names -> UUID isolation) and the integration gotcha (tripId in useLiveQuery dep array)
  </read_first>
  <behavior>
    - While useTripEntries is undefined (Dexie opening), renders a neutral "Loading…" skeleton
    - Renders the ExpenseReport for the trip's entries (Food $10.10 + Hotel $5.20 -> subtotals + '$15.30' grand total visible)
    - Renders a timeline section listing ALL trip entries (expenses AND activities) sorted by occurredAt (fallback recordedAt), expenses showing category + amount, activities showing name + rating
    - Two trips both named 'Paris' with different UUIDs: loading /trips/<uuid-A> shows only trip A's entries, not trip B's
  </behavior>
  <action>
    Create `src/pages/TripDetailPage.tsx` exporting `TripDetailPage`. Read `const { tripId } = useParams<{ tripId: string }>()` and load `const entries = useTripEntries(tripId ?? '')` — useTripEntries already lists by `metadata.tripId` and re-runs when tripId changes, giving UUID-based isolation. When `entries === undefined`, render the neutral centered "Loading…" skeleton (mirror TripHomePage). Otherwise render `<ExpenseReport entries={entries} />` for the category-grouped report + grand total, and a timeline `<section>` that maps a copy of entries sorted ascending by `(e.occurredAt ?? e.recordedAt)`; render each expense row with its category (string `metadata.category` else 'Expense') and `formatUSD(e.amount ?? 0)`, and each activity row with its title/name and rating (`metadata.rating` when present). Do NOT add edit/delete controls here — Plan 24-03 owns those; keep timeline rows read-only with stable keys (`e.id`) and structure that 24-03 can attach buttons to. Import useParams from react-router-dom, useTripEntries from ../services/tripService, ExpenseReport from ../components/ExpenseReport, formatUSD from ../config/money, and the LifeLogEntry type from ../services/db. Then in src/App.tsx replace the placeholder `/trips/:tripId` route element with `<TripDetailPage />` and add the import. Create `src/pages/TripDetailPage.test.tsx` using the Dexie reset beforeEach (db.delete + db.open), afterEach vi.useRealTimers, `vi.useFakeTimers({ toFake: ['Date'] })` where timestamps matter, await act for db.entries.add writes, and a `<MemoryRouter initialEntries={[\`/trips/${trip.id}\`]}><Routes><Route path="/trips/:tripId" element={<TripDetailPage />} /></Routes></MemoryRouter>` wrapper. Cover: category subtotals + '$15.30' grand total, timeline lists both an expense and an activity, and the duplicate-name isolation test (create two 'Paris' trips via createAndActivateTrip, add an expense to each by its UUID, assert the detail page for trip A shows A's amount and not B's).
  </action>
  <acceptance_criteria>
    - `grep -n 'useParams' src/pages/TripDetailPage.tsx` and `grep -n 'useTripEntries' src/pages/TripDetailPage.tsx` both match
    - `grep -n 'ExpenseReport' src/pages/TripDetailPage.tsx` matches (report wired in)
    - timeline sort uses occurredAt fallback: `grep -n 'occurredAt ?? ' src/pages/TripDetailPage.tsx` matches
    - no edit/delete yet: `grep -nE 'buildEntryUpdate|entriesRepository.(update|delete)|EditEntryModal' src/pages/TripDetailPage.tsx` returns NOTHING (deferred to 24-03)
    - `grep -n 'TripDetailPage' src/App.tsx` returns both import and route element; placeholder for /trips/:tripId is gone
    - duplicate-name isolation test present and passing
    - `npx vitest run src/pages/TripDetailPage.test.tsx` passes; full `npx vitest run` green; `npx tsc -b` clean
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/TripDetailPage.test.tsx && npx tsc -b</automated>
  </verify>
  <done>TripDetailPage renders float-safe report + timeline, isolates by UUID, /trips/:tripId wired; tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route param :tripId → query | Untrusted URL segment used to scope entry reads |
| user-entered entry data → DOM | Categories, merchants, amounts rendered in report + timeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Spoofing | trip scoping by :tripId | mitigate | Entries scoped by exact metadata.tripId UUID match (useTripEntries); identical names cannot cross-contaminate (Pitfall 5) — covered by duplicate-name isolation test |
| T-24-05 | Tampering | report subtotals + grand total | mitigate | All monetary values via formatUSD (Math.round(x*100)/100); grand total === sum of subtotals (Pitfall 4) |
| T-24-06 | Information disclosure | merchant/category/title text | accept | React auto-escapes text children; local single-user data, no PII boundary |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — reuses existing deps only |
</threat_model>

<verification>
- `npx vitest run` fully green; `npx tsc -b` clean
- Report subtotals + grand total are float-safe; grand total equals sum of subtotals
- Detail page isolates entries by UUID across identically-named trips
</verification>

<success_criteria>
- ExpenseReport: ordered, expandable category report with float-safe subtotals + grand total
- TripDetailPage: report + chronological timeline of all entries, loaded by UUID
- /trips/:tripId wired in App.tsx
- RPT-01..04 satisfied
</success_criteria>

<output>
Create `.planning/phases/24-previous-trips-trip-detail-report/24-02-SUMMARY.md` when done
</output>
