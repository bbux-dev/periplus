---
phase: 21-app-shell-routing-rewrite-atomic-drop
plan: 04
type: execute
wave: 4
depends_on: [21-03]
files_modified:
  - src/pages/CaptureUrlPage.tsx
  - src/pages/CaptureUrlPage.test.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/DashboardPage.test.tsx
  - src/pages/DomainPage.tsx
  - src/pages/DomainPage.test.tsx
  - src/pages/EntryDetailPage.tsx
  - src/pages/EntryDetailPage.test.tsx
  - src/pages/EntryEditPage.tsx
  - src/pages/EntryEditPage.test.tsx
  - src/pages/EntryListPage.tsx
  - src/pages/EntryListPage.test.tsx
  - src/pages/ManageShortcutsPage.tsx
  - src/pages/ManageShortcutsPage.test.tsx
  - src/pages/ManualEntryPage.tsx
  - src/pages/ManualEntryPage.test.tsx
  - src/pages/ManualEntryPage.integration.test.tsx
  - src/pages/QuickCapturePage.tsx
  - src/pages/QuickCapturePage.test.tsx
  - src/pages/ReviewPage.tsx
  - src/pages/ReviewPage.test.tsx
  - src/pages/ShortcutFormPage.tsx
  - src/pages/ShortcutFormPage.test.tsx
  - src/services/dsl/parser.ts
  - src/services/dsl/parser.test.ts
  - src/services/dsl/suggest.ts
  - src/services/dsl/suggest.test.ts
  - src/config/navigation.ts
  - src/config/navigation.test.ts
  - src/config/shortcutConfig.ts
  - src/config/shortcutConfig.test.ts
  - src/services/configRepository.ts
  - src/services/configRepository.test.tsx
  - src/services/configPort.ts
  - src/services/configPort.test.ts
  - src/services/configValidator.ts
  - src/services/configValidator.test.ts
  - src/services/shortcutMutations.ts
  - src/services/shortcutMutations.test.ts
  - src/services/templateValidator.ts
  - src/services/templateValidator.test.ts
  - src/services/extractMetadataFromUrl.ts
  - src/services/extractMetadataFromUrl.test.ts
  - src/components/dashboard/IconPicker.tsx
  - src/components/dashboard/LayoutChips.tsx
  - src/components/dashboard/ShortcutRow.tsx
  - src/hooks/useShortcutCapture.ts
  - src/hooks/useShortcutCapture.test.ts
  - src/schemas/shortcut-config.v1.schema.json
  - src/services/urlUtils.ts
  - src/services/urlUtils.test.ts
autonomous: true
requirements: [UI-01, TRIP-04]
must_haves:
  truths:
    - "All 11 non-trip pages and every paired test file are deleted in the same commit"
    - "The dead DSL/shortcut/layout/config subsystem is deleted with its tests in the same commit"
    - "After deletion, npx tsc -b is clean with zero Cannot find module errors"
    - "After deletion, npx vitest run is green (suite count drops below 631 — that is expected)"
    - "The active trip persists across a reload because it lives in the activeMode settings key"
  artifacts:
    - path: "src/App.tsx"
      provides: "Compiles and runs with all dead pages removed"
      contains: "TripHomePage"
  key_links:
    - from: "vitest suite"
      to: "deleted modules"
      via: "no dangling imports"
      pattern: "Cannot find module"
---

<objective>
Atomically delete every non-trip page and the dead DSL/shortcut/layout/config subsystem TOGETHER with their test files in a single commit, then run the final verify gate: `npx tsc -b` clean and `npx vitest run` green. This is the central pitfall of the phase — deleting an implementation file without its paired test (or vice versa) leaves a dangling import that turns the whole Vitest file red. Everything in the DROP list goes in one commit. The suite count WILL drop below 631; the gate is GREEN, not a count.

Purpose: Remove the 11 non-trip screens + dead subsystem (UI-01) and confirm trip persistence across reload (TRIP-04).
Output: Deleted DROP-list files; green suite; clean compile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
Conditional-drop verification (confirmed during planning, re-confirm before deleting):
- urlUtils.ts — imported ONLY by EntryDetailPage.tsx and ReviewPage.tsx (both in DROP list) -> DROP urlUtils.ts + urlUtils.test.ts after the page deletions.
- distinctValues.ts — DOES NOT EXIST. distinctValues.test.ts tests entriesRepository.listDistinctValues (a KEPT service) -> KEEP distinctValues.test.ts. Do NOT delete it.
- useBackOrHome.ts — KEEP. Imported by the rewritten SettingsPage.tsx and new CreateTripPage.tsx. Do NOT delete.
- HoleSheet / SavedToast (components/dashboard) — KEEP (reused in Phases 22-24); captureService.ts only mentions HoleSheet in comments, no import coupling.
extractMetadataFromUrl.ts is safe to delete now: its only remaining importers are DROP-list pages (ReviewPage, CaptureUrlPage, QuickCapturePage test) — its ReviewDraft type was relocated to captureService.ts in plan 21-01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic deletion of all DROP-list files in one commit</name>
  <files>(all files in this plan's files_modified — deletions)</files>
  <read_first>
    - .planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-CONTEXT.md section "DROP" (the authoritative inventory)
    - .planning/research/PITFALLS.md Pitfall 1 (dead test files cause full-suite failure — delete impl + test atomically)
  </read_first>
  <action>
    In a SINGLE commit, git rm every file listed in this plan's files_modified. Group: (a) the 11 pages with their paired tests — CaptureUrlPage, DashboardPage, DomainPage, EntryDetailPage, EntryEditPage, EntryListPage, ManageShortcutsPage, ManualEntryPage (including ManualEntryPage.integration.test.tsx), QuickCapturePage, ReviewPage, ShortcutFormPage; (b) DSL — services/dsl/parser.ts+test, suggest.ts+test; (c) config — navigation.ts+test, shortcutConfig.ts+test; (d) services — configRepository.ts+test, configPort.ts+test, configValidator.ts+test, shortcutMutations.ts+test, templateValidator.ts+test, extractMetadataFromUrl.ts+test; (e) dashboard comps — IconPicker.tsx, LayoutChips.tsx, ShortcutRow.tsx; (f) hooks — useShortcutCapture.ts+test; (g) schema — schemas/shortcut-config.v1.schema.json; (h) conditional — urlUtils.ts+test. BEFORE deleting urlUtils.ts, run grep for urlUtils across src and confirm the only remaining references are DROP-list pages and urlUtils' own test; if any KEPT file still imports it, do NOT delete urlUtils and note it in the summary. Do NOT delete useBackOrHome.ts, distinctValues.test.ts, HoleSheet, or SavedToast. After the git rm batch, run tsc -b; if it reports any Cannot find module or unresolved import in a KEPT file, fix by either deleting a straggler that is itself dead or re-pointing the kept importer at a kept module — do not leave a broken compile. Commit all deletions together with message: refactor(21): atomically drop 11 non-trip pages + dead DSL/shortcut/layout subsystem.
  </action>
  <verify>
    <automated>cd /home/bbux/git/life-log && npx tsc -b 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `git show --stat HEAD` lists every page `.tsx` deletion alongside its paired `.test.tsx` deletion in the SAME commit (no page deleted without its test, no test deleted without its page).
    - `ls src/pages/` shows only PlaceholderPage, SettingsPage, CreateTripPage, TripHomePage (+ their tests) remaining.
    - `ls src/services/dsl 2>&1` is empty/absent; navigation.ts, shortcutConfig.ts, configRepository.ts, configPort.ts, configValidator.ts, shortcutMutations.ts, templateValidator.ts, extractMetadataFromUrl.ts no longer exist.
    - `test -f src/hooks/useBackOrHome.ts && echo KEPT` prints KEPT; `test -f src/services/distinctValues.test.ts && echo KEPT` prints KEPT.
    - `npx tsc -b` exits clean with zero Cannot find module errors.
    - tsc output contains no `Cannot find module` lines.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Final verify gate — full suite green + reload persistence</name>
  <files>(verification only — no source changes)</files>
  <read_first>
    - .planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-CONTEXT.md section "Specific Ideas" (suite count WILL drop below 631 — gate is green, not count)
    - .planning/research/PITFALLS.md "Looks Done But Isn't" checklist (page deletion, AppShell rewrite, App.test replacement)
  </read_first>
  <action>
    Run the full gate. First tsc -b and confirm clean. Then npx vitest run and confirm green with zero failing files and zero Cannot find module errors — note the new total test count in the summary (it is expected to be below the pre-phase 631; do NOT treat the drop as a regression). Run the AppShell dead-import grep gate over src/components/layout/AppShell.tsx for NAVIGATION/useShortcutConfig/listModes/LayoutChips — it must return nothing. Confirm TRIP-04 reload persistence by code-reasoning plus the existing activeMode tests: the active trip lives in the activeMode Dexie settings key (no in-memory-only state), so a reload re-reads it via useActiveMode; cite the passing activeMode/tripService tests that exercise activeModeRepository.get() returning the persisted trip after a fresh db.open(). If any check fails, fix the straggler (delete dead file or re-point kept import) and re-run until green. Do not modify source beyond fixing dangling imports surfaced by the gate.
  </action>
  <verify>
    <automated>cd /home/bbux/git/life-log && npx tsc -b 2>&1 | tail -3 && npx vitest run 2>&1 | tail -20 && grep -rnE "NAVIGATION|useShortcutConfig|listModes|LayoutChips" src/components/layout/AppShell.tsx; echo "appshell-grep-exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` exits clean.
    - `npx vitest run` reports zero failed tests and zero failed files; no `Cannot find module` anywhere in output.
    - The reported test count is below the pre-phase 631 and this is recorded as expected (not a regression).
    - The AppShell dead-import grep returns no matches (appshell-grep-exit:1).
    - Summary cites the specific passing activeMode/tripService test(s) that prove the active trip survives a fresh db.open() (TRIP-04 reload persistence).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Deletion + verification only; no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Denial of Service | full test suite | mitigate | Atomic impl+test deletion in one commit prevents dangling-import suite failure; verify gate enforces green |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npx tsc -b` clean, zero Cannot find module.
- `npx vitest run` green; count below 631 is expected.
- AppShell dead-import grep returns nothing.
- TRIP-04 reload persistence cited from passing activeMode/tripService tests.
</verification>

<success_criteria>
- All 11 non-trip pages + dead DSL/shortcut/layout/config subsystem deleted atomically with their tests (UI-01).
- Compile clean, full suite green after the deletion commit.
- Active trip persists across reload via the activeMode settings key (TRIP-04).
</success_criteria>

<output>
Create `.planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-04-SUMMARY.md` when done
</output>
