---
phase: 21-app-shell-routing-rewrite-atomic-drop
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/pages/CreateTripPage.tsx
  - src/pages/CreateTripPage.test.tsx
  - src/pages/TripHomePage.tsx
  - src/pages/TripHomePage.test.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/SettingsPage.test.tsx
autonomous: true
requirements: [TRIP-02, TRIP-04, UI-03, UI-05]
must_haves:
  truths:
    - "Navigating to /create-trip shows a name input + Save; Save calls createAndActivateTrip then navigates to /"
    - "After saving a trip, activeMode persists with mode='trip' and the entered label"
    - "TripHomePage shows a neutral loading skeleton until Dexie resolves (no flash of empty state on active-trip cold load)"
    - "TripHomePage redirects to /create-trip only after Dexie has resolved with no active trip"
    - "TripHomePage shows the active trip name once an active trip exists"
    - "SettingsPage shows only a JSON export button; export produces a valid life-log.json via exportEntries"
  artifacts:
    - path: "src/pages/CreateTripPage.tsx"
      provides: "Create-a-Trip empty/first-run screen (TRIP-02)"
      exports: ["CreateTripPage"]
      min_lines: 25
    - path: "src/pages/TripHomePage.tsx"
      provides: "Trip home STUB with loading-vs-no-trip guard (TRIP-04)"
      exports: ["TripHomePage"]
      min_lines: 30
    - path: "src/pages/SettingsPage.tsx"
      provides: "Export-only Settings page (UI-03)"
      exports: ["SettingsPage"]
      contains: "buildExportJson"
  key_links:
    - from: "src/pages/CreateTripPage.tsx"
      to: "src/services/tripService.ts"
      via: "createAndActivateTrip(name) then navigate('/')"
      pattern: "createAndActivateTrip"
    - from: "src/pages/TripHomePage.tsx"
      to: "src/services/db.ts"
      via: "useLiveQuery db.settings.count() as dbReady settled signal"
      pattern: "db\\.settings\\.count"
    - from: "src/pages/SettingsPage.tsx"
      to: "src/services/exportEntries.ts"
      via: "buildExportJson + triggerDownload"
      pattern: "buildExportJson|triggerDownload"
---

<objective>
Build the new and rewritten trip-flow pages that the router (plan 21-03) wires up: a CreateTripPage empty-state, a TripHomePage stub with the correct loading-vs-no-trip guard, and a Settings page reduced to JSON export only. These ship BEFORE the router rewrite so App.tsx (plan 21-03) compiles against pages that already exist, and BEFORE the atomic deletion (plan 21-04) so the new shell stands on its own.

Purpose: Deliver the first-run "Create a Trip" screen (TRIP-02), the reload-persistent trip home guard (TRIP-04), the export-only Settings (UI-03), all composing existing primitives/engine without duplication (UI-05).
Output: CreateTripPage(+test), TripHomePage(+test), rewritten SettingsPage(+test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-CONTEXT.md
@.planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
From src/services/tripService.ts: `createAndActivateTrip(name: string)` creates a trip entry and activates it; `useTrips()`, plus the re-exported `useActiveMode`.
From src/services/activeMode.ts: `useActiveMode()` returns the ActiveMode `{ mode, label, tripId? }` or `undefined` (undefined = Dexie loading OR no active trip — do NOT treat bare undefined as "no trip"); `activeModeRepository.get()/put()`.
From src/services/db.ts: `db` (Dexie instance); `db.settings.count()` is the cheapest settled-signal read.
From src/services/exportEntries.ts: `buildExportJson(entries, exportedAt)`, `triggerDownload(json, filename)`.
From src/services/entriesRepository.ts: `entriesRepository.list()`.
From src/hooks/useBackOrHome.ts: `useBackOrHome('/')` (KEPT — used by SettingsPage and CreateTripPage).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CreateTripPage (empty-state) + test</name>
  <files>src/pages/CreateTripPage.tsx, src/pages/CreateTripPage.test.tsx</files>
  <behavior>
    - Renders a trip-name textbox and a Save button under a "Create a Trip" heading + Back button.
    - Typing a name and clicking Save calls createAndActivateTrip(name) and navigates to '/'.
    - After Save, activeModeRepository.get() returns mode='trip' with the entered label.
    - Save with an empty/whitespace name is a no-op (no navigation, no activation).
  </behavior>
  <read_first>
    - .planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-PATTERNS.md sections "CreateTripPage.tsx" and "CreateTripPage.test.tsx" (page scaffold, save-and-navigate, MemoryRouter probe-route, Dexie reset, fake-timers guard)
    - src/pages/PlaceholderPage.tsx (page scaffold + Back button analog)
    - src/services/tripService.ts (createAndActivateTrip signature + behavior)
    - src/services/tripService.test.tsx (Dexie beforeEach reset + act() pattern)
  </read_first>
  <action>
    Create CreateTripPage.tsx exporting a named `CreateTripPage` component. Use the page-scaffold div pair and Back button from PlaceholderPage (useBackOrHome('/') + ChevronLeftIcon). Render an h1 "Create a Trip", a controlled text input bound to a name useState, and an outline-styled Save button reusing the button CSS from SettingsPage. handleSave: if name.trim() is empty, return early; otherwise await createAndActivateTrip(name) then navigate('/'). Wire the Save click via `() => { void handleSave() }`. Create CreateTripPage.test.tsx using the MemoryRouter + probe-route pattern: a "/create-trip" route rendering CreateTripPage and a "/" route rendering a data-testid="home-probe" element. Use the Dexie reset beforeEach (`await db.delete(); await db.open()`) and `afterEach(() => vi.useRealTimers())`. Cover: (a) type a name + click Save -> home-probe appears; (b) after Save, activeModeRepository.get() returns mode='trip' and the entered label; (c) clicking Save with empty input does not navigate. If any test needs Date control, use `vi.useFakeTimers({ toFake: ['Date'] })` ONLY.
  </action>
  <verify>
    <automated>cd /home/bbux/git/life-log && npx vitest run src/pages/CreateTripPage.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "createAndActivateTrip" src/pages/CreateTripPage.tsx` returns a hit and `navigate('/')` follows it in handleSave.
    - `grep -v '^\s*//' src/pages/CreateTripPage.test.tsx | grep -c "vi.useFakeTimers()"` is 0 (no full fake timers).
    - `npx vitest run src/pages/CreateTripPage.test.tsx` is green with the three behaviors above.
    - CreateTripPage exports a named `CreateTripPage` (not default-only) for App.tsx import in plan 21-03.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TripHomePage stub with loading-vs-no-trip guard + test</name>
  <files>src/pages/TripHomePage.tsx, src/pages/TripHomePage.test.tsx</files>
  <behavior>
    - While Dexie is still opening (dbReady false), renders a neutral "Loading…" skeleton — never the create/empty state.
    - Once Dexie resolves with no active trip, redirects to /create-trip (replace).
    - Once Dexie resolves with an active trip, renders the trip label as a heading.
  </behavior>
  <read_first>
    - .planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-PATTERNS.md section "TripHomePage.tsx" (the dbReady useLiveQuery settled-signal pattern) and "TripHomePage.test.tsx" (loading / redirect / active-trip tests)
    - .planning/research/PITFALLS.md Pitfall 2 (useActiveMode undefined conflates loading vs no-trip)
    - src/services/activeMode.test.tsx (act + activeModeRepository.put pattern)
  </read_first>
  <action>
    Create TripHomePage.tsx exporting a named `TripHomePage`. Read `activeMode = useActiveMode()` and a settled signal `dbReady = useLiveQuery(() => db.settings.count().then(() => true as const), [], false as const)`. Guard order: if `!dbReady` return the neutral centered "Loading…" skeleton (using the project's background/muted tokens); else if `!activeMode || activeMode.mode !== 'trip'` return `<Navigate to="/create-trip" replace />` (import `Navigate` from `react-router-dom`; use the declarative element, NOT an imperative `navigate()` during render — that triggers a React "cannot update during render" act() warning); else render the page scaffold with an h1 showing `activeMode.label` plus a muted "Trip home — coming in Phase 22." placeholder line (full Home is Phase 22 — keep this a STUB, do NOT build totals/recent entries/CTAs). Create TripHomePage.test.tsx with the Dexie reset beforeEach + `afterEach(() => vi.useRealTimers())`. Cover three cases per PATTERNS: (a) loading skeleton renders synchronously before Dexie resolves; (b) with no active trip, MemoryRouter routes "/" -> TripHomePage and "/create-trip" -> a data-testid="create-probe" element, asserting the probe appears; (c) after `activeModeRepository.put({ mode:'trip', label:'Paris', tripId:'uuid-1' })` inside act(), the heading 'Paris' is shown. Use `vi.useFakeTimers({ toFake: ['Date'] })` only if Date control is needed.
  </action>
  <verify>
    <automated>cd /home/bbux/git/life-log && npx vitest run src/pages/TripHomePage.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "db.settings.count" src/pages/TripHomePage.tsx` returns a hit (settled signal present).
    - `grep -n "create-trip" src/pages/TripHomePage.tsx` shows a declarative `<Navigate to="/create-trip" replace />` redirect (no imperative `navigate('/create-trip')` call in the render body).
    - The component does NOT render the create/empty state on bare `undefined` — the loading branch is gated on `!dbReady`, verified by the loading test passing.
    - `grep -v '^\s*//' src/pages/TripHomePage.test.tsx | grep -c "vi.useFakeTimers()"` is 0.
    - `npx vitest run src/pages/TripHomePage.test.tsx` is green with all three behaviors.
    - TripHomePage exports a named `TripHomePage` for App.tsx import in plan 21-03.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rewrite SettingsPage to export-only + update its test</name>
  <files>src/pages/SettingsPage.tsx, src/pages/SettingsPage.test.tsx</files>
  <read_first>
    - src/pages/SettingsPage.tsx (current file — keep page scaffold + Back button + export-button CSS; drop all import/config-port logic)
    - .planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-PATTERNS.md section "SettingsPage.tsx" (export-button repoint to exportEntries; full DROP list of imports/state/elements)
    - src/services/exportEntries.ts (buildExportJson + triggerDownload signatures)
    - src/services/entriesRepository.ts (list())
    - src/pages/SettingsPage.test.tsx (current test — replace config import/export assertions with export-only assertions)
  </read_first>
  <action>
    Rewrite SettingsPage.tsx to retain only: the page-scaffold div pair, the Back button (useBackOrHome('/') + ChevronLeftIcon), an h1 heading, and a single "Export JSON" outline button (keep the existing button CSS verbatim). Implement `async function handleExport()` that does `const entries = await entriesRepository.list()` then `triggerDownload(buildExportJson(entries, Date.now()), 'life-log.json')`. Wire the button via `() => { void handleExport() }`. REMOVE every shortcut/layout/DSL/import-config artifact: the useShortcutConfig hook, buildConfigExportJson, importConfig, the configPort triggerDownload, ImportResult type, fileInputRef, importStatus state, handleFileChange, the file input element, the Import JSON button, and all import-status message elements. Update SettingsPage.test.tsx to assert: the Export JSON button is present and clicking it triggers a download (mock/spy on triggerDownload or assert exportEntries is invoked), AND no shortcut/import/layout UI is present (e.g., queryByText for "Import" returns null). Remove any test imports of dropped config modules so the file has zero dangling imports.
  </action>
  <verify>
    <automated>cd /home/bbux/git/life-log && npx tsc -b 2>&1 | tail -5 && npx vitest run src/pages/SettingsPage.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "useShortcutConfig|buildConfigExportJson|importConfig|configPort|ImportResult|handleFileChange" src/pages/SettingsPage.tsx` returns NO hits.
    - `grep -nE "buildExportJson|triggerDownload" src/pages/SettingsPage.tsx` returns hits from exportEntries.
    - `grep -nE "configPort|configRepository|shortcutConfig" src/pages/SettingsPage.test.tsx` returns NO hits (no dangling imports of soon-deleted modules).
    - `npx vitest run src/pages/SettingsPage.test.tsx` is green; a test asserts the Import UI is absent.
    - `npx tsc -b` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> trip name | Free-text trip name persisted to Dexie via createAndActivateTrip |
| app -> file download | JSON export written to disk via triggerDownload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Tampering | trip name input | accept | Local-only Dexie write, no server/eval; name rendered as React-escaped text (no dangerouslySetInnerHTML) |
| T-21-03 | Information Disclosure | JSON export | accept | Export is user-initiated, local download of the user's own entries; no new exposure |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npx tsc -b` clean.
- CreateTripPage, TripHomePage, SettingsPage all export named components and have green tests.
- TripHomePage loading guard proven by the loading-before-resolve test (no flash of empty state).
- SettingsPage has zero shortcut/config imports.
</verification>

<success_criteria>
- CreateTripPage save -> createAndActivateTrip -> navigate('/'), activeMode persisted (TRIP-02; reload persistence verified end-to-end in 21-04).
- TripHomePage shows loading skeleton until Dexie resolves, then trip name or redirect (TRIP-04).
- SettingsPage is export-only and produces valid JSON (UI-03).
- New screens compose existing engine/primitives, no duplication (UI-05).
</success_criteria>

<output>
Create `.planning/phases/21-app-shell-routing-rewrite-atomic-drop/21-02-SUMMARY.md` when done
</output>
