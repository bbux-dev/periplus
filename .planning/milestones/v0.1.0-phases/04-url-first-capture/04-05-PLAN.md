---
phase: 04-url-first-capture
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/components/Counter.tsx
  - src/components/Counter.test.tsx
  - src/pages/WelcomePage.tsx
  - src/pages/WelcomePage.test.tsx
  - src/pages/EntryTypePage.tsx
  - src/pages/EntryTypePage.test.tsx
autonomous: true
requirements: [CAPT-01]
must_haves:
  truths:
    - "The route /d/:domain/:type renders CaptureUrlPage (the URL Capture screen), not the old EntryTypePage"
    - "The route /d/:domain/:type/review renders the real ReviewPage and /d/:domain/:type/manual remains a placeholder (Phase 5)"
    - "The /d/:domain/:type/capture stub route is removed and its App.test.tsx case is gone"
    - "The Phase 1 throwaway counter (Counter, WelcomePage) and the replaced EntryTypePage are fully deleted with no dead imports remaining"
    - "An end-to-end capture→review→save flow through the real App router persists a LifeLogEntry and lands on the domain screen"
  artifacts:
    - path: "src/App.tsx"
      provides: "Route table wiring CaptureUrlPage + ReviewPage; counter/EntryTypePage imports removed"
      contains: "CaptureUrlPage"
    - path: "src/App.test.tsx"
      provides: "Updated route table tests + end-to-end capture→review→save integration test"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/CaptureUrlPage.tsx"
      via: "Route path=/d/:domain/:type element=<CaptureUrlPage />"
      pattern: "CaptureUrlPage"
    - from: "src/App.tsx"
      to: "src/pages/ReviewPage.tsx"
      via: "Route path=/d/:domain/:type/review element=<ReviewPage />"
      pattern: "ReviewPage"
---

<objective>
Rewire `App.tsx` so the URL-first flow is live: `CaptureUrlPage` at `/d/:domain/:type`, the real `ReviewPage` at `/d/:domain/:type/review`, the manual route kept as a Phase 5 placeholder, and the obsolete `/d/:domain/:type/capture` stub removed. Delete the Phase 1 throwaway counter (`Counter`, `WelcomePage`) and the now-replaced `EntryTypePage` — all six files — and update `App.test.tsx` (drop the stale `/capture` case, keep the rest green) plus add the end-to-end capture→review→save integration test. This is the only plan that touches `App.tsx`, so deletions are coupled here to keep the build green throughout.

Purpose: CAPT-01 (full routing — capture shown by default at the entry-type route) and the mandated counter/dead-code removal.
Output: rewired `App.tsx`, updated `App.test.tsx`, six files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-url-first-capture/04-RESEARCH.md

<interfaces>
<!-- Current App.tsx routes (to be changed) + the new page exports. -->

Current src/App.tsx route block:
  <Route path="/"                          element={<DashboardPage />} />
  <Route path="/d/:domain"                 element={<DomainPage />} />
  <Route path="/d/:domain/:type"           element={<EntryTypePage />} />          // REPLACE → CaptureUrlPage
  <Route path="/d/:domain/:type/capture"   element={<PlaceholderPage title="URL Capture" />} />  // REMOVE
  <Route path="/d/:domain/:type/review"    element={<PlaceholderPage title="Review" />} />       // REPLACE → ReviewPage
  <Route path="/d/:domain/:type/manual"    element={<PlaceholderPage title="Manual Entry" />} /> // KEEP (Phase 5 stub)
  <Route path="/entries"                   element={<PlaceholderPage title="Entry List" />} />
  <Route path="/entries/:id"               element={<PlaceholderPage title="Entry Detail" />} />
  <Route path="*"                          element={<PlaceholderPage title="Page Not Found" />} />

New page exports (Wave 2):
  src/pages/CaptureUrlPage.tsx → export function CaptureUrlPage()
  src/pages/ReviewPage.tsx     → export function ReviewPage()

Files to DELETE (RESEARCH "Counter Removal — Exact File Manifest"; grep-confirmed no live imports except App.tsx's EntryTypePage import which this plan removes):
  src/components/Counter.tsx, src/components/Counter.test.tsx,
  src/pages/WelcomePage.tsx, src/pages/WelcomePage.test.tsx,
  src/pages/EntryTypePage.tsx, src/pages/EntryTypePage.test.tsx

NOTE: src/services/db.ts has an internal `Counter` interface + `counter` store — that is the IndexedDB tracer store, NOT the Counter component. DO NOT touch db.ts.

App.test.tsx cases affected:
  - The it.each routes array includes `{ path: '/d/media/book/capture', expectedHeading: /url capture/i }` → REMOVE this entry (route deleted).
  - `{ path: '/d/media/book', expectedHeading: /add book/i }` → KEEP (CaptureUrlPage renders "Add Book").
  - `{ path: '/d/media/book/review', expectedHeading: /review/i }` → KEEP only if it can render without a draft; ReviewPage with no location.state now REDIRECTS to capture. So this case must change: either drop it or update its expected heading to /add book/i (the redirect target). Choose to update it to assert the redirect lands on the capture heading, OR remove it and cover review via the new integration test.
  - W-01 "renders raw type string for unknown :type" at /d/media/bogus_type expects "Add bogus_type" → KEEP (CaptureUrlPage uses `typeConfig?.label ?? type`).
  - Navigation-flow + back-nav tests that go to /d/media/book and expect /add book/i → KEEP (still pass with CaptureUrlPage).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire App.tsx and delete the six dead files</name>
  <files>src/App.tsx, src/components/Counter.tsx, src/components/Counter.test.tsx, src/pages/WelcomePage.tsx, src/pages/WelcomePage.test.tsx, src/pages/EntryTypePage.tsx, src/pages/EntryTypePage.test.tsx</files>
  <read_first>
    - src/App.tsx (current route table + imports)
    - src/pages/CaptureUrlPage.tsx, src/pages/ReviewPage.tsx (Wave 2 — the new elements to wire)
    - .planning/phases/04-url-first-capture/04-RESEARCH.md Pattern 1 (routing) + "Counter Removal — Exact File Manifest"
  </read_first>
  <action>
    In App.tsx: remove the `EntryTypePage` import; add imports for `CaptureUrlPage` and `ReviewPage`. Change the `/d/:domain/:type` route element to `<CaptureUrlPage />`. Remove the `/d/:domain/:type/capture` Route line entirely. Change the `/d/:domain/:type/review` route element to `<ReviewPage />`. Keep `/d/:domain/:type/manual` as `<PlaceholderPage title="Manual Entry" />` (Phase 5 fills it). Leave the entries routes and catch-all unchanged.

    Delete all six files in the manifest using the filesystem (git rm or rm): Counter.tsx, Counter.test.tsx, WelcomePage.tsx, WelcomePage.test.tsx, EntryTypePage.tsx, EntryTypePage.test.tsx. Do NOT touch src/services/db.ts (its Counter interface/store is unrelated). After deletion, confirm no remaining source file imports any of the deleted modules.
  </action>
  <verify>
    <automated>grep -rEl "from '.*(Counter|WelcomePage|EntryTypePage)'" src/ | grep -v node_modules; echo "exit:$?"; npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx imports `CaptureUrlPage` and `ReviewPage` and no longer imports `EntryTypePage` (grep confirms).
    - App.tsx route `/d/:domain/:type` element is `<CaptureUrlPage />`; `/d/:domain/:type/review` element is `<ReviewPage />`; the `/d/:domain/:type/capture` route line is gone (grep -c 'type/capture' src/App.tsx returns 0).
    - All six manifest files no longer exist on disk.
    - No source file under src/ imports Counter, WelcomePage, or EntryTypePage (the grep for those import paths returns no files).
    - `npx tsc -b` compiles clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update App.test.tsx route table + add end-to-end capture→review→save test</name>
  <files>src/App.test.tsx</files>
  <read_first>
    - src/App.test.tsx (current it.each routes array + nav-flow + back-nav tests)
    - src/pages/ReviewPage.test.tsx (Wave 2 — fake-indexeddb pattern + how the save flow asserts persistence)
    - .planning/phases/04-url-first-capture/04-RESEARCH.md Pitfall 4 (the /capture test case must be removed) + Pitfall 3 (ReviewPage redirect when no draft)
  </read_first>
  <action>
    In the `it.each(routes)` array: REMOVE the `{ path: '/d/media/book/capture', expectedHeading: /url capture/i }` entry (route deleted). For the `{ path: '/d/media/book/review', expectedHeading: /review/i }` entry: because ReviewPage now redirects to the capture screen when there is no location.state, update this entry to expect the redirect target heading `/add book/i` (the capture screen), OR remove the entry and rely on the new integration test below. Keep all other route-table entries, the catch-all 404 tests, the W-01 unknown-type/domain tests (CaptureUrlPage renders "Add bogus_type" and the unknown-domain guard), and the navigation-flow + back-navigation tests unchanged — they still pass because CaptureUrlPage renders the "Add {label}" heading and keeps the Back button.

    Add a new `describe('App — capture → review → save (CAPT end-to-end, SC2/SC4)')` block with fake-indexeddb: `beforeEach(async () => { await db.delete(); await db.open() })` (import db from './services/db'). Render `<App />` in a MemoryRouter starting at `/d/trips/place`. Steps: type a Google Maps URL ("https://www.google.com/maps/place/Eiffel+Tower/@48.8583701,2.2944813,17z") into the URL input, click "Import from URL", assert the Review screen appears with the pre-filled title "Eiffel Tower", optionally edit the title, click "Save", then await and assert (a) the app navigated to the trips domain screen (DomainPage marker, e.g. "Place"/"Event"/"Expense" tiles or the "Trips" content) AND (b) `entriesRepository.list()` returns one entry with title containing "Eiffel Tower", domain 'trips', type 'place', sourceUrl preserved. This proves the full URL-first slice end-to-end through the real router.
  </action>
  <verify>
    <automated>npx vitest run src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App.test.tsx no longer contains the `/d/media/book/capture` route-table case (grep -c 'book/capture' src/App.test.tsx returns 0).
    - The new end-to-end describe block exists and uses fake-indexeddb (beforeEach db.delete/db.open).
    - The integration test drives URL input → Import → Review prefill ("Eiffel Tower") → Save → asserts navigation to the trips domain AND one persisted entry via entriesRepository.
    - `npx vitest run src/App.test.tsx` fully green.
    - FULL suite + build green: `npx vitest run && npx tsc -b && npx vite build` (the phase gate from VALIDATION.md). Existing Phase 2/3 tests preserved except the intentionally-removed counter/welcome/entrytype files and the stale /capture case.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router → page elements | Route params (:domain, :type) are untrusted path segments; pages guard unknown values |
| end-to-end URL → IndexedDB | Full slice: pasted URL parsed offline, edited, persisted locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Denial of Service | unknown :domain / :type route params | mitigate | CaptureUrlPage carries forward EntryTypePage's unknown-domain guard and `typeConfig?.label ?? type` fallback; ReviewPage redirects on missing draft. No route param crashes the app (W-01 tests preserved). |
| T-04-14 | Tampering | dead-code deletion breaking the build | mitigate | Deletions are coupled with the App.tsx rewiring in one plan; grep gate + `npx tsc -b` + full suite confirm no dangling imports remain. |
| T-04-15 | Tampering (XSS) | end-to-end rendered entry data | mitigate | All rendered strings are React text nodes — auto-escaped; no dangerouslySetInnerHTML in any page. |
| T-04-SC | Tampering | npm installs | accept | Zero new packages this plan (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx vitest run && npx tsc -b && npx vite build` — full phase gate green (VALIDATION.md "Full suite command").
- Grep gates: `src/App.tsx` has no `type/capture` route and no `EntryTypePage` import; the six deleted files are absent; no source file imports the deleted modules.
- The end-to-end capture→review→save test persists a real LifeLogEntry and lands on the trips domain.
</verification>

<success_criteria>
CAPT-01 fully wired: choosing an entry type lands on the URL Capture screen by default; the obsolete /capture stub is gone; ReviewPage is live; the Phase 1 counter and the replaced EntryTypePage are cleanly removed with the suite green.
</success_criteria>

<output>
Create `.planning/phases/04-url-first-capture/04-05-SUMMARY.md` when done.
</output>
