---
phase: 04-url-first-capture
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/pages/CaptureUrlPage.tsx
  - src/pages/CaptureUrlPage.test.tsx
autonomous: true
requirements: [CAPT-01, CAPT-02, CAPT-06]
must_haves:
  truths:
    - "After choosing an entry type, the Capture screen renders an 'Add {Type}' heading, a URL input, a primary 'Import from URL' button, and a secondary 'Enter Manually' button"
    - "Pasting a Google Maps URL and clicking Import navigates to the review route with the extracted draft carried in router location.state"
    - "The Import button is disabled while the URL field is empty"
    - "'Enter Manually' is a clearly secondary (non-primary) action that navigates to the manual route"
  artifacts:
    - path: "src/pages/CaptureUrlPage.tsx"
      provides: "URL Capture screen at /d/:domain/:type"
      exports: ["CaptureUrlPage"]
      min_lines: 40
    - path: "src/pages/CaptureUrlPage.test.tsx"
      provides: "RTL tests for render (CAPT-01/06) + import navigation (CAPT-02)"
  key_links:
    - from: "src/pages/CaptureUrlPage.tsx"
      to: "src/services/extractMetadataFromUrl.ts"
      via: "import { extractMetadataFromUrl }"
      pattern: "extractMetadataFromUrl"
    - from: "src/pages/CaptureUrlPage.tsx"
      to: "review route"
      via: "navigate(reviewPath, { state: { draft } })"
      pattern: "state:\\s*\\{\\s*draft"
    - from: "src/pages/CaptureUrlPage.tsx"
      to: "src/components/ui/FormField.tsx"
      via: "import { FormField }"
      pattern: "FormField"
---

<objective>
Build `CaptureUrlPage` — the DEFAULT screen shown at `/d/:domain/:type` after a user chooses an entry type. It presents a URL input plus a primary "Import from URL" action (runs the extractor, navigates to Review with the draft in router state) and a clearly-secondary "Enter Manually" action. It replaces `EntryTypePage` at the same route (the actual App.tsx rewiring + EntryTypePage deletion happens in Wave 3 / plan 04-05).

Purpose: CAPT-01 (capture shown by default), CAPT-02 (paste → infer → navigate to Review), CAPT-06 (Enter Manually visible as a secondary, non-default action).
Output: `CaptureUrlPage.tsx` + RTL tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-url-first-capture/04-RESEARCH.md

<interfaces>
<!-- Everything CaptureUrlPage consumes. Extracted from codebase + Wave 1 outputs. -->

From src/services/extractMetadataFromUrl.ts (Wave 1 / plan 04-02):
  export function extractMetadataFromUrl(url: string, type: EntryType): ExtractedDraft
  export interface ExtractedDraft { sourceUrl: string; title?: string; location?: string; metadata: Record<string, unknown> }

From src/components/ui/FormField.tsx (Wave 1 / plan 04-01):
  export function FormField(props)  // id, label, value, onChange, placeholder, ...input attrs

From src/components/ui/Button.tsx:
  export function Button({ variant, size, ...props })  // variant 'primary' | 'secondary' | 'ghost'

From src/config/navigation.ts:
  export function getDomainConfig(domain: string): DomainConfig | undefined
  // DomainConfig.types: { type: EntryType; label: string; icon }[]

From src/hooks/useBackOrHome.ts:
  export function useBackOrHome(fallback?: string): () => void

From src/services/db.ts:
  export type EntryType = 'show' | 'movie' | 'book' | 'podcast' | 'place' | 'event' | 'expense'

Heading + guard pattern to carry forward — from src/pages/EntryTypePage.tsx (being replaced):
  heading: <h1>Add {typeConfig?.label ?? type}</h1>
  unknown-domain guard renders "Unknown domain: {domain}" with a Back button (aria-label "Go back")
  page chrome classes: outer div 'min-h-screen flex flex-col px-6 py-8 bg-[var(--color-background)] text-[var(--color-foreground)]'
                       inner 'w-full max-w-sm mx-auto flex flex-col gap-4'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CaptureUrlPage component</name>
  <files>src/pages/CaptureUrlPage.tsx</files>
  <read_first>
    - src/pages/EntryTypePage.tsx (page chrome, heading "Add {label}", Back button, unknown-domain guard — carry these forward exactly)
    - src/components/ui/FormField.tsx (Wave 1) and src/components/ui/Button.tsx (variant prop)
    - src/services/extractMetadataFromUrl.ts (Wave 1 — signature + ExtractedDraft)
    - src/hooks/useBackOrHome.ts, src/config/navigation.ts (getDomainConfig)
    - .planning/phases/04-url-first-capture/04-RESEARCH.md Pattern 5 (CaptureUrlPage structure)
  </read_first>
  <action>
    Create named export `CaptureUrlPage`. Read params with `useParams<{ domain: string; type: string }>()` (default both to ''). Resolve `config = getDomainConfig(domain)` and `typeConfig = config?.types.find(t => t.type === type)`. Set up `useNavigate()` and `useBackOrHome(`/d/${domain}`)`. Keep local `const [url, setUrl] = useState('')`.

    Preserve the unknown-domain guard from EntryTypePage (same Back button + "Unknown domain: {domain}" message) — do NOT crash on unknown domain. NOTE the existing App.test.tsx W-01 case renders `/d/media/bogus_type` (known domain, unknown TYPE) and expects "Add bogus_type"; so the heading must use `typeConfig?.label ?? type` and the page must render normally for an unknown type under a known domain.

    Render heading `<h1>Add {typeConfig?.label ?? type}</h1>` (carries CAPT-01 + keeps the existing App.test.tsx "add book" assertion passing). Render a `FormField` with id "capture-url", label "URL", a placeholder like "Paste a URL...", `value={url}`, `onChange={e => setUrl(e.target.value)}`. Below it render two Buttons: a primary `Button variant="primary"` labelled "Import from URL", disabled when `url.trim()` is empty, onClick handler `handleImport`; and a secondary `Button variant="secondary"` labelled "Enter Manually", onClick `handleManual`.

    handleImport: `const draft = extractMetadataFromUrl(url.trim(), type as EntryType)` then `navigate(`/d/${domain}/${type}/review`, { state: { draft } })`. handleManual: `navigate(`/d/${domain}/${type}/manual`)`. Reuse EntryTypePage's outer/inner div chrome classes and the Back button (aria-label "Go back"). Do NOT render with dangerouslySetInnerHTML.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/CaptureUrlPage.tsx` exists and exports a named `CaptureUrlPage`.
    - Imports `extractMetadataFromUrl` and `FormField` (grep both tokens present).
    - The Import handler calls `navigate` with `{ state: { draft } }` to the `/review` path (grep `state:` and `/review`).
    - The "Enter Manually" Button uses `variant="secondary"` and the Import Button uses `variant="primary"` (grep both).
    - `npx tsc -b` compiles clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CaptureUrlPage tests (CAPT-01/02/06 + SC1/SC2)</name>
  <files>src/pages/CaptureUrlPage.test.tsx</files>
  <read_first>
    - src/pages/CaptureUrlPage.tsx (Task 1)
    - src/pages/EntryTypePage.test.tsx (existing RTL + MemoryRouter + Routes pattern to mirror)
    - .planning/phases/04-url-first-capture/04-RESEARCH.md "Success Criteria → Test Mapping" (SC1, SC2)
  </read_first>
  <action>
    Mirror the existing EntryTypePage.test.tsx render pattern (MemoryRouter + Routes). To prove CAPT-02 navigation WITHOUT importing ReviewPage (which is built in parallel plan 04-04), register a lightweight probe route at `/d/:domain/:type/review` whose element reads `useLocation().state` and renders the draft's title and sourceUrl into the DOM (e.g. a small inline `function ReviewProbe()` defined inside the test file). This keeps the plan decoupled while still asserting the draft crossed the route boundary via location.state.

    Test cases:
      1. CAPT-01/SC1 render: render at `/d/trips/place`; assert heading /add place/i, a textbox (the URL FormField), a button /import from url/i, and a button /enter manually/i are all present.
      2. CAPT-01: the Import button is disabled initially (empty URL) and becomes enabled after typing a URL (userEvent.type).
      3. CAPT-02/SC2: render at `/d/trips/place` with the ReviewProbe route mounted; type a Google Maps URL ("https://www.google.com/maps/place/Eiffel+Tower/@48.8583701,2.2944813,17z"), click "Import from URL", then assert the probe shows "Eiffel Tower" (extracted title) and the sourceUrl — proving extraction ran and the draft was passed via location.state.
      4. CAPT-06: click "Enter Manually" with a manual probe route at `/d/:domain/:type/manual` mounted; assert navigation reached the manual route (probe renders an identifiable marker). Also assert the "Enter Manually" button is NOT the primary/default styling by confirming the primary action is "Import from URL" (e.g. both buttons render and Import is the enabled primary once a URL is typed).
  </action>
  <verify>
    <automated>npx vitest run src/pages/CaptureUrlPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Test file renders CaptureUrlPage via MemoryRouter + Routes (no import of ReviewPage).
    - CAPT-01 render test green (heading + URL input + both buttons).
    - CAPT-02 test green: after typing a Google Maps URL and clicking Import, the probe route shows the extracted "Eiffel Tower" title and the sourceUrl (proves location.state draft transport).
    - CAPT-06 test green: "Enter Manually" navigates to the manual route.
    - `npx vitest run src/pages/CaptureUrlPage.test.tsx` fully green; full suite `npx vitest run` stays green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL input → React state → router location.state | Untrusted pasted text held in state, passed transiently to Review; never rendered as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering (XSS) | extracted title/url rendered on screen | mitigate | All extracted/typed strings render as React text nodes (heading, input value) — auto-escaped. No dangerouslySetInnerHTML. |
| T-04-07 | Denial of Service | extractMetadataFromUrl on bad input | mitigate | Extractor is hardened in plan 04-02 (try/catch, never throws); CaptureUrlPage calls it with a trimmed string and always navigates with whatever draft returns (CAPT-04). |
| T-04-08 | Tampering | empty-URL import → empty entry | accept | Import button is disabled while url.trim() is empty, preventing a navigation with no input (low-value guard, not a security control). |
| T-04-SC | Tampering | npm installs | accept | Zero new packages this plan. |
</threat_model>

<verification>
- `npx vitest run src/pages/CaptureUrlPage.test.tsx` green (CAPT-01/02/06, SC1/SC2).
- `npx tsc -b` compiles.
- Full suite `npx vitest run` green (no regression; App.tsx not yet touched — EntryTypePage still live until Wave 3).
</verification>

<success_criteria>
SC1: URL Capture shown by default with a visible, secondary "Enter Manually" button. SC2: pasting a Google Maps URL and importing carries extracted fields to the Review route via location.state.
</success_criteria>

<output>
Create `.planning/phases/04-url-first-capture/04-03-SUMMARY.md` when done.
</output>
