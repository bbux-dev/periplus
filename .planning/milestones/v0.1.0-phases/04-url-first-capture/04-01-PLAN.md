---
phase: 04-url-first-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Input.tsx
  - src/components/ui/Input.test.tsx
  - src/components/ui/FormField.tsx
  - src/components/ui/FormField.test.tsx
autonomous: true
requirements: [SETUP-05]
must_haves:
  truths:
    - "An <Input> primitive renders a styled <input> that forwards arbitrary input props (value, onChange, placeholder, disabled, type)"
    - "A <FormField> primitive renders a <label> associated with its <input> via htmlFor/id so screen readers announce the label"
    - "FormField surfaces an error message with role=alert and sets aria-invalid on the input when error is present"
  artifacts:
    - path: "src/components/ui/Input.tsx"
      provides: "Input primitive (ref-as-prop, cn-styled)"
      exports: ["Input"]
      min_lines: 15
    - path: "src/components/ui/FormField.tsx"
      provides: "FormField primitive (label + Input + error/help)"
      exports: ["FormField"]
      min_lines: 20
    - path: "src/components/ui/Input.test.tsx"
      provides: "RTL tests for Input rendering + prop forwarding"
    - path: "src/components/ui/FormField.test.tsx"
      provides: "RTL tests for label association + aria attributes"
  key_links:
    - from: "src/components/ui/FormField.tsx"
      to: "src/components/ui/Input.tsx"
      via: "import { Input } from './Input'"
      pattern: "from './Input'"
    - from: "src/components/ui/Input.tsx"
      to: "src/components/ui/cn.ts"
      via: "import { cn } from './cn'"
      pattern: "from './cn'"
---

<objective>
Add the `Input` and `FormField` UI primitives to `components/ui/`, mirroring the existing `Button.tsx` + `cn` convention (Tailwind v4, React 19 ref-as-prop — NO `forwardRef`). These primitives back the URL field on the Capture screen and every editable field on the Review screen.

Purpose: SETUP-05. Establishes the accessible form-input building blocks the rest of Phase 4 (CaptureUrlPage, ReviewPage) consumes.
Output: `Input.tsx`, `FormField.tsx` and their co-located RTL tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-url-first-capture/04-RESEARCH.md

<interfaces>
<!-- Mirror these exactly. Extracted from codebase — no exploration needed. -->

From src/components/ui/Button.tsx (the pattern to mirror — props-spread + cn + var(--color-*) tokens, NO forwardRef):
  export function Button({ variant = 'primary', size = 'md', className, ...props }: ButtonProps)
  variantClasses.secondary = 'bg-transparent border border-[var(--color-border)] hover:bg-[var(--color-muted)]'
  focus ring class in use: 'focus-visible:ring-2 focus-visible:ring-[var(--color-primary)]'

From src/components/ui/cn.ts:
  export function cn(...inputs: ClassValue[]): string   // twMerge(clsx(inputs))

Theme tokens available (index.css): --color-background, --color-foreground, --color-primary,
  --color-primary-foreground, --color-border, --color-muted
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Input primitive + tests</name>
  <files>src/components/ui/Input.tsx, src/components/ui/Input.test.tsx</files>
  <read_first>
    - src/components/ui/Button.tsx (exact mirror: function component, props spread, cn, var(--color-*) tokens, no forwardRef)
    - src/components/ui/cn.ts (the class merge helper to import)
    - .planning/phases/04-url-first-capture/04-RESEARCH.md Pattern 3 (Input primitive — React 19 ref as prop)
  </read_first>
  <behavior>
    - Test: renders an <input> element (getByRole('textbox')) — passes when Input is mounted with no props.
    - Test: forwards value + onChange — typing into the input fires onChange and reflects the controlled value.
    - Test: forwards placeholder — getByPlaceholderText finds the input.
    - Test: forwards disabled — the input is disabled when disabled prop is set.
    - Test: merges a custom className without dropping base classes (assert the element has both a base token class and the custom class).
  </behavior>
  <action>
    Create `Input` as a named function-component export (NOT default, NOT forwardRef — React 19 accepts `ref` as a plain prop). Props interface extends `InputHTMLAttributes<HTMLInputElement>` and adds an optional `ref?: Ref<HTMLInputElement>`. Destructure `{ ref, className, ...props }`, render a single `<input>` that spreads `...props`, sets `ref={ref}`, and applies `cn(...)` with: full-width rounded bordered input using `border-[var(--color-border)]`, `bg-[var(--color-background)]`, `text-sm`, padding `px-3 py-2`, height `h-10`, placeholder opacity, a focus-visible ring matching Button (`focus-visible:ring-2 focus-visible:ring-[var(--color-primary)]` plus `focus-visible:outline-none`), disabled styles (`disabled:opacity-50 disabled:cursor-not-allowed`), and `className` last so callers can override. Import `cn` from `./cn`. Import the `InputHTMLAttributes` and `Ref` types from `react` as type-only imports. Write the co-located test file with the five behavior cases above using @testing-library/react + userEvent (no router needed — these are leaf primitives).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/Input.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/Input.tsx` exists and exports a named `Input` function component (grep `export function Input`).
    - The file contains NO `forwardRef` (grep -c 'forwardRef' src/components/ui/Input.tsx returns 0).
    - The file imports `cn` from `./cn` (grep `from './cn'`).
    - `npx vitest run src/components/ui/Input.test.tsx` passes with at least the 5 behavior cases green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FormField primitive + tests</name>
  <files>src/components/ui/FormField.tsx, src/components/ui/FormField.test.tsx</files>
  <read_first>
    - src/components/ui/Input.tsx (from Task 1 — the input it wraps)
    - .planning/phases/04-url-first-capture/04-RESEARCH.md Pattern 4 (FormField primitive — htmlFor/id, aria-invalid, aria-describedby, role=alert)
  </read_first>
  <behavior>
    - Test: renders a <label> whose text is the `label` prop, associated with the input via htmlFor matching the input id (getByLabelText(label) returns the input).
    - Test: forwards input props (value, onChange, placeholder) down to the inner Input.
    - Test: when `error` is set, renders a paragraph with role=alert containing the error text AND the input has aria-invalid=true AND aria-describedby points at the error element id.
    - Test: when `helpText` is set and no error, renders help text linked via aria-describedby; aria-invalid is not true.
  </behavior>
  <action>
    Create `FormField` as a named function-component export. Props interface extends `InputHTMLAttributes<HTMLInputElement>` and adds required `id: string`, required `label: string`, optional `error?: string`, optional `helpText?: string`, optional `ref?: Ref<HTMLInputElement>`. Render a wrapping `div` (flex column, small gap), a `<label htmlFor={id}>` showing `label`, then the `Input` from `./Input` with `id={id}`, `ref={ref}`, `aria-invalid={!!error}`, and `aria-describedby` set to `${id}-error` when error present else `${id}-help` when helpText present else undefined. After the input, conditionally render the error paragraph (`id={`${id}-error`}`, `role="alert"`) when error is set, OR the help paragraph (`id={`${id}-help`}`) when helpText is set and no error. Apply an error border style to the Input via `cn(error && 'border-red-500 focus-visible:ring-red-500', className)`. Spread remaining `...inputProps` onto the Input. This is input-only (no textarea) per RESEARCH Open Question 5 (YAGNI for Phase 4). Write the co-located test file covering the four behavior cases.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/FormField.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/FormField.tsx` exists and exports a named `FormField` function component.
    - FormField imports `Input` from `./Input` (grep `from './Input'`).
    - Test asserting `getByLabelText(label)` returns the input passes (proves htmlFor/id association).
    - Test asserting role=alert + aria-invalid on error passes.
    - `npx vitest run src/components/ui/FormField.test.tsx` is fully green.
    - Full suite stays green: `npx vitest run` (no regressions in the existing 106 tests).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystrokes → controlled input | Untrusted text enters React state; rendered back as a value, never as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (XSS) | Input/FormField rendered values | mitigate | Values rendered only as React text nodes / the `value` attribute — auto-escaped by React. No `dangerouslySetInnerHTML` anywhere in these primitives. |
| T-04-02 | Information Disclosure | FormField error/help text | accept | Error/help strings are developer-authored constants, not user data; no sensitive info surfaced. |
| T-04-SC | Tampering | npm installs | accept | Zero new packages this plan (RESEARCH Package Legitimacy Audit: none). No install step → no supply-chain surface. |
</threat_model>

<verification>
- `npx vitest run src/components/ui` is fully green.
- `npx tsc -b` compiles with the two new primitives (ref-as-prop typed correctly under @types/react 19).
- No `forwardRef` usage introduced anywhere.
</verification>

<success_criteria>
SETUP-05 satisfied: `components/ui/` now contains `Input` and `FormField` primitives with passing accessibility-focused RTL tests. These back the capture/review forms (SC5).
</success_criteria>

<output>
Create `.planning/phases/04-url-first-capture/04-01-SUMMARY.md` when done.
</output>
