---
phase: 02-data-layer-pwa-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/common/requestState.ts
  - src/state/common/requestState.test.ts
  - src/state/common/assertNever.ts
  - src/state/common/assertNever.test.ts
  - src/config/appBrand.ts
  - src/config/appBrand.test.ts
  - src/config/publicEnv.ts
  - src/config/publicEnv.test.ts
autonomous: true
requirements: [SETUP-04]

must_haves:
  truths:
    - "requestState helpers (idle, loading, success, failure) construct correctly-typed RequestState<T> values"
    - "assertNever throws at runtime when passed any value"
    - "appBrand exposes name + themeColor; publicEnv is an object"
  artifacts:
    - path: "src/state/common/requestState.ts"
      provides: "RequestState<T> discriminated union + idle/loading/success/failure constructors"
      exports: ["RequestState", "idle", "loading", "success", "failure"]
    - path: "src/state/common/assertNever.ts"
      provides: "Exhaustiveness-check helper"
      exports: ["assertNever"]
    - path: "src/config/appBrand.ts"
      provides: "App brand constants"
      exports: ["appBrand"]
    - path: "src/config/publicEnv.ts"
      provides: "Public env var accessor placeholder"
      exports: ["publicEnv"]
  key_links:
    - from: "src/state/common/requestState.test.ts"
      to: "src/state/common/requestState.ts"
      via: "co-located Vitest import"
      pattern: "from './requestState'"
---

<objective>
Create the four SETUP-04 shared primitive modules — `requestState`, `assertNever`, `appBrand`, `publicEnv` — each with a co-located Vitest test. These are small, pure-TypeScript scaffolds the architecture template expects; later phases (state management, branding, env reads) build on them.

Purpose: Establish the shared-primitive layer the patrimonium-derived architecture expects, so Phase 3+ has typed request-state and exhaustiveness helpers ready.
Output: 4 source modules + 4 co-located test files under `src/state/common/` and `src/config/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer-pwa-shell/02-RESEARCH.md
@.planning/phases/02-data-layer-pwa-shell/02-VALIDATION.md

<interfaces>
<!-- Target module shapes — from RESEARCH.md Pattern 7. Executor implements these exactly. -->

src/state/common/requestState.ts exports:
  type RequestState<T> =
    | { status: 'idle' }
    | { status: 'loading' }
    | { status: 'success'; data: T }
    | { status: 'error'; error: Error }
  const idle: RequestState<never>      // { status: 'idle' } as const
  const loading: RequestState<never>   // { status: 'loading' } as const
  function success<T>(data: T): RequestState<T>
  function failure(error: Error): RequestState<never>

src/state/common/assertNever.ts exports:
  function assertNever(value: never): never   // throws Error with JSON.stringify(value)

src/config/appBrand.ts exports:
  const appBrand = { name, shortName, description, themeColor } as const
    // name/shortName 'Life Log', themeColor '#1e40af' (matches --color-primary in src/index.css)

src/config/publicEnv.ts exports:
  const publicEnv = {} as const   // extensible placeholder; no env vars in Phase 2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: state/common primitives (requestState + assertNever)</name>
  <files>src/state/common/requestState.ts, src/state/common/requestState.test.ts, src/state/common/assertNever.ts, src/state/common/assertNever.test.ts</files>
  <read_first>
    - .planning/phases/02-data-layer-pwa-shell/02-RESEARCH.md (Pattern 7 — exact module shapes)
    - .planning/phases/02-data-layer-pwa-shell/02-VALIDATION.md (Per-Task Verification Map row `setup04-modules`)
  </read_first>
  <behavior>
    - requestState: `success(42)` returns `{ status: 'success', data: 42 }`; `failure(new Error('x'))` returns `{ status: 'error', error }`; `idle` is `{ status: 'idle' }`; `loading` is `{ status: 'loading' }`.
    - assertNever: calling `assertNever(x)` throws an Error whose message includes the stringified value (cast through `as never` in the test to call it).
  </behavior>
  <action>
    Create `src/state/common/requestState.ts` exporting the `RequestState<T>` discriminated union (statuses: idle | loading | success | error) plus the constructors `idle`, `loading` (both `as const`), `success<T>(data)`, and `failure(error: Error)` exactly per the interfaces block. Create `src/state/common/assertNever.ts` exporting `assertNever(value: never): never` that throws `new Error(\`Unhandled discriminant: ${JSON.stringify(value)}\`)`. Write co-located tests asserting the constructor return shapes and that `assertNever` throws. Follow the Phase 1 test convention (Vitest globals, no extra setup needed for pure TS). Do NOT add external dependencies — these are pure TypeScript.
  </action>
  <acceptance_criteria>
    - Files `src/state/common/requestState.ts`, `requestState.test.ts`, `assertNever.ts`, `assertNever.test.ts` all exist.
    - `requestState.ts` exports `RequestState`, `idle`, `loading`, `success`, `failure`.
    - `assertNever.ts` exports `assertNever`.
    - `npx vitest run src/state/common` exits 0 with tests passing.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/state/common</automated>
  </verify>
  <done>Both modules exist with co-located tests green; `npx vitest run src/state/common` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config primitives (appBrand + publicEnv)</name>
  <files>src/config/appBrand.ts, src/config/appBrand.test.ts, src/config/publicEnv.ts, src/config/publicEnv.test.ts</files>
  <read_first>
    - .planning/phases/02-data-layer-pwa-shell/02-RESEARCH.md (Pattern 7 — appBrand / publicEnv shapes)
    - src/index.css (confirm the --color-primary token value the themeColor must match)
  </read_first>
  <behavior>
    - appBrand: `appBrand.name` is `'Life Log'`; `appBrand.themeColor` is a non-empty hex string (`'#1e40af'`).
    - publicEnv: `typeof publicEnv === 'object'` and it is not null.
  </behavior>
  <action>
    Create `src/config/appBrand.ts` exporting `const appBrand = { name: 'Life Log', shortName: 'Life Log', description: 'Capture structured life events locally and offline', themeColor: '#1e40af' } as const`. Create `src/config/publicEnv.ts` exporting `const publicEnv = {} as const` with a comment noting it is an extensible placeholder for future `import.meta.env` reads (no env vars in Phase 2). Write co-located tests asserting `appBrand.name` and a truthy `appBrand.themeColor`, and that `publicEnv` is an object. Do NOT import these into pwaConfig (PWA plan inlines its own brand constants per RESEARCH to stay within the node tsconfig scope).
  </action>
  <acceptance_criteria>
    - Files `src/config/appBrand.ts`, `appBrand.test.ts`, `publicEnv.ts`, `publicEnv.test.ts` all exist.
    - `appBrand.ts` exports `appBrand` with `name` and `themeColor`; `publicEnv.ts` exports `publicEnv`.
    - `npx vitest run src/config` exits 0 with tests passing.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/config</automated>
  </verify>
  <done>Both config modules exist with co-located tests green; `npx vitest run src/config` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-process TypeScript utilities; no input crosses a trust boundary, no I/O, no network, no storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | publicEnv accessor | accept | Empty object in Phase 2; future env reads must expose only non-secret `import.meta.env` keys (documented in the module comment). No secrets present now. |
| T-02-02 | Tampering | assertNever error message | accept | `JSON.stringify(value)` in the thrown message could echo arbitrary runtime input, but this path is dev-time exhaustiveness only and never reaches a user-facing surface. Low risk. |
</threat_model>

<verification>
- `npx vitest run src/state/common src/config` — all SETUP-04 module tests green.
- `npx tsc -b` exits 0 (types compile, no unused exports flagged).
</verification>

<success_criteria>
All four SETUP-04 modules exist with co-located passing tests; `npx vitest run src/state/common src/config` exits 0 and `npx tsc -b` exits 0.
</success_criteria>

<output>
Create `.planning/phases/02-data-layer-pwa-shell/02-01-SUMMARY.md` when done.
</output>
