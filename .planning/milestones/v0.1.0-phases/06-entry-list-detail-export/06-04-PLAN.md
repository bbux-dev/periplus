---
phase: 06-entry-list-detail-export
plan: 04
type: execute
wave: 2
depends_on: ["06-03"]
files_modified:
  - src/pages/EntryListPage.tsx
  - src/pages/EntryListPage.test.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/DashboardPage.test.tsx
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-04, EXP-01]
must_haves:
  truths:
    - "Entry List shows every saved entry with title, domain-scoped type label, date, and amount when present"
    - "User can filter the list by All / Media / Trips / Expenditures, narrowing to entry.domain"
    - "Empty state appears when there are no entries"
    - "Each row links to /entries/:id"
    - "An Export button exports all entries as JSON via buildExportJson + triggerDownload"
    - "Dashboard links to /entries so the list is reachable"
  artifacts:
    - path: "src/pages/EntryListPage.tsx"
      provides: "Reactive filtered entry list + export button"
      min_lines: 50
    - path: "src/pages/DashboardPage.tsx"
      provides: "View All Entries link to /entries"
      contains: "to=\"/entries\""
  key_links:
    - from: "src/pages/EntryListPage.tsx"
      to: "useEntries()"
      via: "reactive read"
      pattern: "useEntries\\("
    - from: "src/pages/EntryListPage.tsx"
      to: "/entries/:id"
      via: "row Link"
      pattern: "/entries/\\$\\{entry.id\\}|/entries/`"
    - from: "src/pages/EntryListPage.tsx"
      to: "buildExportJson / triggerDownload"
      via: "Export button onClick"
      pattern: "buildExportJson|triggerDownload"
    - from: "src/pages/DashboardPage.tsx"
      to: "/entries"
      via: "Link tile"
      pattern: "to=\"/entries\""
---

<objective>
Build `EntryListPage` at `/entries`: a reactive, filterable list of all saved entries with an export action, plus a Dashboard link so the list is reachable. Delivers VIEW-01 (list all), VIEW-02 (row fields + filter), VIEW-04 (list reads persisted IndexedDB — automated persistence proxy), and the EXP-01 user-facing export trigger.

Purpose: This is the browse surface of the milestone — it turns the append-only log into something the user can scan, filter, drill into, and export.
Output: `EntryListPage.tsx` (+ test) and a `/entries` link on `DashboardPage`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-entry-list-detail-export/06-RESEARCH.md

<interfaces>
<!-- From src/services/entriesRepository.ts -->
  useEntries(): LifeLogEntry[] | undefined   // undefined = loading; handle explicitly

<!-- From src/services/exportEntries.ts (plan 06-03) -->
  buildExportJson(entries: LifeLogEntry[], exportedAt: number): string
  triggerDownload(json: string, filename?: string): void

<!-- From src/config/navigation.ts -->
  NAVIGATION: DomainConfig[]   // order: media, trips, expenditures; each { domain, label, types[] }
  getDomainConfig(domain: string): DomainConfig | undefined
  // CRITICAL: 'expense' type exists in BOTH 'trips' and 'expenditures'. Type-label lookup MUST be domain-scoped.

<!-- From src/services/db.ts -->
  type EntryDomain = 'media' | 'trips' | 'expenditures'
  LifeLogEntry fields used here: id, domain, type, title, occurredAt?, recordedAt, amount?

<!-- Layout/style precedent: src/pages/DomainPage.tsx (Link tile), src/pages/DashboardPage.tsx (page shell). -->
<!-- cn helper: import { cn } from '../components/ui/cn' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EntryListPage — reactive filtered list + export button + tests</name>
  <files>src/pages/EntryListPage.tsx, src/pages/EntryListPage.test.tsx</files>
  <read_first>
    - .planning/phases/06-entry-list-detail-export/06-RESEARCH.md (Patterns 1-4: filter state, domain-scoped type label, date display, EntryRow; Pitfalls 1-2)
    - src/pages/DomainPage.tsx (page shell + Link tile className precedent)
    - src/services/entriesRepository.ts (useEntries contract)
    - src/services/exportEntries.ts (buildExportJson, triggerDownload — from 06-03)
    - src/config/navigation.ts (NAVIGATION, getDomainConfig, the 'expense' dual-domain note)
    - src/services/entriesRepository.test.tsx (fake-indexeddb seed pattern: beforeEach db reset + entriesRepository.create + act/findBy)
  </read_first>
  <behavior>
    - Rendering with two seeded entries (e.g. a media book and a trips expense with amount) shows both titles, each type label, a date string, and the amount for the expense row
    - Clicking the "Trips" filter button hides the media entry and keeps the trips entry
    - Clicking "All" shows every entry again
    - Rendering with zero entries shows the empty-state text "No entries yet."
    - Clicking "Export JSON" invokes triggerDownload exactly once with a JSON string containing all entry titles
  </behavior>
  <action>
    Create `src/pages/EntryListPage.tsx` exporting `function EntryListPage()`. Use the page shell from DomainPage (min-h-screen flex column, max-w-sm centered) and render an `<h1>` with text `Entries`. Read entries with `useEntries()`; if it is `undefined`, return a `Loading...` paragraph (gate before any `.map`/`.filter` — Pitfall 1).
    Define `type FilterKey = 'all' | EntryDomain` and a module-level `FILTER_OPTIONS` array derived from `NAVIGATION` — `[{ value: 'all', label: 'All' }, ...NAVIGATION.map(d => ({ value: d.domain as FilterKey, label: d.label }))]` so order/labels stay in sync. Hold filter state with `useState<FilterKey>('all')`. Render the filter as a `<div role="group" aria-label="Filter entries">` of `<button>`s, each with `aria-pressed={filter === opt.value}` and text `opt.label`; clicking sets the filter. Compute `const filtered = filter === 'all' ? entries : entries.filter(e => e.domain === filter)`.
    For the type label, write a helper `getTypeLabel(domain, type)` = `getDomainConfig(domain)?.types.find(t => t.type === type)?.label ?? type` — DOMAIN-SCOPED; do NOT flat-map across all domains (the 'expense' hazard). For the date, compute `new Date(entry.occurredAt ?? entry.recordedAt).toLocaleDateString()`; in tests assert presence via a `data-testid="entry-date"` rather than an exact string (Pitfall 2). Render each entry as a row inside a `<ul>` where each `<li>` contains a react-router `Link to={`/entries/${entry.id}`}` showing title, `getTypeLabel(...)`, the date, and — only when `entry.amount != null` — `$${entry.amount.toFixed(2)}`. When `filtered.length === 0`, render `No entries yet.` instead of the list.
    Add an `Export JSON` button (use the existing Button primitive or a styled `<button>`) whose onClick calls `triggerDownload(buildExportJson(entries, Date.now()))`. Export operates on the full unfiltered `entries`.
    Create `src/pages/EntryListPage.test.tsx`: wrap renders in `<MemoryRouter>`; reset+open the db in `beforeEach`; seed entries with `entriesRepository.create`. Cover every behavior case. For the export test, `vi.mock('../services/exportEntries')` or `vi.spyOn` `triggerDownload` and assert it was called once with a string containing the seeded titles; `vi.restoreAllMocks()` in afterEach. Use `await screen.findBy*` to clear the loading frame.
  </action>
  <acceptance_criteria>
    - `EntryListPage.tsx` exports `EntryListPage` and renders an `<h1>` containing "Entries".
    - Type label uses `getDomainConfig(entry.domain)?.types.find(...)` — grep `grep -c 'flatMap' src/pages/EntryListPage.tsx` returns 0.
    - Filter buttons carry `aria-pressed`; the group has `aria-label="Filter entries"`.
    - Rows are react-router `Link`s to `/entries/${entry.id}`.
    - Amount renders only when `entry.amount != null` (gated, not always shown).
    - Empty state text is exactly `No entries yet.`.
    - Export button onClick calls `triggerDownload(buildExportJson(entries, Date.now()))`.
    - `npx vitest run src/pages/EntryListPage.test.tsx` exits 0, covering: all-entries render, filter-narrows-by-domain, all-shows-all, empty state, and export-calls-triggerDownload.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/EntryListPage.test.tsx</automated>
  </verify>
  <done>EntryListPage lists/filters all entries with correct row fields, shows the empty state, links rows to detail, and exports JSON; all tests pass (VIEW-01/02/04 + EXP-01 trigger).</done>
</task>

<task type="auto">
  <name>Task 2: Add a "View All Entries" link to /entries on the Dashboard</name>
  <files>src/pages/DashboardPage.tsx, src/pages/DashboardPage.test.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (full file — add the link after the NAVIGATION.map block)
    - src/pages/DashboardPage.test.tsx (the link-href assertion style to extend)
    - .planning/phases/06-entry-list-detail-export/06-RESEARCH.md (Pattern 8; Assumption A1 about icon name)
  </read_first>
  <action>
    In `src/pages/DashboardPage.tsx`, after the `NAVIGATION.map(...)` tile block, add a react-router `Link to="/entries"` styled with the same tile className as the domain tiles, containing an icon and the text `View All Entries`. Use `QueueListIcon` from `@heroicons/react/24/outline`; if that export does not exist at the installed heroicons version, fall back to `ListBulletIcon` or `Bars3Icon` (all are in the 24/outline set) — verify the import resolves before committing.
    In `src/pages/DashboardPage.test.tsx`, add a test asserting a link whose text includes `View All Entries` has `getAttribute('href') === '/entries'` (mirror the existing Media-link href assertion). Do not break the existing domain-link assertions.
  </action>
  <acceptance_criteria>
    - `DashboardPage.tsx` contains `to="/entries"` and the text `View All Entries`.
    - The heroicons import used resolves (no TS/Vite import error — confirmed by the build/test run).
    - `DashboardPage.test.tsx` asserts the `/entries` link href.
    - `npx vitest run src/pages/DashboardPage.test.tsx` exits 0; existing domain-tile assertions still pass.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/DashboardPage.test.tsx</automated>
  </verify>
  <done>Dashboard renders a working link to /entries; its test asserts the href; existing Dashboard tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB entries → list DOM | Stored entry fields (title, amount, type) are rendered as React-escaped text — no raw HTML |
| IndexedDB entries → local file | Export button serializes all entries to a local download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | list row rendering | mitigate | All entry fields render as JSX text nodes (React-escaped); no `dangerouslySetInnerHTML`. Row links target `/entries/${id}` internal routes only (no sourceUrl href here) |
| T-06-03 | Information Disclosure | Export button | accept | Exports the user's own local data to their own device; no network egress |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- `npx vitest run src/pages/EntryListPage.test.tsx src/pages/DashboardPage.test.tsx` green.
- Wave merge: `npx vitest run && npx tsc -b && npx vite build` green.
- Manual-only (per 06-VALIDATION): true browser hard-refresh persistence (VIEW-04), real .json download (EXP-01), phone-viewport layout.
</verification>

<success_criteria>
The Entry List shows all saved entries (title/type/date/amount), filters by All/Media/Trips/Expenditures, links each row to detail, exports all entries as JSON, and is reachable from the Dashboard.
</success_criteria>

<output>
Create `.planning/phases/06-entry-list-detail-export/06-04-SUMMARY.md` when done.
</output>
