---
phase: 01-foundation-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - eslint.config.mjs
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - src/test-setup.ts
  - src/pages/.gitkeep
  - src/components/ui/.gitkeep
  - src/services/.gitkeep
  - src/state/common/.gitkeep
  - src/config/.gitkeep
  - src/pwa/.gitkeep
  - src/assets/.gitkeep
autonomous: false
requirements: [SETUP-01, SETUP-02]
must_haves:
  truths:
    - "Running the dev server serves the app without errors"
    - "tsc -b && vite build exits 0 with the template directory layout present"
    - "Tailwind v4 utility classes are compiled and applied (no tailwind.config.js)"
    - "Vitest runs against jsdom with fake-indexeddb registered as a global"
  artifacts:
    - path: "vite.config.ts"
      provides: "react() + tailwindcss() plugins and the Vitest test block"
      contains: "tailwindcss"
    - path: "src/index.css"
      provides: "Tailwind v4 CSS-first entry with theme tokens"
      contains: "@import \"tailwindcss\""
    - path: "src/test-setup.ts"
      provides: "fake-indexeddb + jest-dom test bootstrap"
      contains: "fake-indexeddb/auto"
    - path: "tsconfig.json"
      provides: "TypeScript project references (app + node)"
      contains: "references"
  key_links:
    - from: "src/main.tsx"
      to: "src/index.css"
      via: "import './index.css'"
      pattern: "import.*index\\.css"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "tailwindcss() plugin"
      pattern: "tailwindcss\\(\\)"
---

<objective>
Scaffold a brand-new Vite 7 + React 19 + TypeScript 5.9 app at the repo root on the LOCKED stack, wire Tailwind CSS v4 (CSS-first, no config file), create the required template directory skeleton, and stand up the Vitest + fake-indexeddb test harness that the rest of Phase 1 validates against.

Purpose: Everything else in this milestone depends on this foundation existing and building cleanly. This is Wave 0 (test infra) + Wave 1 (scaffold) combined into the bootstrap plan.
Output: A runnable, type-checking, building app shell skeleton with empty placeholder directories and a green (empty) test run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-app-shell/01-RESEARCH.md
@.planning/phases/01-foundation-app-shell/01-CONTEXT.md
@.planning/phases/01-foundation-app-shell/01-VALIDATION.md

<interfaces>
<!-- Exact locked versions and config from RESEARCH.md. Executor uses these directly. -->

Production deps (install exactly):
  react-router-dom dexie dexie-react-hooks @heroicons/react clsx tailwind-merge

Dev deps:
  typescript@~5.9.3 (bump from scaffold default ~5.8.3)
  tailwindcss @tailwindcss/vite
  vitest jsdom fake-indexeddb @testing-library/react @testing-library/jest-dom @testing-library/user-event

vite.config.ts shape: import { defineConfig } from 'vitest/config'; plugins [react(), tailwindcss()];
  test: { environment: 'jsdom', globals: true, setupFiles: ['./src/test-setup.ts'] }

src/index.css: `@import "tailwindcss";` then `@theme { ... }` with --color-* and --font-sans tokens
  (v4 tokens hold FULL values e.g. hsl(222,89%,40%), referenced as var(--color-primary) — NOT hsl(var(...)))

src/test-setup.ts: `import 'fake-indexeddb/auto';` then `import '@testing-library/jest-dom';`
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (pre-install)</name>
  <read_first>
    - .planning/phases/01-foundation-app-shell/01-RESEARCH.md (## Package Legitimacy Audit — all packages tagged [ASSUMED]; slopcheck was unavailable)
  </read_first>
  <files>none (verification gate — no files modified)</files>
  <what-built>Nothing yet — this gate runs BEFORE the first npm install batch. RESEARCH.md could not run slopcheck, so every package is tagged [ASSUMED] and must be confirmed legitimate before installation. This is threat T-01-SC (supply-chain tampering).</what-built>
  <action>Pause for human verification of package legitimacy before any npm install runs. Present the RESEARCH.md "Package Legitimacy Audit" table and the spot-check list below; do not run installs until the user approves. This gate is NOT auto-approvable.</action>
  <how-to-verify>
    1. Open the RESEARCH.md "Package Legitimacy Audit" table.
    2. Spot-check the non-obvious packages against their official registry pages:
       - dexie → npmjs.com/package/dexie (repo github.com/dexie/Dexie.js)
       - dexie-react-hooks → npmjs.com/package/dexie-react-hooks
       - @tailwindcss/vite → npmjs.com/package/@tailwindcss/vite (org tailwindlabs)
       - fake-indexeddb → npmjs.com/package/fake-indexeddb (repo github.com/dumbmatter/fakeIndexedDB)
       - tailwind-merge → npmjs.com/package/tailwind-merge (repo github.com/dcastil/tailwind-merge)
    3. Confirm each is the well-known maintainer, not a typosquat, and 2+ years old.
  </how-to-verify>
  <verify><human-check>User confirms all packages legitimate, or names packages to flag/replace.</human-check></verify>
  <acceptance_criteria>
    - User confirms all packages are legitimate, OR names any package to remove/replace before install proceeds.
    - This checkpoint is NOT auto-approvable (legitimacy gates ignore auto_advance).
  </acceptance_criteria>
  <done>Human has approved the package set (or flagged replacements) and installs may proceed.</done>
  <resume-signal>Type "approved" to proceed with installs, or name packages to flag.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold the locked stack, wire Tailwind v4, build the directory skeleton, stand up the test harness</name>
  <read_first>
    - .planning/phases/01-foundation-app-shell/01-RESEARCH.md (Installation steps, Pattern 1 project references, Pattern 2 Tailwind v4 config, Validation Architecture / Dexie test setup pattern)
    - .planning/phases/01-foundation-app-shell/01-VALIDATION.md (## Wave 0 Requirements, ## Test Infrastructure)
  </read_first>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, src/main.tsx, src/App.tsx, src/index.css, src/test-setup.ts, and placeholder dirs under src/</files>
  <action>
    Scaffold and configure in this order (the ordering avoids the Vite-major and TS-drift pitfalls in RESEARCH.md):

    1. Scaffold into the repo root: run `npm create vite@7 . -- --template react-ts`. Use `@7` explicitly — the `latest` tag now pulls create-vite@8 (Vite 8 / TS 6) which breaks the lock. Accept overwrite of the empty repo root (docs/, spec.md, .planning/ already present — do not delete them).
    2. Bump TypeScript to the locked version: `npm install --save-dev typescript@~5.9.3` (scaffold default is ~5.8.3). Confirm `npx tsc --version` reports 5.9.x.
    3. Install production deps in one batch: `npm install react-router-dom dexie dexie-react-hooks @heroicons/react clsx tailwind-merge`.
    4. Install Tailwind v4: `npm install --save-dev tailwindcss @tailwindcss/vite`.
    5. Install test tooling: `npm install --save-dev vitest jsdom fake-indexeddb @testing-library/react @testing-library/jest-dom @testing-library/user-event`.
    6. Wire vite.config.ts per the interfaces block: import defineConfig from `vitest/config` (not `vite`), add `react()` and `tailwindcss()` plugins, and a `test` block with `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./src/test-setup.ts']`.
    7. Convert src/index.css to Tailwind v4 CSS-first: single `@import "tailwindcss";` line, then a `@theme { }` block defining --color-primary, --color-primary-foreground, --color-background, --color-foreground, --color-muted, --color-border, and --font-sans. Tokens hold FULL CSS values (e.g. hsl(222, 89%, 40%)). Do NOT create tailwind.config.js or postcss.config.js, and do NOT use `@tailwind base/components/utilities` directives.
    8. Ensure src/main.tsx imports './index.css' and renders <App />. Replace the scaffold's demo App.tsx with a minimal placeholder that renders a single element (e.g. a div containing "Life Log") so the build is green — routing and the real WelcomePage land in plan 03. Remove the scaffold's demo counter/logo assets from App.tsx.
    9. Create src/test-setup.ts with `import 'fake-indexeddb/auto';` then `import '@testing-library/jest-dom';` (fake-indexeddb import MUST come first).
    10. Create the required directory skeleton as empty placeholders with .gitkeep files: src/pages, src/components/ui, src/services, src/state/common, src/config, src/pwa, src/assets. Per RESEARCH.md these stay empty in Phase 1 — do NOT install vite-plugin-pwa.
  </action>
  <verify>
    <automated>npx tsc -b && npx vite build && test -d src/pages && test -d src/components/ui && test -d src/services && test -d src/state/common && test -d src/config && test -d src/pwa && test -d src/assets && grep -q 'tailwindcss' vite.config.ts && grep -q '@import "tailwindcss"' src/index.css && grep -q 'fake-indexeddb/auto' src/test-setup.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` exits 0 (project references resolve; no type errors).
    - `npx vite build` exits 0 and emits dist/.
    - `npx tsc --version` reports 5.9.x; `node_modules/vite/package.json` version is 7.x.
    - All seven template directories exist under src/.
    - No tailwind.config.js or postcss.config.js exists; index.css uses `@import "tailwindcss"`.
    - `npx vitest run` exits 0 (no tests yet, but the harness loads fake-indexeddb + jsdom without error).
    - vite-plugin-pwa is NOT in package.json.
  </acceptance_criteria>
  <done>App scaffolds, type-checks, and builds on the locked stack; Tailwind v4 is wired CSS-first; the template directory skeleton and Vitest/fake-indexeddb harness are in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party package code is installed and executed at build/runtime; the only externally-sourced trust boundary in this phase |
| (no others) | 100% local-first, offline, single-user app — no backend, no auth, no network requests, no PII, no user-supplied strings in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (all packages [ASSUMED] — slopcheck unavailable) | mitigate | Blocking human-verify legitimacy gate (Task 1) before any install batch; verified-maintainer + age + empty postinstall signals per RESEARCH.md |
| T-01-01 | Tampering (DOM XSS) | React rendering | accept | No user-supplied string input in Phase 1; counter value is an integer driven only by +/- buttons; React escapes by default |
| T-01-02 | Information Disclosure | IndexedDB store | accept | Single-user local device storage; no PII, no secrets, no network transmission |
</threat_model>

<verification>
- `npx tsc -b && npx vite build` exits 0.
- Seven template directories present under src/.
- `npx vitest run` loads the harness cleanly.
- No tailwind.config.js / postcss.config.js / vite-plugin-pwa.
</verification>

<success_criteria>
The locked stack is scaffolded at the repo root, type-checks and builds, Tailwind v4 is wired CSS-first, the template directory skeleton exists, and the Vitest + fake-indexeddb harness runs green. (SETUP-01, SETUP-02)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-app-shell/01-01-SUMMARY.md` when done.
</output>
