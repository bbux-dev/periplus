---
phase: 01-foundation-app-shell
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/ui/cn.ts
  - src/components/ui/Button.tsx
  - src/components/ui/cn.test.ts
  - src/services/db.ts
  - src/services/db.test.ts
autonomous: true
requirements: [SETUP-03, DEMO-01]
must_haves:
  truths:
    - "cn() merges conflicting Tailwind classes with last-wins semantics"
    - "Button renders with the requested variant and size classes and forwards onClick/aria-label"
    - "db.counter.put({id:1,value:N}) upserts a single counter row keyed by id=1"
    - "db.counter.get(1) returns the persisted value after a put"
  artifacts:
    - path: "src/components/ui/cn.ts"
      provides: "clsx + tailwind-merge class composer"
      exports: ["cn"]
    - path: "src/components/ui/Button.tsx"
      provides: "Button primitive with variant/size Record lookup referencing theme tokens"
      exports: ["Button"]
    - path: "src/services/db.ts"
      provides: "Dexie LifeLogDB with version(1) counter store keyed by id"
      exports: ["db"]
      contains: "counter: 'id'"
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/components/ui/cn.ts"
      via: "import { cn }"
      pattern: "from '\\./cn'"
    - from: "src/services/db.ts"
      to: "dexie"
      via: "Dexie subclass + EntityTable"
      pattern: "extends Dexie"
---

<objective>
Build the two foundation pieces the tracer slice depends on: the `cn` class-composition helper plus a `Button` primitive (SETUP-03), and the Dexie counter store keyed at a fixed id=1 (the data half of DEMO-01). Both are unit-tested with Vitest (Button/cn via class-string assertions, db via fake-indexeddb put/get).

Purpose: These are independent leaf modules that everything in plan 03 (the Counter UI + welcome screen) imports. Defining them first hands plan 03 their contracts directly.
Output: Tested `cn`, `Button`, and Dexie `db` modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-app-shell/01-RESEARCH.md
@.planning/phases/01-foundation-app-shell/01-VALIDATION.md

<interfaces>
<!-- Exact contracts from RESEARCH.md Patterns 3 & 5. Implement against these signatures. -->

cn (src/components/ui/cn.ts):
  export function cn(...inputs: ClassValue[]): string  // twMerge(clsx(inputs))

Button (src/components/ui/Button.tsx):
  variantClasses Record<string,string>: primary | secondary | ghost  (reference var(--color-*) tokens)
  sizeClasses Record<string,string>:    sm | md | lg | icon  (icon = h-10 w-10 p-0)
  interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
    variant?: keyof typeof variantClasses; size?: keyof typeof sizeClasses;
  }
  defaults: variant='primary', size='md'; spreads {...props}; merges className via cn()

db (src/services/db.ts):
  interface Counter { id: number; value: number }     // id is always 1, plain key (NOT ++id)
  class LifeLogDB extends Dexie { counter!: EntityTable<Counter,'id'> }
  constructor: super('LifeLogDB'); this.version(1).stores({ counter: 'id' })
  export const db = new LifeLogDB()
  // Phase 2 will add version(2) with entries+settings — do NOT mutate version(1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cn helper + Button primitive (SETUP-03)</name>
  <read_first>
    - .planning/phases/01-foundation-app-shell/01-RESEARCH.md (Pattern 5: cn Helper + Button Primitive — exact variant/size Records and token references; Anti-Patterns: use var(--color-*) not hsl(var(...)))
    - src/index.css (the --color-* theme tokens defined in plan 01, so Button references existing tokens)
  </read_first>
  <files>src/components/ui/cn.ts, src/components/ui/Button.tsx, src/components/ui/cn.test.ts</files>
  <behavior>
    - cn('px-2','px-4') returns 'px-4' (tailwind-merge last-wins on conflicting utilities).
    - cn('a', false && 'b', 'c') returns 'a c' (clsx drops falsy entries).
    - Button with variant='ghost' size='icon' renders a button whose className contains the ghost + icon classes.
    - Button forwards onClick (clicking fires the handler) and arbitrary props like aria-label.
    - Button default (no props) uses variant='primary' size='md'.
  </behavior>
  <action>
    Create cn.ts as `twMerge(clsx(inputs))` importing `clsx, type ClassValue` from clsx and `twMerge` from tailwind-merge. Create Button.tsx using the variant/size `Record<string,string>` lookup from the interfaces block; variant classes reference theme tokens via `var(--color-primary)` etc. (NOT the v3 `hsl(var(...))` form); the component spreads `{...props}` onto a native button and merges the incoming `className` through `cn()`. No class-variance-authority dependency. Write cn.test.ts covering the cn merge/falsy cases and a couple of Button render assertions (use @testing-library/react render + screen).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui</automated>
  </verify>
  <acceptance_criteria>
    - cn.ts exports `cn`; Button.tsx exports `Button`; Button imports cn from './cn'.
    - `npx vitest run src/components/ui` passes (cn merge + Button render cases green).
    - Button variant classes reference `var(--color-*)` tokens, not `hsl(var(...))`.
    - `npx tsc -b` still exits 0.
  </acceptance_criteria>
  <done>cn merges classes correctly and Button renders variant/size classes while forwarding native button props — both verified by passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dexie counter store (DEMO-01 data layer)</name>
  <read_first>
    - .planning/phases/01-foundation-app-shell/01-RESEARCH.md (Pattern 3: Dexie Counter Store; Anti-Patterns: use `id` not `++id`; Pitfall 6: never mutate version(1); Dexie Test Setup Pattern)
    - src/test-setup.ts (fake-indexeddb/auto is registered globally by plan 01)
  </read_first>
  <files>src/services/db.ts, src/services/db.test.ts</files>
  <behavior>
    - put({id:1,value:0}) then get(1) returns a row with value 0 (creates the row when absent).
    - put({id:1,value:3}) then put({id:1,value:7}) then get(1) returns value 7 (upsert overwrites, not duplicates).
    - count() of the counter table after multiple puts at id=1 is 1 (single fixed row).
  </behavior>
  <action>
    Create db.ts: a `LifeLogDB extends Dexie` class with `counter!: EntityTable<Counter,'id'>` (Counter = {id:number; value:number}), constructor calls `super('LifeLogDB')` and `this.version(1).stores({ counter: 'id' })` — plain `id` key, NOT `++id` (the counter owns a stable id=1 row updated in place). Export `const db = new LifeLogDB()`. Write db.test.ts importing db; in `beforeEach` call `await db.delete(); await db.open();` to isolate state between tests (relies on plan 01's fake-indexeddb/auto setup). Cover the create, upsert-overwrite, and single-row behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/services</automated>
  </verify>
  <acceptance_criteria>
    - db.ts exports `db`; schema string is `counter: 'id'` (no `++`).
    - `npx vitest run src/services` passes (create + upsert + single-row cases green).
    - version(1) defines only the counter store (entries/settings deferred to Phase 2).
    - `npx tsc -b` still exits 0.
  </acceptance_criteria>
  <done>The Dexie counter store upserts a single id=1 row and reads it back, verified against fake-indexeddb.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local-first, offline, single-user. db.ts reads/writes same-origin IndexedDB only; cn/Button are pure presentation with no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | IndexedDB counter store | accept | Single-user local device storage; integer value only, no PII or secrets, no network transmission |
| T-01-03 | Tampering (DOM XSS) | Button className passthrough | accept | className is developer-supplied (not user input); React escapes children; no dangerouslySetInnerHTML |
</threat_model>

<verification>
- `npx vitest run src/components/ui` and `npx vitest run src/services` both pass.
- `npx tsc -b` exits 0.
</verification>

<success_criteria>
`cn` and `Button` exist and are unit-tested (SETUP-03); the Dexie counter store upserts and reads a single id=1 row against fake-indexeddb (DEMO-01 data layer).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-app-shell/01-02-SUMMARY.md` when done.
</output>
