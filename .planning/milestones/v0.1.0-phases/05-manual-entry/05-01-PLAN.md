---
phase: 05-manual-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/extractMetadataFromUrl.ts
  - src/config/entryFields.ts
  - src/config/entryFields.test.ts
  - src/pages/ReviewPage.tsx
autonomous: true
requirements: [MAN-02, MAN-03]

must_haves:
  truths:
    - "ENTRY_FIELDS covers all 7 EntryTypes (show, movie, book, podcast, place, event, expense) with type-appropriate FieldDescriptors"
    - "buildReviewDraft maps expense amount→core, place name→core.title and address→core.location, media creator/rating→metadata, and skips empty fields"
    - "ReviewPage persists amount, occurredAt, description, and parsed tags from the richer ReviewDraft (tags no longer hardcoded to [])"
    - "Phase 4 ReviewPage and capture tests pass UNCHANGED — ExtractedDraft remains structurally assignable to ReviewDraft"
  artifacts:
    - path: "src/config/entryFields.ts"
      provides: "ENTRY_FIELDS config, FieldDescriptor/FieldMapping types, buildReviewDraft mapper"
      contains: "ENTRY_FIELDS"
      min_lines: 90
    - path: "src/config/entryFields.test.ts"
      provides: "Unit tests: all 7 types covered + buildReviewDraft mapping/skip rules"
    - path: "src/services/extractMetadataFromUrl.ts"
      provides: "ReviewDraft interface (richer draft contract); ExtractedDraft unchanged"
      contains: "interface ReviewDraft"
    - path: "src/pages/ReviewPage.tsx"
      provides: "Option A-light extension: ReviewDraft cast + amount/occurredAt/tags state + handleSave maps them"
      contains: "ReviewDraft"
  key_links:
    - from: "src/config/entryFields.ts"
      to: "src/services/extractMetadataFromUrl.ts (ReviewDraft)"
      via: "import type { ReviewDraft }"
      pattern: "import type \\{ ReviewDraft \\}"
    - from: "src/pages/ReviewPage.tsx"
      to: "ReviewDraft"
      via: "location.state cast to { draft?: ReviewDraft }"
      pattern: "draft\\?: ReviewDraft"
    - from: "src/pages/ReviewPage.tsx handleSave"
      to: "LifeLogEntry.tags / amount / occurredAt"
      via: "parsedTags / parsedAmount / parsedDate"
      pattern: "parsedTags"
---

<objective>
Establish the shared Phase 5 contract layer: the `ReviewDraft` type, the `ENTRY_FIELDS` per-type field config with its `buildReviewDraft` mapper, and the minimal "Option A-light" extension to `ReviewPage` so manual-entry fields (amount, occurredAt, description, tags) review and persist.

Purpose: This is the seam consumed by Plan 02's ManualEntryPage. It is the single source of truth for what fields each EntryType shows and how each maps onto `LifeLogEntry`. It must not regress the Phase 4 URL-capture path.
Output: `src/config/entryFields.ts` (+ test), the `ReviewDraft` interface added to `extractMetadataFromUrl.ts`, and an extended `ReviewPage.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-manual-entry/05-RESEARCH.md
@.planning/phases/05-manual-entry/05-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Already extracted — no codebase exploration required. -->

From src/services/db.ts:
- `export type EntryDomain = 'media' | 'trips' | 'expenditures'`
- `export type EntryType = 'show' | 'movie' | 'book' | 'podcast' | 'place' | 'event' | 'expense'`
- `LifeLogEntry` core fields: id, domain, type, title:string, description?:string, occurredAt?:number (epoch ms), recordedAt:number, sourceUrl?:string, amount?:number, location?:string, tags:string[], metadata:Record<string,unknown>, syncedAt:number|null

From src/services/extractMetadataFromUrl.ts (UNCHANGED, the return type of extractMetadataFromUrl):
- `interface ExtractedDraft { sourceUrl: string; title?: string; location?: string; metadata: Record<string, unknown> }`

ReviewDraft (NEW — to be added in Task 1, per RESEARCH.md §"Code Examples" item 1, lines 219-228):
- `interface ReviewDraft { sourceUrl?: string; title?: string; location?: string; description?: string; occurredAt?: number; amount?: number; tags?: string[]; metadata: Record<string, unknown> }`
- ExtractedDraft is structurally assignable to ReviewDraft (only `metadata` is required; all new fields optional). Verified in RESEARCH.md lines 161-164.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ReviewDraft type + create entryFields config and unit tests</name>
  <read_first>
    - .planning/phases/05-manual-entry/05-RESEARCH.md §"Exact Field → LifeLogEntry Mapping Table" (lines 176-208) and §"Code Examples" items 1-2 (lines 213-383) — the AUTHORITATIVE copy-pasteable config + mapper
    - .planning/phases/05-manual-entry/05-VALIDATION.md §"Per-Task Verification Map" (entry-fields row)
    - src/services/db.ts (EntryType, EntryDomain, LifeLogEntry — mapping targets)
    - src/services/extractMetadataFromUrl.ts (ExtractedDraft lives here; ReviewDraft is added here)
  </read_first>
  <files>src/services/extractMetadataFromUrl.ts, src/config/entryFields.ts, src/config/entryFields.test.ts</files>
  <action>
    Add the `ReviewDraft` interface to src/services/extractMetadataFromUrl.ts exactly as specified in RESEARCH.md lines 219-228 (fields: optional sourceUrl, title, location, description, occurredAt, amount, tags; required metadata). Export it. Do NOT modify the existing `ExtractedDraft` interface or any extractor function — ExtractedDraft must remain the return type of extractMetadataFromUrl and stay structurally assignable to ReviewDraft.

    Create src/config/entryFields.ts containing, copied exactly from RESEARCH.md lines 238-382: the `FieldInputType` union ('text' | 'number' | 'date' | 'tags'), the `FieldMapping` discriminated union ({ kind:'core'; field:'title'|'description'|'occurredAt'|'amount'|'location'|'tags' } | { kind:'metadata'; key:string }), the `FieldDescriptor` interface (key, label, inputType, placeholder?, required?, mapTo), the `ENTRY_FIELDS: Record<EntryType, FieldDescriptor[]>` config for ALL 7 types, and the `buildReviewDraft(fields, formValues)` mapper. Import EntryType from ../services/db and ReviewDraft from ../services/extractMetadataFromUrl. Honor the mapping table EXACTLY: expense.amount→core amount; place.name→core title (label "Name"); place.address→core location (label "Address"); media creator→metadata.creator (labels Creator/Director/Author/Host per type); rating→metadata.rating; currency/category/merchant→metadata; title/description/occurredAt/tags→core; description label is "Notes". buildReviewDraft rules: trim values, skip empty, parseFloat for amount/number-metadata (skip NaN amount), Date.parse for occurredAt date strings (skip NaN), split tags on comma+trim+filter Boolean. Do NOT invent a different mapping or labels.

    Create src/config/entryFields.test.ts covering the unit expectations in RESEARCH.md §"Phase Requirements → Test Map" (lines 642-645): (a) ENTRY_FIELDS has an entry for every one of the 7 EntryType values; (b) buildReviewDraft on expense fields with amount '15.5' + currency 'USD' yields draft.amount===15.5 (a number) and draft.metadata.currency==='USD'; (c) buildReviewDraft on place fields with name 'Eiffel Tower' + address '...' yields draft.title==='Eiffel Tower' and draft.location set; (d) empty/whitespace form values are skipped (resulting fields undefined); (e) a non-numeric amount yields no draft.amount.
  </action>
  <acceptance_criteria>
    - src/services/extractMetadataFromUrl.ts exports `interface ReviewDraft` with optional sourceUrl/title/location/description/occurredAt/amount/tags and required metadata; ExtractedDraft is byte-for-byte unchanged.
    - src/config/entryFields.ts exports ENTRY_FIELDS, FieldDescriptor, FieldInputType, FieldMapping, and buildReviewDraft. ENTRY_FIELDS keys are exactly: show, movie, book, podcast, place, event, expense.
    - Field labels match the mapping table: book creator label is "Author"; movie creator label is "Director"; podcast creator label is "Host"; show creator label is "Creator"; place uses "Name" (→title) and "Address" (→location); expense exposes "Amount" (→core amount), "Currency", "Category", "Merchant".
    - buildReviewDraft maps amount/occurredAt/title/description/location/tags to core; currency/category/merchant/creator/rating to metadata; number metadata stored as JS number; empty values skipped; NaN amount/date skipped.
    - `npx vitest run src/config/entryFields.test.ts` passes (all 7 types asserted; expense amount→core; place name→title/address→location; empty-field skip).
    - `npx tsc -b` reports no type errors.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/config/entryFields.test.ts</automated>
  </verify>
  <done>entryFields.ts + ReviewDraft exist, all 7 types covered, mapper unit tests green, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ReviewPage (Option A-light) and preserve Phase 4 tests</name>
  <read_first>
    - .planning/phases/05-manual-entry/05-RESEARCH.md §"The KEY Decision: ReviewPage Integration" (lines 153-173) and §"Code Examples" item 4 "ReviewPage changes — exact diff description" (lines 449-547) — the exact edits
    - .planning/phases/05-manual-entry/05-RESEARCH.md §"Common Pitfalls" 1-3 and 5 (lines 579-604) — amount→core, tags fix, description-from-draft, date format
    - src/pages/ReviewPage.tsx (the file being modified — current state vars, handleSave, rendered FormFields)
    - src/pages/ReviewPage.test.tsx (Phase 4 assertions that MUST stay green — do NOT modify this file)
  </read_first>
  <files>src/pages/ReviewPage.tsx</files>
  <action>
    Apply the exact ReviewPage diff from RESEARCH.md item 4. Change the import and the location.state cast from `ExtractedDraft` to `ReviewDraft` (import type ReviewDraft from ../services/extractMetadataFromUrl). Initialize `description` from `initialDraft?.description ?? ''` (currently useState('')). Add three new state variables: `occurredAt` initialized from `initialDraft?.occurredAt` converted epoch-ms→'YYYY-MM-DD' via `new Date(ms).toISOString().split('T')[0]` (empty string when absent); `amount` initialized from `initialDraft?.amount != null ? String(initialDraft.amount) : ''`; `tags` initialized from `initialDraft?.tags?.join(', ') ?? ''`. In handleSave, compute `parsedAmount = parseFloat(amount)`, `parsedDate = occurredAt ? Date.parse(occurredAt) : NaN`, `parsedTags = tags.split(',').map(t=>t.trim()).filter(Boolean)`. Replace the hardcoded `tags: [] as string[]` with `tags: parsedTags`. Add conditional optional fields to the entry object: spread `amount: parsedAmount` only when !isNaN(parsedAmount), and `occurredAt: parsedDate` only when !isNaN(parsedDate). Keep the existing safeSourceUrl/location/description conditional spreads. Render three new FormFields after the existing sourceUrl field: a "Date" field (id "review-occurred-at", type="date") shown always; an "Amount" field (id "review-amount", type="number", placeholder "0.00") shown only when `type === 'expense' || initialDraft?.amount != null`; a "Tags" field (id "review-tags", placeholder "tag1, tag2") shown always. Do NOT change the existing Title/Location/Description/Source URL fields, the guards, isSafeUrl, or the Save/Cancel buttons. Do NOT modify ReviewPage.test.tsx.
  </action>
  <acceptance_criteria>
    - ReviewPage imports `ReviewDraft` (not ExtractedDraft) and casts location.state to `{ draft?: ReviewDraft }`.
    - description state initializes from initialDraft?.description ?? ''.
    - New state vars occurredAt (date-string), amount (string), tags (comma string) exist with the specified initializers.
    - handleSave persists tags: parsedTags (not []), conditionally includes amount when !isNaN(parsedAmount) and occurredAt when !isNaN(parsedDate).
    - Amount FormField renders only when type==='expense' or the draft already carries amount; Date and Tags FormFields always render.
    - `<input type="number">` is used for amount (role spinbutton, not textbox) so Phase 4 textbox-based queries are unaffected.
    - `npx vitest run src/pages/ReviewPage.test.tsx` passes with ZERO Phase 4 regressions (tags===[] for empty input, Title label query, sourceUrl display, javascript: guard, unknown-domain guard, no-draft redirect all still pass).
    - `npx vitest run` (full suite incl. Phase 4 CaptureUrlPage tests) passes; `npx tsc -b` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/ReviewPage.test.tsx && npx vitest run</automated>
  </verify>
  <done>ReviewPage carries the richer ReviewDraft; amount/occurredAt/description/tags persist; all Phase 4 tests stay green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user form input → ReviewDraft → IndexedDB | Untrusted user-typed strings/numbers cross into persisted LifeLogEntry records |
| location.state draft → ReviewPage | Draft object (from manual or URL flow) read and rendered/persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering/EoP | ReviewPage rendered title/description/notes | mitigate | React DOM auto-escapes string values; user input rendered as JSX text/`value` props only — no `dangerouslySetInnerHTML`. No change needed; verified by code. |
| T-05-02 | Tampering | buildReviewDraft + ReviewPage handleSave numeric amount | mitigate | parseFloat + isNaN guard in both buildReviewDraft (skips NaN amount) and handleSave (amount only spread when !isNaN) — never persist a NaN amount. Asserted by entryFields unit test (non-numeric amount → no draft.amount). |
| T-05-03 | Spoofing | sourceUrl scheme | mitigate | ReviewPage `isSafeUrl()` guard unchanged; only http/https sourceUrl persisted. Manual drafts carry no sourceUrl (sourceUrl undefined → skipped). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed in Phase 5 (RESEARCH.md §"Package Legitimacy Audit": Not applicable). No supply-chain surface added. |
</threat_model>

<verification>
- `npx vitest run src/config/entryFields.test.ts` — config + mapper unit suite green
- `npx vitest run src/pages/ReviewPage.test.tsx` — Phase 4 ReviewPage regression green
- `npx vitest run` — full suite (incl. Phase 4 capture flow) green
- `npx tsc -b` — no type errors (confirms ExtractedDraft → ReviewDraft assignability holds)
</verification>

<success_criteria>
- ENTRY_FIELDS covers all 7 EntryTypes with the exact research mapping (MAN-02 source of truth).
- buildReviewDraft correctly maps expense amount→core, place name→title/address→location, media creator→metadata, and skips empty/NaN values.
- ReviewPage reviews and persists amount, occurredAt, description, and parsed tags (MAN-03 save path enabled).
- Phase 4 ReviewPage and capture tests pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/05-manual-entry/05-01-SUMMARY.md` when done.
</output>
