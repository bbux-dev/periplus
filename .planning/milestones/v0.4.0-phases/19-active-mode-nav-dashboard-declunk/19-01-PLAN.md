---
phase: 19-active-mode-nav-dashboard-declunk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/AppShell.tsx
  - src/components/layout/AppShell.test.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/DashboardPage.test.tsx
autonomous: true
requirements: [MODE-03, MODE-04, DASH-04]

must_haves:
  truths:
    - "The hamburger menu has an Active Mode item that lists the modes and lets the user activate one after confirming/editing the instance label"
    - "Activating a mode from the menu persists it (activateMode) and the app bar reflects mode · label immediately"
    - "The app bar shows the current active mode and instance label"
    - "The dashboard renders only the active mode's buttons — no layout/mode switcher chips in steady state"
    - "Switching the active mode changes which mode's buttons the dashboard shows"
  artifacts:
    - path: "src/components/layout/AppShell.tsx"
      provides: "Active Mode menu item + inline label-prompt (activateMode) and app-bar mode·label display"
      contains: "activateMode"
    - path: "src/pages/DashboardPage.tsx"
      provides: "Active-mode-driven layout rendering; LayoutChips switcher removed; idempotent first-run mode activation"
      contains: "useActiveMode"
  key_links:
    - from: "src/components/layout/AppShell.tsx"
      to: "src/services/activeMode.ts"
      via: "useActiveMode + activateMode + defaultInstanceLabel + listModes"
      pattern: "activateMode\\("
    - from: "src/pages/DashboardPage.tsx"
      to: "src/services/activeMode.ts"
      via: "useActiveMode() drives activeLayout selection"
      pattern: "useActiveMode\\("
---

<objective>
Deliver the v0.4.0 de-clunk. Move mode switching into the hamburger menu (Active Mode item + inline
label prompt), show `mode · label` in the app bar, and strip the dashboard to ONLY the active mode's
buttons (remove the on-dashboard LayoutChips switcher). Consumes the Phase 18 active-mode model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-active-mode-nav-dashboard-declunk/19-CONTEXT.md

<interfaces>
```typescript
// src/services/activeMode.ts (Phase 18)
useActiveMode(): { mode: string; label: string } | undefined
activateMode(mode: string, label?: string): Promise<void>
defaultInstanceLabel(mode: string, now?: Date): string   // `${mode}-${Mon}-${Year}`
listModes(config: ShortcutConfig): string[]
// src/services/configRepository.ts
useShortcutConfig(): ShortcutConfig | undefined
activeModeRepository.get(): Promise<{mode,label}|undefined>   // for first-run check
```
Conventions: only `var(--color-*)` tokens; `type="button"` on buttons; named exports; reuse existing
Button/cn. TDD. fake-indexeddb is auto-hoisted in test-setup — seed config via `configRepository.put`
and wrap live-query-affecting writes in `act()`. NO `window.prompt`/`window.confirm`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppShell — Active Mode menu + label prompt + app-bar display</name>
  <files>src/components/layout/AppShell.tsx, src/components/layout/AppShell.test.tsx</files>
  <read_first>
    - src/components/layout/AppShell.tsx (full — top bar row L54-79; dropdown nav L82-167)
    - src/services/activeMode.ts (useActiveMode, activateMode, defaultInstanceLabel, listModes)
    - src/services/configRepository.ts (useShortcutConfig)
    - src/components/layout/AppShell.test.tsx (renderAt harness; add a seeded-config variant)
  </read_first>
  <behavior>
    - App bar: between the home button (left) and hamburger (right), render the active mode as
      `{mode} · {label}` when `useActiveMode()` is defined; render nothing when undefined. Keep the
      existing layout balanced (left/center/right) and truncate long labels.
    - Menu: add an "Active Mode" item (button) to the dropdown. Clicking it toggles a submenu listing
      the modes from `listModes(useShortcutConfig() ?? {layouts:[]})`.
    - Selecting a mode from the list sets a pending selection and shows an inline labelled text input
      pre-filled with `defaultInstanceLabel(mode)`, plus Confirm + Cancel buttons.
    - Confirm calls `await activateMode(pendingMode, inputValue)`, clears the pending state, and closes
      the whole menu (setOpen(false)); the app bar updates to the new mode · label.
    - Cancel clears the pending selection without persisting.
    - Existing menu links + nav tree + home button + Escape/outside-close behavior remain unchanged.
  </behavior>
  <action>
    Import useActiveMode/activateMode/defaultInstanceLabel/listModes and useShortcutConfig. Add state:
    modeSubmenuOpen, pendingMode (string|null), pendingLabel (string). Render the app-bar display and
    the "Active Mode" menu section (use var(--color-*) tokens, type="button"). Write AppShell.test.tsx
    cases FIRST (RED): seed a config (DayToDay/Travel/WorkTrip) via configRepository.put in a beforeEach
    or per-test; (a) app bar shows "Travel · <label>" after activeModeRepository.put({mode:'Travel',label:'X'});
    (b) the menu exposes an "Active Mode" control; (c) opening it lists the mode names; (d) selecting
    "Travel" shows a label input pre-filled with defaultInstanceLabel('Travel'); (e) editing the label +
    Confirm persists via activateMode (assert activeModeRepository.get) and the app bar reflects it;
    (f) Cancel does not persist. Keep existing AppShell tests green. Wrap writes/await in act()/waitFor.
  </action>
  <verify><automated>pnpm exec vitest run src/components/layout/AppShell.test.tsx</automated></verify>
  <acceptance_criteria>
    - `grep -c "activateMode" src/components/layout/AppShell.tsx` >= 1; `grep -c "useActiveMode" src/components/layout/AppShell.tsx` >= 1.
    - No hardcoded colors introduced: `grep -nE "#[0-9a-fA-F]{3,6}|rgb\(" src/components/layout/AppShell.tsx` returns nothing new.
    - RTL proves: app-bar mode·label; Active Mode item; mode list; pre-filled label; Confirm persists + updates bar; Cancel no-op; existing tests green.
    - `pnpm exec tsc -b` clean.
  </acceptance_criteria>
  <done>The hamburger menu switches the active mode (with a label prompt) and the app bar shows mode · label; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DashboardPage de-clunk — only the active mode's buttons</name>
  <files>src/pages/DashboardPage.tsx, src/pages/DashboardPage.test.tsx</files>
  <read_first>
    - src/pages/DashboardPage.tsx (full — seeding effect L25-38; layout derivation L41-50; LayoutChips L70-75)
    - src/services/activeMode.ts (useActiveMode, activateMode, activeModeRepository)
    - src/pages/DashboardPage.test.tsx (existing chip tests to remove/replace; render harness)
    - src/pages/ManageShortcutsPage.tsx (confirm LayoutChips stays used there — do NOT delete the component)
  </read_first>
  <behavior>
    - The dashboard derives the rendered layout from the active MODE:
      `activeLayout = layouts.find(l => l.name === useActiveMode()?.mode) ?? layouts[0]`.
    - The `<LayoutChips>` switcher and `handleLayoutSelect` are REMOVED from the dashboard. Only the
      active mode's `ShortcutRow`s render (plus the existing Quick Capture / nav / entries / settings links).
    - First-run activation (idempotent): in the one-shot mount effect, after ensuring a config exists,
      if `activeModeRepository.get()` is undefined and the config has at least one layout, call
      `activateMode(firstLayout.name)` so a default mode is active. Never overwrite an existing selection.
    - No behavior change to the capture orchestrator (handleTap/HoleSheet/SavedToast) — those still work
      and now inherit the Phase 18 mode stamp.
  </behavior>
  <action>
    Replace useActiveLayoutName/activeLayoutRepository usage + LayoutChips with useActiveMode-driven
    selection; delete the LayoutChips import + render + handleLayoutSelect from this file ONLY. Extend
    the seeding effect with the idempotent first-run activateMode. Update DashboardPage.test.tsx FIRST
    (RED): REMOVE the chip-switcher tests (aria-pressed chip, "+ New" chip, click-to-switch-persists);
    ADD: no layout-switcher chips render on the dashboard (e.g. querying the old chip names/role finds
    nothing beyond the shortcut rows); the active mode's shortcut names render as buttons; setting a
    different active mode (activeModeRepository.put) renders that layout's shortcuts; first-run mount
    with a seeded config results in an active mode being persisted. Keep all non-chip dashboard tests green.
  </action>
  <verify><automated>pnpm exec vitest run src/pages/DashboardPage.test.tsx</automated></verify>
  <acceptance_criteria>
    - `grep -c "LayoutChips" src/pages/DashboardPage.tsx` == 0; `grep -c "useActiveMode" src/pages/DashboardPage.tsx` >= 1.
    - LayoutChips component still exists and is still imported by ManageShortcutsPage (`grep -rl LayoutChips src` includes ManageShortcutsPage).
    - RTL proves: no dashboard switcher chips; only active mode's shortcuts render; changing active mode changes rows; first-run persists a default mode.
    - `pnpm exec tsc -b` clean.
  </acceptance_criteria>
  <done>The dashboard shows only the active mode's buttons with no switcher; the active mode drives rendering; tests green.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run src/components/layout/AppShell.test.tsx src/pages/DashboardPage.test.tsx` green.
- Full suite `pnpm exec vitest run` green (no regression; chip tests intentionally replaced).
- `pnpm exec tsc -b` clean; `pnpm exec vite build` clean.
- Manual north-star check (note in SUMMARY): the dashboard's steady-state on-screen control count is
  LOWER than before (switcher removed), and switching is reachable from the menu.
</verification>

<success_criteria>
- MODE-03: active mode switched from a hamburger "Active Mode" item with a label prompt.
- MODE-04: app bar shows mode · label.
- DASH-04: dashboard renders only the active mode's buttons; on-dashboard switcher removed.
</success_criteria>

<output>
Create `.planning/phases/19-active-mode-nav-dashboard-declunk/19-01-SUMMARY.md` when done.
</output>
