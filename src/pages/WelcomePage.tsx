export function WelcomePage() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center px-6 py-12 bg-[var(--color-background)] text-[var(--color-foreground)]">
      <div className="w-full max-w-sm flex flex-col items-center gap-8">
        <h1 className="text-3xl font-bold tracking-tight">Life Log</h1>
        {/* Counter slot — mounted in Task 2 */}
      </div>
    </div>
  )
}
